{
  "name": "@telga/persistence",
  "version": "0.0.0",
  "private": true,
  "description": "Telga persistence layer. SQLite behind a LedgerDriver interface — append-only ledger enforced by database triggers, not only by TypeScript.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@telga/domain": "*",
    "better-sqlite3": "^13.0.3"
  }
}
