---
title: Decision Log
type: governance
status: draft
owner: telga
created: 2026-08-19
updated: 2026-08-19
tags:
  - telga
  - governance
  - decision
related:
  - "[[00 Home]]"
  - "[[Launch Gates]]"
  - "[[Founders and Roles]]"
  - "[[Decision]]"
depends_on: []
implements: []
validates: []
decision_status: confirmed
---

# Decision Log

Every material decision, with its status. Use the [[Decision]] template for anything needing a
full record. `decision_status` values: `proposed` · `accepted` · `superseded` · `rejected` ·
`pending`.

## Accepted

| # | Date | Decision | Rationale | Recorded by |
|---|---|---|---|---|
| D1 | 2026-08-19 | `CLAUDE.md` at the repository root is the authoritative specification; `CLAUDE.pdf` is a rendering of it | The PDF is clipped at the right margin — see [[Source Specification Clipped In PDF]] | Claude Code |
| D2 | 2026-08-19 | Vault content lives in `docs/obsidian/`; the vault boundary stays the repository root | Notes and source share one Graphify graph; the repo root is already a registered Obsidian vault | Claude Code |
| D3 | 2026-08-19 | Full, unabbreviated note names throughout the vault | Founder instruction; abbreviated names break wiki links and graph nodes | Founder |
| D4 | 2026-08-19 | SQLite (WAL, STRICT) for the Phase 2 ledger, behind a driver interface; Postgres at Phase 3 | No Docker or Postgres on the build machine; SQLite gives real transactions and constraints now, and the interface makes the move a driver swap | Founder |
| D5 | 2026-08-19 | Responsive web POS / PWA as the first client surface; native Android deferred to Phase 3 | No JDK, Kotlin or Gradle on the build machine; the prototype's purpose is proving domain logic | Founder |
| D6 | 2026-08-19 | Money stored as integer santim (ETB minor units) | [[Ledger Invariants]] rule 9 — no binary floating point | Claude Code |
| D7 | 2026-08-19 | Default automatic pending maximum of 5 minutes | `CLAUDE.md` default, pending provider-specific rules from the provider agreement terms (commercial material, kept outside this repository) | Founder |
| D8 | 2026-08-20 | Add a `BANK_CLEARING` contra account alongside the five segregated buckets | Double entry requires a counter-side for a funding credit; without it a deposit cannot be posted without breaking invariant 2. It holds no merchant value and appears in no merchant-facing balance. | Claude Code |
| D9 | 2026-08-20 | `PENDING → REVERSAL_REQUIRED` is legal without passing through `UNDER_REVIEW` | A provider callback can state that value was taken and delivery did not happen; that needs no human determination first. Required by the founder's test list. | Founder |
| D10 | 2026-08-20 | `RESERVED → PROCESSING` is legal without passing through `SUBMITTED` | Adapters that do not acknowledge a submission separately need the direct edge. Required by the founder's test list. | Founder |
| D11 | 2026-08-20 | The idempotency key derives from **request identity** (merchant, device, client request id), not from payload contents | Found while writing the payload-mismatch test: if the key hashed the amount and recipient, changing them would change the key, so a mismatch could never be detected — a tampered request would look like a brand new sale. The payload is covered by a separate fingerprint. | Claude Code |
| D12 | 2026-08-20 | The three balance buckets are **real ledger accounts** (`MERCHANT_AVAILABLE`, `MERCHANT_RESERVED`, `MERCHANT_UNDER_REVIEW`), not figures derived from reservation rows | The founder's persistence brief describes moving value between buckets "through new ledger postings". Making each bucket an account means every movement is an auditable balanced posting rather than an inferred number, and the four views become simple sums. The in-memory `MERCHANT_FUNDS` form is retained and both sum to the same merchant total. | Claude Code |
| D13 | 2026-08-20 | `better-sqlite3` v13, WAL, STRICT, `synchronous = FULL` | Verified to load natively on Node 25 with prebuilt binaries — no compiler needed. `FULL` rather than the usual `NORMAL`: `NORMAL` can lose recent commits on power loss, and a lost commit here is a merchant's money unaccounted for. | Claude Code |
| D14 | 2026-08-20 | **Production rollback is forward-fix only.** No `down` migrations | A ledger cannot be un-migrated without risking history. The only rollback is the per-migration transaction at apply time. A schema mistake is corrected by a new forward migration; a data mistake by an `ADJUSTMENT` entry. | Claude Code |
| D15 | 2026-08-20 | Recipient numbers are stored **masked and salted-hashed**, never in full | Support needs to identify a transaction, not to hold every subscriber number in a database for years. An unsalted digest of a phone number is a rainbow-table lookup, so the hash is salted with a deployment secret. | Claude Code |
| D16 | 2026-08-20 | The provider call sits **between two units of work**, not inside one | A SQLite transaction is synchronous; holding one open across a network call would block every other writer for as long as the provider takes to answer, and the provider is the component that may not answer at all. That gap is why `PENDING` exists. | Claude Code |
| D17 | 2026-08-20 | A thrown provider call becomes `PENDING`, not `FAILED` | We do not know whether it delivered. Treating an exception as failure would release value on a sale that may have completed. | Claude Code |
| D18 | 2026-08-20 | Escalation to `UNDER_REVIEW` is **time-based**, driven by a `pending_resolutions` row carrying a deadline | Without a scheduled job, a silent provider leaves merchant value held with nothing chasing it. The row also carries an attempt count, an early signal of provider degradation. | Claude Code |
| D19 | 2026-08-20 | A failure in the outcome unit of work **propagates**; it is not converted into a result | The instruction was explicit: do not catch errors and return success. The cost is a transaction that can stick at `PROCESSING`, recorded as open risk A31. | Founder |

| D20 | 2026-08-20 | The recovery sweep also sweeps `PENDING`, not only `PROCESSING` and `RESERVED` | `resolvePending` acts only when something calls it. An unattended system has nothing calling it, so without `PENDING` in the sweep a transaction the sweep itself moved to pending would hold merchant money forever and the escalation deadline would never be enforced. | Claude Code |
| D21 | 2026-08-20 | `RESERVED` with no provider reference is **proof** the provider was never called | `createSale` transitions to `PROCESSING` before it submits. This is the only case where recovery releases funds without a provider answer, and it rests on evidence rather than assumption. A `RESERVED` row carrying a provider reference is treated as uncertain. | Claude Code |
| D22 | 2026-08-20 | The pending clock starts when the transaction entered the in-flight state, not when the sweep noticed it | A transaction stuck for an hour must not receive a fresh grace period because a worker only just reached it. The merchant's money has already been held for that hour. | Claude Code |
| D23 | 2026-08-20 | Recovery claims use a time-bounded lease, not a permanent lock | A worker that dies must not hold a merchant's money hostage. An expired lease is reclaimable; a permanent lock would need manual intervention exactly when nobody is watching. | Claude Code |
| D24 | 2026-08-20 | A refund, reversal or exceptional balance action requires supervisor approval, enforced in code | `completeReversal` refuses any role outside `OPS_APPROVER` and `ADMIN` and records the approver on the support case. A rule about moving money that lives only in a runbook is not a control. | Claude Code |

| D25 | 2026-08-20 | Sweeps are scheduled **fixed-delay**, from the end of the previous sweep, not fixed-rate | Fixed-rate scheduling degenerates into continuous execution the moment a sweep takes longer than its interval — the scheduler spends the rest of its life catching up on a backlog it can never clear. Fixed delay cannot run away. | Claude Code |
| D26 | 2026-08-20 | Database and schema failures are **fatal** to the worker; provider failures back off | Retrying into a broken connection or a wrong schema helps nobody and hides the fault. A provider being unreachable is a normal transient condition and is exactly what backoff is for. | Claude Code |
| D27 | 2026-08-20 | The production worker policy contains **no numbers at all**, and production never falls back to development values | Every value depends on provider timeout semantics and connectivity data that do not exist. A deployment missing a setting fails at startup rather than running a developer's numbers against merchant money. | Claude Code |
| D28 | 2026-08-20 | Real system time is read in exactly one place — `systemWorkerClock` at the worker boundary | Everything below takes time as an argument, which is what makes the recovery and domain layers testable. A test asserts the recovery service contains no `Date.now(`. | Claude Code |
| D29 | 2026-08-20 | A shutting-down worker releases **only its own claims**; everything else is left to expire | Touching another worker's claim would let two workers act on one transaction. An abandoned lease expires on its own, so nothing is stranded and no claim row is ever deleted. | Claude Code |

| D30 | 2026-08-20 | The build emits **CommonJS**, with `{"type":"commonjs"}` stamped into each `dist/` | The sources use extensionless relative imports, which Node's ESM resolver rejects. CommonJS resolves them natively, so no source changes were needed. The alternative — appending `.js` to ~60 imports across five packages — buys nothing else. `verbatimModuleSyntax` is disabled for the build only. | Claude Code |
| D31 | 2026-08-20 | Each package builds to **its own** `dist/`, and `main` points there | Cross-package `require('@telga/domain')` then resolves to built JavaScript at runtime, while Vitest aliases and tsconfig `paths` keep development on the sources. One mechanism, no duplication. | Claude Code |
| D32 | 2026-08-20 | The worker CLI has a `--once` mode that sweeps once, releases its claims and exits | An operator needs a way to run a single sweep by hand, and a test needs process separation without a supervised loop running forever. The same mode serves both. | Claude Code |

| D33 | 2026-08-20 | The worker **does not migrate**. It refuses to start against an unmigrated database (exit 6); migrations are applied once by a single writer with `--migrate` | `createSqliteDriver` migrated on open, which made every worker process a migrator — the untested concurrent case recorded as A30. One flag removes the risk without building a lock nothing needs yet. | Claude Code |
| D34 | 2026-08-20 | An intermittent test is investigated, never retried, delayed, skipped or loosened | A flake in recovery code is a claim about money that sometimes fails. It is either a real defect or a test asserting the wrong thing; both deserve finding. | Founder |
| D35 | 2026-08-20 | Graphify is **not** a CI step; `docs:validate` is the deterministic documentation gate | Graphify is a locally-installed tool, not a declared dependency. A CI step for it would install something unpinned or quietly no-op — worse than a documented local step. | Claude Code |

| D36 | 2026-08-20 | The POS screens are **pure functions returning an element tree**, with no UI framework, no bundler and no DOM emulator | What the UI tests must assert — the banner is present, an uncertain state renders no success affordance, the retry instruction exists as text, controls have accessible names — are properties of the tree, not of a layout engine. Same reasoning as D30. The limitation is stated: component-level, not browser-level. | Claude Code |
| D37 | 2026-08-20 | An unknown outcome is an **HTTP success**: `POST /sales` returns 201 with `kind: "PENDING"` | Making it a 4xx would teach a client to treat an unknown outcome as an error, which is the exact mistake the pending path exists to prevent. | Claude Code |
| D38 | 2026-08-20 | The POS exposes **no reversal endpoint** | `completeReversal` requires a supervisor approval. Exposing it without an authenticated supervisor session would be a way *around* that approval, not an implementation of it. | Claude Code |
| D39 | 2026-08-20 | Fourteen Amharic strings stay **missing** rather than machine-translated; `translate()` reports the English fallback | An unreviewed guess that looks finished is worse than a visible gap, and a native reviewer needs to see what is actually outstanding. | Founder |

| D40 | 2026-08-20 | The child-process tests build **only when `dist` is stale**, and `testTimeout` is stated explicitly at 30s | Compiling eight packages inside the test run saturated a two-core machine and tripped two of Vitest's five-second budgets — the reporter RPC and an unrelated test. The guarantee needed is that the tests run *current* output, not that a build happens every time. The timeout is a resource budget; no assertion was loosened, no retry added, nothing skipped. | Claude Code |

| D36 | 2026-08-21 | **Identity comes from a server-side session, never from a request.** A client-supplied merchant id is a consistency check and is refused on mismatch | A URL parameter is not an identity boundary. Anything a client can edit cannot decide what a client may read. | Founder |
| D37 | 2026-08-21 | **scrypt from `node:crypto`**, not argon2, for PINs and device keys | argon2 needs a native dependency and a build step to defend a six-digit PIN that is already defended by lockout and device binding. `N=16384` is real work with no supply-chain question. Production parameters NOT_YET_CONFIRMED. | Claude Code |
| D38 | 2026-08-21 | **The device is re-checked on every request**, not only at sign-in | The window that matters is between a POS being stolen and its session expiring. Checking at sign-in only would leave a revoked device working for up to 12 hours. | Claude Code |
| D39 | 2026-08-21 | **CSRF tokens in addition to `SameSite=Strict`** | `SameSite` is a browser behaviour, not a server guarantee; an old browser, a proxy or a same-site subdomain weakens it. A server-issued token does not depend on the client behaving. | Claude Code |
| D40 | 2026-08-21 | **A merchant role never holds `REVERSAL_APPROVE`, `FUNDS_RELEASE`, `TRANSACTION_FORCE_STATE`, `RECOVERY_CONFIGURE`, `PROVIDER_OVERRIDE_OUTCOME`, `ADMIN_DIAGNOSTICS` or `DEVICE_REVOKE`** — enforced by a second list consulted independently of the grant table | Every one is a money control. Two locks that must both fail is a materially different guarantee from one lookup. A stolen device is exactly the case where the holder must not be able to revoke it. | Founder |
| D41 | 2026-08-21 | **No response body carries a token or a secret**, with one deliberate exception: the device key at enrolment, which bypasses the display gate rather than weakening it | The redaction gate refused the first sign-in handler because it returned the CSRF token in the body. Loosening the gate for one response would have loosened it for all of them; the token travels in its own cookie instead. | Claude Code |
| D42 | 2026-08-21 | **A device refusal outranks a session refusal**, and is audited once per session | Revoking a device revokes its sessions, so the next request would report `SESSION_REVOKED` — a 401 sending the operator to a sign-in they cannot pass. Reporting the device reason keeps them out of a loop. Auditing once stops a stolen POS flooding the trail. | Claude Code |
| D43 | 2026-08-21 | **Training thresholds are named as training thresholds**: 6-digit PIN, 5 failures, 5-minute lockout, 15-minute idle, 12-hour lifetime, 30 sales/minute, 16 KB body | Real numbers depend on how a counter behaves over a shift, which nobody has measured. Naming them as training values keeps a production claim from being made by default. | Founder |

| D44 | 2026-08-21 | **The CSP allows inline script and style by per-response nonce**, never `unsafe-inline` | `unsafe-inline` permits any injected inline script, which is most of what a CSP exists to stop. A fresh 128-bit nonce per response cannot be read by injected markup. | Claude Code |
| D45 | 2026-08-21 | **A forwarding header is believed only from a configured trusted address**, and there is no "trust all proxies" option | Believing one from any client lets a plain HTTP request set a `Secure` cookie the browser never returns — and makes an insecure deployment report itself secure. That single setting would turn the control into a decoration. | Founder |
| D46 | 2026-08-21 | **Plain HTTP is refused on any non-loopback binding**, rather than warned about | The whole safety argument for plain HTTP is that nobody else can reach it. A LAN binding removes the argument, so a warning would be a note attached to an unsafe deployment. | Founder |
| D47 | 2026-08-21 | **Telga never generates or writes TLS key material.** `TRAINING_HTTPS` requires explicit paths | A tool that quietly creates a key creates one somewhere, and somewhere becomes a repository, a backup or an image. | Claude Code |
| D48 | 2026-08-21 | **Certificate and key are checked as a pair at startup** | A mismatched pair otherwise fails per-connection during the handshake, as an error a browser renders as an unexplained failure. One clear refusal beats that. | Claude Code |
| D49 | 2026-08-21 | **A simulated opening balance is a named flag** (`--training-float`), off by default | Creating a balance is a money operation even when the money is simulated. It should never be a side effect of setting up an operator. | Founder |
| D50 | 2026-08-21 | **Test and build commands are never run concurrently** on this two-core machine | An overlapping run produced four failures whose output was truncated; isolated runs of the same suite passed. Recorded as A55. | Founder |

| D51 | 2026-08-21 | **Every database unit of work begins `IMMEDIATE`**, not deferred | A deferred transaction upgrades from reader to writer on its first write, and in WAL mode that upgrade fails with `SQLITE_BUSY_SNAPSHOT` when another connection has written — an error `busy_timeout` does not wait out and which cannot be safely retried, because the transaction's reads may be stale. Taking the write lock up front converts it into an ordinary wait. | Founder |
| D52 | 2026-08-21 | **No retry wrapper around a failed unit of work.** The fix removes the error rather than surviving it | A retry around an ambiguous commit is exactly what the ledger rules forbid. `BEGIN IMMEDIATE` makes the contention waitable, so there is nothing left to retry. | Founder |
| D53 | 2026-08-21 | **Worker health reads the sweep outcome, not only the ledger residual** | A zero residual says the books are consistent; it says nothing about whether recovery worked. A sweep that claimed work and resolved none of it reported `HEALTHY`, which was the wrong answer twice over. | Founder |
| D54 | 2026-08-21 | **A stress harness preserves the failing database**, and the worker reports `skipped`, `recoveryFailures`, `stoppedEarly` and `failureReasonCodes` | The evidence already existed in the audit trail; the test deleted it, and the CLI did not surface it. Diagnosing A54 was impossible until both were fixed. | Claude Code |
| D55 | 2026-08-24 | **`.gitignore`'s generated-output patterns must be exact, not generic prefixes** — removed the bare `build/` pattern, keeping `dist/` | A pattern meant for compiler output silently matched `tests/build/`, a real source directory, so it was never committed. CI found "No test files found" on a fresh checkout while every local run passed, because Vitest reads the filesystem directly, not git state. Recorded as A57. | Claude Code |
| D56 | 2026-08-24 | **Added `scripts/check-ci-test-paths.mjs`** as the first CI step, failing fast if any workflow-referenced `tests/...` path has zero files tracked by git | A57 was invisible to every existing check because `check-committed.mjs` and `validate-vault.mjs` both reason from `git ls-files`, which is exactly what a shadowed directory is absent from. This is the same shape of gap those checks fill for secrets and boundary crossings, applied to CI test discovery. | Claude Code |
| D57 | 2026-08-24 | **Each stress script scopes its own vitest invocation to one named file**, never a shared directory glob | `stress-recovery.mjs`'s soak pass ran the whole `tests/stress/` glob unscoped, silently also executing the unrelated A54 scenario (which needs a build the `recovery stress` CI job never runs) and reporting its failure as `soak-200`. `stress-child-process.mjs` already scoped its own two invocations this way; the same discipline now applies to both scripts. Recorded as A58 — kept a distinct ID from A44, which tracks a different, unreproduced failure shape. | Claude Code |
| D58 | 2026-08-24 | **Upgraded `actions/checkout` and `actions/setup-node` from `v4` to `v5`**, in their own maintenance commit, separate from any functional CI fix | Both `v4` pins declare `node20` in their actual `action.yml`; GitHub now forces that onto Node 24 at runtime and warns on every job. Verified `v5` declares `node24` directly from each action's metadata file rather than a summarized changelog. No inputs, triggers, timeouts, reporters, or the Node matrix changed. Recorded as A59, resolved on remote run `32730213755` — Success, Annotations panel confirmed empty by direct owner inspection. | Claude Code |
| D60 | 2026-08-24 | **`GET /api/health/ready` reuses `recoveryGauges`/`evaluateAlerts`** rather than a second definition of recovery health | Those functions already existed and are what the worker's own observability computes from. A second, independent "is the queue fine" check would risk silently disagreeing with the one the worker already trusts. | Claude Code |
| D61 | 2026-08-24 | **Every session is revoked and every recovery claim is released, unconditionally, on restore** — not only ones already known-expired or known-revoked | A session or claim from a backup predates the restore point by definition; the restored copy's own timeline has no worker or operator legitimately holding either. Unconditional release is simpler than a conditional policy and cannot be half-applied. Operators sign in again; the next real sweep re-claims cleanly. Recorded in [[Backup and Restore Runbook]] and the backup/restore implementation note (implemented separately). | Claude Code |
| D62 | 2026-08-24 | **`@telga/backup` restores checksum-first, before any file copy to the target** | A corrupt backup must never produce even a partial target file. Checking the checksum against the backup file itself, before `copyFileSync` runs, makes a corrupt-backup refusal leave nothing behind to clean up. | Claude Code |
| D63 | 2026-08-24 | **Backup and restore refuse every path by default** — `TELGA_BACKUP_ALLOWED_ROOTS` has no default value | An unset allow-list must mean "nothing is permitted," not "anywhere is fine." Matches the same fail-closed posture as the trusted-proxy design (D45) — no "trust everything" option exists for either. | Claude Code |
| D64 | 2026-08-24 | **Backup and restore never migrate the database they open** — both use `new SqliteLedgerDriver()` directly, never `createSqliteDriver` | Migration is a single-writer startup procedure belonging to the worker or the POS ([[Migration Ownership]]). A backup or restore tool that silently migrated would be an unreviewed second writer — exactly the untested concurrent-migration shape recorded as A30. | Claude Code |
| D65 | 2026-08-25 | **Selected the local/office machine as the host category for Phase 2/3 controlled training deployment** — a conditional training decision, not production approval | The project owner confirmed the deciding facts that [[Deployment Target Evaluation]] and [[Persistent Host Deployment Plan]] had identified as missing: a reliable office computer is available, the office internet connection is reliable, the computer can run Telga continuously, the intended use is controlled training only, and remote public access is not required at this stage. No vendor or VPS is selected, no host is provisioned, and no launch gate moves. See [[Persistent Host Deployment Plan]] §2. Recorded as A60 — kept OPEN; category selection is not the same as host hardening, backup, restore, HTTPS, supervision, or real-infrastructure evidence, all of which remain unrecorded. | Founder |
| D66 | 2026-08-25 | **The training voucher flow adopts a South African voucher reference's screen *sequence* only** — main menu → vouchers → airtime → network → product type → amount → order details → PRINT → PIN → result. Networks are clearly-labelled simulated placeholders (`Network A (simulated)`…`Network D (simulated)`); no real Vodacom/MTN/Cell C/Telkom/k'nect name, no ZAR, no live South African network integration appears anywhere. Telga's Ethiopia/ETB charter (CLAUDE.md §1–2) is unchanged | The founder explicitly approved the reference as a UX pattern only, not a market-expansion decision, and separately confirmed ETB is the only currency the system may use. `transactions.currency CHECK (currency = 'ETB')` and the new `pending_orders.currency CHECK (currency = 'ETB')` both enforce this structurally, not only by convention. See [[Voucher Purchase Flow]]. | Founder |
| D67 | 2026-08-25 | **Voucher PIN-authorization lockout (`PIN_AUTH`) is a separate, purely time-windowed mechanism** — never `recordFailedLogin`, never `merchant_users.failed_attempts` or `locked_until` | The founder explicitly rejected sharing the login lockout counter: a wrong voucher transaction PIN must not lock a merchant out of signing in. Implemented as a trailing failure count against `auth_attempts` (scope `PIN_AUTH`, subject = operator id) — locked while 5+ failures fall within the last 5 minutes, with no stored "locked until" value to expire or clear. Proven by a test that fails PIN_AUTH five times, confirms `merchant_users.failed_attempts` and `locked_until` are untouched, and confirms a fresh login with the correct PIN still succeeds. See `services/api/src/application/voucherOrders.ts` and `tests/application/voucher-orders.test.ts`. | Founder |
| D68 | 2026-08-25 | **Telga is one application** — one repository, one login, one authenticated session — presenting two internal module tiles (TELGA, TELGA PAY) from a single in-browser launcher screen. Corrects an earlier draft of this same work that mistakenly described "two separate apps"; that framing was never implemented and is superseded here. Telga Pay is built as a **UI-only training card-payment simulator** in this version — no new table, no migration, no persistence — with an explicit training banner on every screen and no code capable of reaching a real card network, processor, or bank | The founder was explicit: one login, one session, no second authentication system, no native-app claim (confirmed no Electron/Tauri/packaging exists in this repository). Flagged, not silently resolved: `CLAUDE.md` §2 lists "payment acceptance," "cash-in/cash-out," and "wallets" as disabled until legal review and an authorized-partner structure exist; a card-payment simulator is structurally the same feature category even fully training-labelled and UI-only. Building the UI-only version is consistent with this repository's existing pattern of building training-only versions of otherwise-gated concepts (e.g. simulated funding), but the decision to do so is recorded here rather than assumed. See [[Telga Launcher and Dashboard]], [[Telga Pay Card Simulator]]. | Founder |

| D69 | 2026-08-27 | **Training profit is a shop-side credit, never a surcharge on the customer.** A sale of face value F moves the merchant float by −F and credits a configurable percentage of F to `TELGA_REVENUE` as a separate ledger entry. At the confirmed training rate of 4%: a 100 birr sale is float −100, revenue +4. The customer pays exactly F — the profit never changes what they hand over | The founder confirmed this against the original POS screenshots, which show **Balance and Profit as two separate running fields**: selling airtime decreases the balance by face value while profit accrues independently. The alternatives considered and rejected were (ii) float −96, margin taken as a discount at purchase, and (iii) float −104, which would have made the customer pay the profit. `TELGA_REVENUE` already exists as a ledger account type (migration 001), so no schema change is required for the profit entry itself. **The 4% rate is training configuration only** — it is not a commercial or production rate, and `packages/domain/src/commission.ts` continues to throw rather than return a real commission, because provider commission terms remain NOT YET CONFIRMED (CLAUDE.md §19). To be implemented in Stage 3; nothing in the code applies a profit today. | Founder |

| D70 | 2026-08-27 | **Telga Pay may credit the simulated training float — training mode only.** A new owner-only endpoint (`POST /api/training/pay/deposits`, permission `POS_DEPOSIT_TRAINING_FUNDS`) posts a balanced `fundMerchant` credit against `BANK_CLEARING`, so a deposit practised at the card screen becomes float a sale can then reserve against. Purchase and Cashback stay UI-only and still write nothing. | This **reverses** the earlier instruction that Telga Pay be UI-only in every path (see `apps/merchant-pos/src/ui/telgaPay.ts`, original file header). The founder confirmed the reversal because a training float that can only be topped up from the command line makes the end-to-end shop day untrainable. It is **not payment acceptance and not cash-in**: no card is read, no PAN exists, no processor or partner is contacted, nothing leaves the machine, and the driver plus the schema both refuse any mode other than TRAINING. It is therefore outside the regulated-activity gate in CLAUDE.md §2 — but it is recorded here rather than left implicit, because a future reader seeing ledger credits originating from a screen called Telga Pay is entitled to an explanation. The deposit is bounded (10–50,000 birr), audited as `TRAINING_DEPOSIT_CREDITED`, and idempotent through the posting id. | Founder |
| D71 | 2026-08-27 | **One slip for every printed thing.** Sale, top-up, reprint and Telga Pay deposit all render through a single `slipCard` function, and the `TRAINING — NO REAL VALUE` line is drawn by that function rather than passed to it, so no caller can omit it. Slip width (58/80 mm) and an optional shop advertising line are per-merchant `settings` rows, owner-only to change. | Four separately-drawn receipts had already begun to drift — the voucher result screen showed an order summary while history showed a slip, so a sale and a reprint of that same sale produced two different-looking pieces of paper. Making the training notice a property of the drawing function rather than a parameter closes the one way a receipt could ever print without it. Settings live server-side because a slip’s format is the merchant’s, not the browser’s, and must survive a device being replaced. | Claude Code |

| D72 | 2026-08-27 | **Data vouchers are approved as a training-only flow**, reversing their place on the CLAUDE.md §7 disabled list. Nine categories (Monthly, Weekly, Daily, Hourly, Weekend, All-Access, LTE, Social, Voice), each with bundles carrying a volume, a validity and a price; the flow is network → category → package + phone → PIN → slip. `pending_orders.product_type` gains `DATA` (migration 009). | The founder approved data explicitly after the Stage 1–2 retest, noting the original Telga POS screenshots show data products. Nothing here reaches a provider: bundles are clearly-simulated training values, and volumes, validities and prices are **not** researched market rates (`ASSUMPTIONS.md` A66). Two names in the founder’s list were **not** used: `FreeMe` is a Vodacom trademark, and the request to print real carrier names (`Ethio Telecom`, `MTN`) on slips would contradict D66 and CLAUDE.md §10 — Telga has no agreement with any of them. The category is called `All-Access`, the networks stay `Network A–D (simulated)`, and a test asserts none of those names appears anywhere in the flow. Data remains disabled for any live release; this decision covers training only. | Founder |
| D73 | 2026-08-27 | **A slip prints a simulated redemption code**: network, voucher PIN, token reference and dial string. Every value is derived from the transaction id by `simulatedVoucherCode()` — `TRAIN-` prefix, `*000*` dial prefix — and a boxed `SIMULATED CODE — will not load airtime` notice is drawn beside it by the slip card itself, so no caller can print a code without the warning. | A voucher slip without a redemption code is not a voucher, so the training build has to print something — and because a merchant hands this paper to a customer who has no other way to check, what it prints must be unmistakably worthless. The founder asked for the `*805*PIN#` **format**; `*805*` is Ethio Telecom’s real recharge string, so the shape is kept and the digits are a placeholder belonging to no operator. The codes are **derived, never stored**: a pure function of the transaction id means a reprint prints the identical code, a recovery-resolved sale gets one for free, and `lookupReceipt` stays a read that writes nothing — so no table, no triggers and no write on the sale path were added. | Claude Code |

| D74 | 2026-08-28 | **The device key is remembered in an `httpOnly` cookie, and sign-out becomes two different actions.** An idle timeout costs the operator their **PIN only** — device, device key and operator id are all remembered. Closing or restarting the app additionally forgets the operator (the operator cookie carries no `Max-Age`). **Sign out** in Settings clears all four and requires a full re-login. | The founder specified this sign-in model directly after retesting. It **reverses** the standing rule that the device key is never stored on the client, which `cookies.ts` had documented as a security property. The cost is real and is not hidden: possession of the browser profile becomes possession of the device key, so the key stops being a second factor beside the session and becomes part of what the device is. Mitigations taken: `httpOnly` (page script cannot read it), `SameSite=Strict`, `Secure` when the transport is, never written to `localStorage`, and sent back only to the login handler that prefills the field. The **PIN is still never stored anywhere in any form**, server-side device revocation still overrides every cookie, and Settings sign-out is the escape hatch when a device changes hands. Recorded as `ASSUMPTIONS.md` A69. | Founder |
| D75 | 2026-08-28 | **Bulk printing**: an order may be for 1–20 vouchers behind a single PIN. `pending_orders` gains `quantity` and `total_minor` becomes `amount_minor * quantity` (migration 010). Each voucher is still a **separate transaction** — its own idempotency key, reservation, ledger postings and simulated redemption code. | A shop selling ten 25-birr vouchers pressed PIN ten times. Batching the *authorization* is the ergonomic win; batching the *money* would not be, because ten vouchers is ten things a customer can hold and one row cannot carry ten different PINs. The batch is found again by re-deriving each voucher client request id from the order (`batchRequestId`), so nothing stores a list that could disagree with what was created. The 1–20 ceiling is a training bound in both the service and the CHECK, so a typo cannot become two hundred postings. | Founder |
| D76 | 2026-08-28 | **The Telga mark is drawn as inline SVG and printed on every slip**, in a mono variant for thermal paper. The palette moves from the uploaded gold to **aged verdigris bronze** on a **cut-stone** ground rather than asphalt, with light scratches on the T. | The founder supplied the artwork and asked for a professional, transparent, non-gold treatment on all slips. A photoreal render cannot print on a thermal head — one bit per dot turns gradients into a smear — so the mark is redrawn as flat shapes that survive a 58 mm roll, and inline SVG needs no external request under the page CSP. Verdigris was chosen because it is unmistakably not the gold, it already agrees with the teal accent Telga Pay uses, and patina carries the requested age through the material rather than through added grime. **This is a stylised interpretation, not a trace of the supplied render** — recorded so nobody later mistakes it for the original file. | Claude Code |

| D77 | 2026-08-28 | **A cover screen is the entry point.** `/splash` is public, shows the mark, the name and one Sign-in action, and every unauthenticated redirect now lands there instead of on the login form. Signing in proceeds to the launcher and dashboard as before. | The founder asked for a professional entry screen rather than dropping an operator straight onto a form. It is drawn with the pre-session chrome, so it renders with no session at all, and it carries **no merchant name, device id, operator name or balance** — none of that is known before sign-in, and a cover screen that leaked any of it would show shop data to whoever picked the machine up. The training banner still prints on it. | Founder |
| D78 | 2026-08-28 | **Earned profit can be moved into the selling balance.** Pressing the dashboard Profit pill opens a screen where the owner types an amount; the ledger posts a balanced pair (DEBIT `TELGA_REVENUE`, CREDIT `MERCHANT_AVAILABLE`) as an `ADJUSTMENT`. An amount above the profit earned is refused and moves nothing. | The bound is **all-time profit not yet moved** — earned minus already moved — not today profit, which is what the dashboard shows. They differ the moment a shop earns on Monday and moves on Tuesday, and bounding by today figure would offer an owner a number they cannot take. Because a move is posted as a DEBIT against the same account, the remaining net *is* the untaken balance, with no separate tally to drift out of agreement with the ledger. The check and the posting run inside one database transaction, so two simultaneous transfers cannot both pass and both post. Owner-only, CSRF-protected, rate-limited as a sale is, and audited as `PROFIT_TRANSFERRED_TO_BALANCE`. Posted as `ADJUSTMENT` because CLAUDE.md eighth ledger invariant requires a correction to be an authorized adjustment entry, never a silent edit — the original `COMMISSION_CREDIT` entries are untouched, so what each sale earned stays readable. | Founder |
| D79 | 2026-08-28 | **An owner can change their transaction PIN in Settings**, and the **current PIN is required** to do it. Six digits; obvious values (`111111`, `123456`, `987654`) and an unchanged PIN are refused. | An open owner session is deliberately not enough. A session left unlocked on a counter is exactly the case this guards: whoever walked up should not be able to rewrite the credential that authorizes sales and then keep using it. A wrong current PIN is recorded as a `PIN_AUTH` failure — the same scope a wrong voucher PIN uses — so guessing at the form is bounded by the existing trailing-window lockout and can never contribute to a *login* lockout. The PIN itself reaches no log, response, audit metadata, cookie or redirect URL; only the scrypt-derived key, its salt and its parameters are stored. The two new-PIN boxes are compared in the POS before the request is made, so a mistyped confirmation never puts a PIN on the wire. | Founder |
| D80 | 2026-08-28 | **Corporate settings**: business name, address, phone, TIN, trade licence and a slip closing line, stored per merchant (migration 011) and printed on every slip under the Telga mark. | A slip that identified the shop only as `merchant_alpha` was a debugging artefact, not a receipt a customer could take anywhere. The fields are carried on `SlipStyle`, which is already read once per render and handed to every slip caller, so a new slip type cannot forget to print the shop name. **Telga verifies none of them** — an owner types their own licence number and the slip prints what it was given; nothing here asserts that a business is registered, licensed or tax-compliant, which CLAUDE.md section 8 forbids claiming without documented review. Validated exactly as the advertisement is (length-bounded, no control characters) because they reach the same thermal printer. Recorded as `ASSUMPTIONS.md` A72. | Founder |

| D81 | 2026-08-28 | **The mark is the founder artwork, served as a file — not redrawn.** `apps/merchant-pos/assets/telga-logo.png` is served from `/assets/telga-logo.png` and shown as an `img`. The fall → catch → stand sequence is a CSS rotation of the whole image that **rests upright**; it plays on the splash and the launcher and never on a slip. | Two earlier attempts hand-drew the mark as SVG paths and both were rejected — the first stood the T upright with the figure beside it, the second tipped the letter but still did not look like the render. The instruction was explicit: keep the look, do not redraw the skeleton. So the real file is used. It is a URL rather than an inline data URI because the page CSP is `img-src self` (a data URI is refused) and because inlining 1.6 MB of base64 into every page would be worse even if allowed. **Known limitation:** rotating the composition rotates the road and the figure with it, because a flat PNG cannot have the letter separated from them without editing the image — which would mean redrawing the thing that must not change. A true three-layer animation needs the artwork supplied as separate layers. | Founder |
| D82 | 2026-08-28 | **A three-bar menu on every authenticated screen**, carrying operator id, Statements, End shift, Customer list, Learning, Settings, Help and Log out. Drawn by `page()` rather than by each screen. | Rendered in one place so an entry cannot exist on the dashboard and be missing from history. It is a `details` element, so opening and closing is native browser behaviour and the menu works with scripting off — which matters on a counter machine whose browser an operator does not control. Nothing about what the menu can *reach* is decided in the browser: every entry is a plain link or a CSRF-carrying form and the server re-checks permission on arrival. A menu entry is a signpost, never an authorisation. | Founder |
| D83 | 2026-08-28 | **Saved customers store a masked number, not a full one** (migration 012). A shop types a regular in once to recognise them; sending to that customer still means typing the number. | A customer list holding full phone numbers would be the largest pool of personal data in the product, sitting on a machine on a counter, in a build with no data-protection review behind it (CLAUDE.md sections 8 and 24). Masking it keeps the list useful for recognition — which is what it is for — without creating that pool. The limitation is stated on the screen itself rather than left for an operator to discover. Recorded as `ASSUMPTIONS.md` A75. | Claude Code |
| D84 | 2026-08-28 | **Policy texts ship as clearly-labelled placeholders.** About, Terms, Privacy and Cookie each open by saying they are training placeholders with no legal review, and must be replaced before real money or real customer data. | CLAUDE.md section 8 forbids claiming compliance without documented qualified review. A plausible-looking privacy policy that nobody wrote is worse than an obviously unfinished one: the first gets relied on, the second gets replaced. The same applies to the support contact, which is a marked placeholder rather than an invented phone number somebody might dial during a real dispute. | Claude Code |

| D85 | 2026-08-29 | **The mark is the founder render, reworked — not redrawn and not copied.** `gpt.png` supplies the letter and the figure; the build (`npm run brand:logo`) makes the background genuinely transparent, cuts the asphalt away entirely, remaps the gold to deep teal, draws a ground of warm stone and low bush, and tilts the piece 15 degrees past the reference so the letter foot is clearly off the ground. | Three earlier attempts were rejected in turn: two hand-drawn SVG versions that did not look like the render, then the render used verbatim — "just copy mine without change". The founder wanted the composition and the skeleton kept and the colour, ground and pose fixed. Notes on the technique, because each was a dead end first: the road cannot be removed by colour (its markings are the same warm yellow as the gold) nor by connected components (the letter foot rests ON the road, so they are one component and regrowing after an erosion brings it back) — the asphalt is a plane seen edge-on, so its top edge is fitted as a straight line from the columns the subject does not cover and everything below is cut. The recolour replaces hue and saturation but **keeps value**, which is what preserves the render own highlights and bevels; replacing colour outright flattens it to a silhouette. The ground is laid along a plane fitted from the columns that actually reach the floor, not from the lowest pixel per column — over the crossbar that is the overhang, and rocks laid along it trail into the sky. Everything is reproducible: `scripts/brand/build-logo.py`. | Founder |
| D86 | 2026-08-29 | **Telga is installable.** A web app manifest at `/manifest.webmanifest` plus a square icon set means a browser offers Install / Add to home screen, and the result is a square Telga icon that opens straight into the launcher with no browser chrome. The launcher then offers Telga Vending and Telga Pay. | The founder asked for a professional square app icon that behaves like an installed app. This repository is a server-rendered HTML application, not a packaged mobile build, so a manifest is the mechanism that actually produces an OS-level icon. `start_url` is the launcher rather than the last page visited, and `display: standalone` drops the address bar. The icon is not the logo scaled down: a wide landscape mark shrunk into a square leaves it unreadable at 48 px, so the icon crops to the letter and the figure, drops the ground, and sits on a brand square. A **maskable** variant with a wider margin ships alongside, because Android crops an icon to a circle and would otherwise cut the mark corners off. Assets are served from a fixed allowlist, never by joining a URL segment onto a directory. | Founder |

| D87 | 2026-08-29 | **Card payments are built as two ports, not one feature.** `CardReader` reads the card; `PaymentProcessor` authorises the money. The whole flow above them — present, read, authorise, approve or decline, slip — is written once and runs unchanged against the simulator today and against a certified reader and a live acquirer when those exist. Simulated implementations of both ship now, with test cards for approve, no money, wrong PIN, expired, blocked, over limit, bank unavailable and no answer. | The founder pushed back on an earlier answer that this could not be built, and was right to. Reading a card is a **driver**, not a capability the product lacks: a browser has no card-reader API, so on a device build the port is served by the vendor SDK (Telpo, Sunmi, PAX, Wizarpos) and on a web build by a simulator — and nothing above the interface changes between them. It is the shape `AirtimeProvider` already uses. Connecting real hardware and a real acquirer is then implementing two interfaces rather than rebuilding the product. **Three outcomes, not two:** approved, declined, and *no response* — the last gets its own screen because the money may or may not have moved, and showing it as either causes a real loss (goods handed over for nothing, or a customer refused after being charged). Declines are turned into sentences a merchant can say aloud; `INSUFFICIENT_FUNDS` means nothing across a counter. | Founder |
| D88 | 2026-08-29 | **No CVV field exists anywhere in Telga**, and `CardRead` carries only a masked PAN, the scheme and the expiry — no full number, no track data, no cryptogram. | PCI DSS forbids retaining the card verification code after authorisation, in any form — encrypted, masked or otherwise. The founder asked for a hidden CVV on a transaction detail; that was declined and no field was added, because a field that exists is a field something eventually writes to. The structure enforces it rather than a rule: there is nowhere to put one. Tests assert that no card number, track data, CVV or CVC can appear in what the flow returns or what the screens render. | Claude Code |
| D89 | 2026-08-29 | **Feature flags now exist as CLAUDE.md section 7 requires**, with a start-up assertion that refuses to run if any live-money capability is enabled. Payment acceptance, wallets, cash in and out, lending, remittance, independent settlement, real funding, electricity, general bill payment and offline vending are all off; data bundles, simulated card payments and training deposits are on. | Section 7 requires a disabled feature to be inaccessible in UI, APIs, roles **and deployment**. Until now they were simply unbuilt, which achieves the same thing by accident and leaves nothing to audit. There is deliberately no setting, environment variable or request that turns one on — enabling one is a code change plus a Decision Log entry, because a flag whose default can be flipped by configuration is one that gets flipped on the day somebody is in a hurry. `assertNoLiveMoneyEnabled()` runs before a listener opens, so a build that shipped with one on refuses to start rather than hoping the UI hides it. | Claude Code |
| D90 | 2026-08-29 | **All eight Settings toggles now change behaviour.** PIN lock locks the screen; sound plays a success cue; hide balance replaces the figure with dots behind a tap; the low-balance alert warns above the tiles; statements admin-only refuses an operator server-side; print barcode adds the reference as bars; print lookup slip adds a shop copy; lock seconds sets the idle window. | They saved and did nothing, which is the dead-control problem the founder explicitly asked to avoid — a switch that changes nothing is worse than no switch. Notes on two: the screen lock keeps its unlock state **in memory rather than a cookie**, because the lock exists to stop somebody who has picked up an unattended machine with a live session, and that is exactly the person who could edit a cookie saying "unlocked"; a restart therefore locks, which is the safe direction to fail. Statements admin-only is enforced in the route, not by hiding the menu entry, so an operator who types the URL meets the same refusal. | Founder |
| D91 | 2026-08-29 | **Outage and offline screens** (CLAUDE.md section 16). An outage blocks one product and lists what is still sellable; being unreachable stops new sales and keeps history, settings and support open. | Section 16 is specific and had no UI at all. An outage must not close a whole counter — a shop losing one network keeps the rest — and the screen says plainly that nothing was charged, which is the question the customer standing there will ask. There is no merchant override, because an override is a way to take money for something that cannot be delivered. The offline screen says selling offline is not allowed rather than queueing sales, because the pilot forbids it and a queue is what an operator would otherwise improvise. | Claude Code |

| D92 | 2026-08-29 | **Feature flags are the register written in `02 Product/Feature Flags`, name for name**, with three of the note's four enforcement layers implemented and the fourth named as missing. `tests/domain/feature-flags.test.ts` reads the note and fails if the code disagrees. | The earlier module invented its own names and its own list, so the note and the code could drift apart with nothing failing — and they had. The note is the specification; the code is the implementation of it. The deployment layer is **not** claimed: every disabled capability is currently unbuilt rather than excluded from the artifact, which achieves the same result by accident, and saying otherwise would be false. `money.live` now exists and requires two keys — ten cleared gates and two assigned approvers — so it cannot be enabled today and a test proves it. | Claude Code |
| D93 | 2026-08-29 | **`product.data` stays on, training-only.** The register note recorded it as off pending separate approval; D72 is that approval and the note now says so. | A conflict between the note and the Decision Log, put to the founder rather than resolved silently. The founder ruled that D72 is authoritative for this case and is **not** approval for production, live money, a real provider, or general availability. Live data traffic still needs its own provider authorization, exactly as airtime does. | Founder |
| D94 | 2026-08-29 | **An uncertain sale is never drawn as a completed one.** The result card takes its icon, colour and words from the transaction's `certainty`, not from a hardcoded success. | `voucherResultScreen` rendered a green tick and the success card for **every** outcome, `PENDING` included. The words underneath said "the result is not known yet"; on a busy counter the tick is what gets read. An operator reading it hands over goods on a sale that may still fail, and may retry it — the loss §15 and §30 exist to prevent. CLAUDE.md §22 requires text, icon and colour to agree, and they now do. | Claude Code |
| D95 | 2026-08-29 | **Today's profit excludes `ADJUSTMENT` entries.** A transfer of earned profit into the selling balance no longer counts against the day it was made. | Reported from the counter: moving profit made the figure negative, and moving again made it more negative. The transfer itself was never wrong — it is bounded and refuses to move more than is available, proven by seven tests that tried to break it. The **dashboard figure** was wrong: `profitForDay` summed the same account a transfer debits, so earning on Monday and collecting on Tuesday showed Tuesday as a day of losses. A transfer is a movement between the shop's own buckets, not an un-earning. `REVERSAL` is deliberately still counted — a reversed sale really is un-earned. | Founder |
| D96 | 2026-08-29 | **The launcher is the Telga mark, and it opens.** One app called Telga; tapping the logo reveals Telga Vending and Telga Pay. Built as `<details>`/`<summary>`. | The mark was a decorative picture with both app tiles sitting permanently beneath it, which reads as two apps beside a logo rather than one app that opens. `<details>` rather than a script for three reasons that all matter on a public screen: `<summary>` is already a button to a screen reader and already in the tab order; it needs no nonce under the page's CSP; and it still works if scripting fails, which on a counter is not hypothetical. | Founder |
| D97 | 2026-08-29 | **The figure is bone, the letter is teal.** The mark's two subjects no longer share one colour, and the icon set is rebuilt from it. | Both were the same teal, so the composition flattened into a single silhouette — unreadable at the 32px a browser tab shows. Separated by **thickness**, not colour: the letter is a slab that survives a heavy erosion, the figure is limbs a few pixels across that do not. Each region is remapped by luminance rather than filled flat, so the render's own modelling carries through and nothing is redrawn — the standing instruction that the artwork must not change. The icon builder's hue test had to change with it: it classified the new bone figure as ground and deleted it. | Founder |
| D98 | 2026-08-29 | **Printed slips name `TELGA TRADING PLC`**, not the brand, from one shared constant. | A receipt is the one artefact that leaves the shop in a customer's hand, and [[Receipt Specification]] requires it to carry Telga's identity — which for a document that may reach a dispute means the registered company. One constant because five code paths draw a slip, and two pieces of paper from one sale naming two entities is worse than one naming the wrong entity. **Launch note:** printing "PLC" asserts a registered legal form, so CLAUDE.md §8's "company authority documented" gate must close before a real customer holds one. | Founder |
| D99 | 2026-08-29 | **Two entities and one migration for provider attempts and provider health** — `transaction_attempts` and `provider_health_events`, migration 013. Both tables are additive; nothing writes to them yet. | CLAUDE.md §12 names both and the build had nowhere to put either. An attempt is not a transaction: §30 requires an uncertain retry to reuse the same logical transaction, so one transaction legitimately produces several provider round-trips, and the only trace was a counter that could not say *when* we asked or *what was said*. §17 gives a 24-hour target on a "paid but no airtime" complaint, which needs that record. Health events are platform-wide and deliberately carry no merchant column — §16 allows no merchant override, and a merchant column is the first step toward one. Shipping the shape empty is safer than shipping it half-wired. | Claude Code |
| D100 | 2026-08-29 | **Four more service tiles — internet, school fees, insurance, government fees — all `COMING_SOON`**, each added only after a source confirmed the service is still settled manually or through an agent in Ethiopia. | A rendered tile is itself a product claim, so §30's ban on inventing product lines applies to the dashboard. Each of the four was checked: ISP bills are paid over the counter through Telebirr and CBE Birr agents; school fees are still largely cash and paper; CBHI premium collection is described in the sector's own literature as predominantly manual; and government fees are moving online under the National Digital Payments Strategy 2026–2030, which is the evidence they are not there yet. None has a provider agreement, price or commission behind it, and none may become `IMPLEMENTED` until a real route, backend, validation and result path exist. | Founder |

| D101 | 2026-08-30 | **Login is the first screen, and the launcher is two screens behind it.** `/login` → `/launcher` (one Telga button) → `/launcher/apps` (📱 Telga Vending · 💳 Telga Pay) → the module chosen. The public cover screen is **removed**; the Telga button uses a new flat drawn mark (`telgaGlyph`), and the two modules use plain emoji, not the Telga logo. | The order has now been both ways round, so the reasoning is worth stating. It was launcher-first and public, on the argument that opening Telga should show what Telga is. The founder reversed it: sign in, then open Telga. That is also the safer order — a chooser shown before authentication tells whoever picked the machine up what the product does and what it is worth, and the module names are shop information. **Two pages, not one panel.** An earlier attempt expanded both modules underneath the button with a `<details>`; that reads as two things sitting beside a logo rather than one product that opens, which was the fault being reported. Opening Telga now navigates. **The icons are deliberate.** The Telga button gets a flat four-shape mark because it sits at button size, where the photographic render loses its detail and looks out of place beside two flat emoji — the render is untouched and still used on slips, the lock screen and the app icons. The two modules go back to 📱 and 💳: inside Telga everything is Telga, so putting the mark on both tiles distinguishes nothing, and what tells them apart is what each one does. **One follow-on defect fixed:** the session-refusal redirect still pointed at `/launcher`, which was correct while it was public and became a loop through a screen that would itself refuse. It now points at `/login`. | Founder |

| D102 | 2026-08-30 | **Telga staff get their own identity table.** `admin_users`, `admin_permissions` and `admin_sessions` (migration 014), separate from `merchant_users`. Email, password and MFA — never a PIN. An admin session carries **no device**. | A Telga admin could not exist. `merchant_users.merchant_id` is `NOT NULL`, so every user in the system — including the ones whose role says `ADMIN` — must belong to a shop; and `sessions.device_id` is `NOT NULL`, so an admin would additionally need an enrolled POS terminal to hold a session. The alternative was making `merchant_users.merchant_id` nullable. **Rejected:** that `NOT NULL` is what makes cross-shop leakage *structurally* impossible rather than merely checked, and trading it away to save one table is a bad exchange. Merchant identity is untouched by the migration and a test asserts it. A six-digit PIN typed at a counter in front of customers is the wrong secret for a console that can suspend merchants and approve funding — CLAUDE.md §24 and the owner brief both refuse it, and `admin_users` has no column one could go in. | Founder |
| D103 | 2026-08-30 | **One database per shop.** A `tenant_registry` table maps merchant to database, carries a **per-tenant** schema version, and records backup and restore times. | Chosen by the owner after the comparison CLAUDE.md §9 requires was put to them. The costs were stated and accepted: backup and restore become N operations rather than one; migrations run 12 × N and a half-migrated tenant becomes a real, detectable state; cross-shop reporting stops being a SQL query and becomes a fan-out. `ledgerResidualMinor()` is referenced at **128 call sites** — under this model it proves double entry *within a shop*, which is the invariant §13.2 actually cares about, but the platform-wide figure has to be built as a sum over tenants rather than assumed. What is bought: one corrupt file affects one shop instead of everyone. **This is the irreversible choice** — splitting later is a data migration and merging later is worse — which is why it was put to the owner before any code. The registry table exists; the routing, per-tenant migration runner and N-backup tooling do not. | Founder |
| D104 | 2026-08-30 | **Public registration creates nothing.** An applicant submits shop details and receives only a reference. No account, no credentials, no device and no database exist until an admin approves. `merchant_applications` holds the eleven-state lifecycle; `merchants.status` keeps its four. | The alternative — creating a locked account immediately — means real credentials exist for unvetted people, and every route then needs a not-yet-approved guard where a single missed one is a way in. Nothing to attack is better than a guard that must never be forgotten. The application is its own record rather than a draft merchant, because a rejected application must be able to exist without ever having created a shop. Approving is what creates the merchant. `LIVE_ELIGIBLE` is eligibility, not permission: `money.live` still needs its ten gates and two approvers, and no lifecycle state enables a regulated feature. | Founder |
| D105 | 2026-08-30 | **Face biometric is a device passkey (WebAuthn), never a stored template.** The phone or laptop verifies the face and releases a key; Telga stores a public key and no biometric data at all. Password and MFA ship first; the passkey is additive. | Storing face templates would mean holding the most sensitive category of personal data with no data-protection review done (CLAUDE.md §8), no retention policy and no documented legal basis — and it would be phishable, which a passkey is not. The owner brief requires biometric *in addition to* a password, which this satisfies without Telga ever seeing a face. | Founder |
| D106 | 2026-08-31 | **Telga ships as an Android app via a Capacitor shell, in `apps/mobile/`, and the web app is unchanged.** The shell hosts a WebView pointed at a Telga server; it reimplements no screens. `shell.config.json` is the single source for which servers are reachable, consumed by both Capacitor's `allowNavigation` and the bundled connect screen. HTTPS only. | The owner's requirement is that a merchant can install Telga from Google Play on a phone or a smart-POS. A PWA cannot be listed on Play, so a native container is the only route. **What was rejected:** reimplementing the vending flow natively — a second implementation is a second place a duplicate sale can originate, and §13 makes duplicate prevention a ledger invariant, not a preference. **What it buys today is distribution, not capability:** the app is a browser pointed at the same server, and the native-API argument (NFC reader, native printing) arrives later. **The cost accepted:** HTTPS-only means the app cannot reach a self-signed LAN server, so pilot LAN use stays on the PWA route; the remedy is a real certificate, never a trust exception. **Deviation from §27:** the tree names `apps/android/`; the project is `apps/mobile/` because Capacitor generates its native project into a child `android/` directory, and `apps/android/android/` reads badly. **Blocked, not done:** no hosted server and no registered domain exist, so the app builds and installs but cannot yet connect — recorded as A89. | Founder |
| D107 | 2026-08-31 | **The APK handed to a merchant is release-signed with Telga's own key and carries a v1 signature the tooling says is unnecessary.** `npm run mobile:package` produces it. | A debug APK was refused by the owner's phone with "App not installed". **The build was not at fault** — it is signed, architecture-independent, zip-intact, minSdk 24, and installs and launches on Android 14 (verified on an emulator: `Success`, MainActivity resumed, no fatal logs). What a debug build *does* carry is `debuggable=true` and the universally-known Android debug key, which Play Protect blocks after "Installing…". **What was rejected:** shipping debug builds to merchants at all. **The v1 signature:** AGP drops v1 above minSdk 23 and apksigner refuses it below `--min-sdk-version 21`; both are correct by specification, and it is added anyway because some OEM package managers have been fussier than stock Android, and the cost is a slightly larger file against a merchant who cannot install Telga and is told nothing. **Not yet proven:** the owner's phone still refused the release build, and the remaining causes — Play Protect, a conflicting package, a broken transfer, a device policy — cannot be told apart without the device. `npm run mobile:diagnose` prints the real code. Recorded as A92. | Founder |

| D108 | 2026-09-05 | **The cloud target is one Railway service with a persistent volume, keeping SQLite. The PostgreSQL/Supabase migration is deferred, and nothing has been provisioned.** Current environment: **local development only**. | The owner asked for Supabase + Railway + Vercel. A vault compliance scan found that plan already documented as deployment-blocking ([[Vercel Deployment Limits]], accepted; R30; A56), and inspection confirmed why. **Vercel cannot host either front end:** the POS and the console are Node HTTP servers rendering HTML server-side with database-backed sessions, not static bundles — there is no web root to publish. **The Postgres swap is larger than the vault claimed:** [[Vercel Deployment Limits]] said a second `LedgerDriver` implementation would change "nothing else", but `driver/types.ts` has *zero* Promise-returning methods and its unit of work is `transaction<T>(work: () => T): T`, a synchronous callback. Every Node Postgres client is async, so the swap is an async refactor across ~80 files and 21 transaction call sites, plus re-proving the claim lease (A37/R16) on a new engine, plus resolving D103 one-database-per-shop against a single shared instance. That is a project needing its own plan and its own approval, and it is **not justified before a provider agreement exists**. **What was chosen instead** is Category 2 of [[Deployment Target Evaluation]]: one service, one volume, one replica, SQLite unchanged, the worker and the POS sharing one file because `recovery_claims` only prevents double resolution when they do. **Explicitly not approved:** the PostgreSQL/Supabase migration, a Railway subscription, and `telga.et`, which has not been purchased (A89) and must not be configured. | Founder |
| D109 | 2026-09-05 | **`--trust-proxy` accepts a CIDR range, and Telga ships no built-in range for any hosting platform.** A zero-length prefix (`0.0.0.0/0`, `::/0`) is refused at startup, and an untrusted forwarding hop is reported once as `PROXY_PEER_OBSERVED`. | A platform terminator draws its address from an internal pool, so an exact address cannot be configured for one — and matching was exact-string only, which is why the platform question looked unanswerable. A range is still an **explicit** statement about who is trusted, so this widens what can be *expressed* without widening what is trusted by default: D45's rule that there is no trust-all setting is preserved by refusing `/0` in the parser, where it cannot be forgotten. **No platform range is hardcoded.** Railway's own documentation does not publish the address its edge forwards from; a community post reports `100.0.0.0/8`, which is unverified and wider than the `100.64.0.0/10` carrier-grade NAT block, so it would include publicly routable space. Guessing it would be exactly the "wrong answer arriving looking like the right one" that the whole trusted-proxy design exists to prevent. So Telga **observes and reports** the hop instead, once per address, and the operator verifies and configures it. Observing never trusts. A malformed entry is refused at startup rather than silently matching nothing, because a range that matches nothing looks identical to a working deployment until the first sign-in fails. | Claude Code |
| D110 | 2026-09-06 | **The Railway deployment is a training deployment, and is recorded as one everywhere it could be misread.** No custom domain, no merchant access, a Railway-generated address only, `TELGA_MODE=TRAINING` refused any other value, and `TELGA_RECIPIENT_SALT` set in Railway so the public fallback constant is never the salt in use. | Asked directly whether Telga Pay and the voucher flow are regulated features being switched on by the back door. They are not, and the check was worth making rather than answering from memory. **Telga Pay** is gated by `card.simulated`, a flag deliberately kept distinct from `payments.acceptance` — the simulator implements the same `CardReader` and `PaymentProcessor` ports a certified terminal will, decides from the card number, marks every result `simulated: true`, and a search of `services/provider-adapters/` finds no `fetch`, no HTTP client and no socket: nothing there can contact anything. The Visa and Mastercard wordmarks on screen are labels carrying the string *"shown as training examples only. No real card network is connected."* **Vouchers** are gated by `product.data` (D72, training-scoped) and cannot carry value: every PIN is prefixed `TRAIN-`, every dial string `*000*`, and the network is named `NETWORK_A` rather than a carrier. **Every regulated capability is off** — `money.live`, `wallet`, `payments.acceptance`, `cash.in_out`, `lending`, `remittance`, `settlement.independent`, `funding.submission`, `product.electricity`, `bills.general`, `vending.offline` — enforced in three layers: `isEnabled` for the UI, `routeBlockedBy` answering `404` before authentication or any body read, and `findRoleCapabilityLeaks` for roles. The fourth layer, deployment exclusion, is **still not implemented**, and the module says so rather than claiming it. **The honest caveat:** a real URL with real uptime invites the description "live". It is not. Recorded as A98. | Founder |
| D111 | 2026-09-06 | **`TELGA_RECIPIENT_SALT` is a required secret for the Railway training deployment, and the committed fallback must never be the salt in use.** | The worker falls back to the literal `cli-salt-not-a-production-secret` when the variable is unset, and that literal is public in this repository — so a digest made with it is recomputable by anyone who can read the repo, which is the rainbow-table exposure [[Security Model]] introduced the salt to close. Setting the variable costs nothing and removes a published constant from the security boundary. **What this decision does not fix, and does not pretend to:** the POS ignores the variable entirely and salts per process with `randomUUID()`, deliberately, on the grounds that the training build stores no real recipient. So the two processes salt differently and the POS's salt changes on every redeploy. That is tolerable while every recipient is simulated and nothing compares digests across processes; it is not tolerable the day a real recipient is stored, and it is recorded as A97 rather than quietly carried. | Founder |
| D112 | 2026-09-06 | **Current Telga training scope is airtime vending simulation and platform operations only. Telga Pay / card simulation and data/voucher functions are disabled. No payment gateway, Chapa, wallet, card payment, cash-in/out, funding, settlement, or real-money function is approved.** `card.simulated` and `product.data` are set to `false`, reversing D87's surface and D72. `deposits.training` is retained after audit. | The founder narrowed the scope after deciding against Chapa and against payment integration generally. **What is reversed, and what is not.** D87's two-port design (`CardReader` + `PaymentProcessor`) stands and the ports remain in the tree — it was always the right shape for dropping in a certified reader later. What is switched off is the *surface*. D72's reasoning likewise stands — the data flow never reached a provider — but it no longer describes what is on. Neither flow is deleted: removing the screens and migration 009 would be a larger and riskier change than switching them off, and with the flags false no route serves them and no role can reach them. **The finding that made this more than a flag edit.** Turning `card.simulated` off exposed that it had never gated what it appeared to: the route table listed `/pay/card` alone, so `/pay`, `/pay/purchase`, `/pay/cashback`, `/pay/deposit`, `/pay/deposit/slip`, `/pay/settings`, `/pay/statements`, `/pay/transactions` and `/pay/result` — **nine routes** — would have kept answering normally with the buttons gone from the screen. That is precisely the "disabled, not merely hidden" failure CLAUDE.md §7 forbids, and it was found by switching the flag off rather than by reading the table. `deposits.training` had the same class of fault and was worse: it gated `/deposit`, a path **no route in the system uses**, so it gated nothing at all. The table now names `/pay` and `/api/training/pay/deposits`. **Why `deposits.training` stays on.** Audited against the founder's nine conditions: `application/deposits.ts` refuses any mode but `TRAINING` before it reads an amount, bounds the credit to 10–50,000 birr, posts a balanced append-only entry against `BANK_CLEARING`, audits `TRAINING_DEPOSIT_CREDITED`, and imports no HTTP client and no provider — a grep for a network call in that module returns a single hit which is a *type* import. It cannot be turned into a live-money feature by configuration: `money.live` needs ten gates and two unassigned approvers, and no flag is environment-readable. **The one consequence to state plainly:** the deposit's only screen was `/pay/deposit`, so it is unreachable while Telga Pay is off. The endpoint remains, gated by its own flag. If in-app top-up is wanted during training, the screen must be moved out of `/pay` — a separate decision, not done here. | Founder |
| D113 | 2026-09-06 | **The first Railway deployment is a single-shop training environment, and five things are deferred rather than done.** One merchant, one operator, one enrolled device, one SQLite database, one Railway service, one volume, one replica, TRAINING MODE - NO REAL VALUE, mock airtime only. **(a) D103 is deferred, not withdrawn.** Per-shop database files are not implemented now; the shared SQLite file with server-side session-derived merchant context and merchant-scoped queries is the accepted isolation boundary **for training only**. **(b) Automatic registration is deferred.** The existing CLI `provision()` creates the one training merchant, operator and device; no self-service registration is exposed in the Android app, the POS, the console or the API. **(c) The training float is created by CLI only** - the in-app deposit screen stays unreachable (A100). **(d) Phase 2 is IN PROGRESS, not complete.** **(e) No printer support exists** and none may be claimed (A101). `telga.pro` is purchased and deliberately not configured (A102). | Taken after the B2/B5/B6 audit, which found three things worth deciding on rather than drifting past. **On D103:** `tenantRouting.ts` is written and checks the registry, the derived name, the tenant status and the schema version on every open - and **has no callers**, so every merchant lives in one file today. That is not the model D103 chose, and the honest options were to wire it (a real project: routing, per-tenant migrations, N-backup tooling, and re-proving the claim lease per file) or to say plainly that it is deferred. The founder chose deferral, so D103 stays **open and deferred**, not complete, and the vault must not describe per-tenant routing as active. **Why deferral is defensible here:** the training deployment is one shop, so a per-shop file separates nothing that scoped queries do not already separate - and that boundary is genuinely tested (11 cases in `merchant-isolation.test.ts`, plus URL, body and encoded-id tampering in `authorization.test.ts`, against a live server). It stops being defensible the moment a second real shop exists, which is why multi-shop onboarding is recorded as its own future project requiring a founder-approved decision on shared-versus-per-tenant storage **before** implementation. **On registration:** `merchant_applications` is written by nothing and `provisionMerchant`/`merchantIdFor` have no callers, so the schema for onboarding exists while the workflow does not. Wiring it would mean generating immutable server-side ids, idempotency, approval, enrolment, roles, audit and isolation tests - a project, not a patch. **What must not be claimed:** automatic merchant registration, automatic tenant creation, per-shop database files, complete admin operational workflows, receipt printing, real-money activity, or production readiness. | Founder |

| D114 | 2026-09-07 | **SUPERSEDED by D115.** Set `railway.json`'s `buildCommand` to `npm ci --ignore-scripts && npm run build:clean` under the Nixpacks builder. | **The diagnosis was right and the fix was aimed at the wrong phase.** `better-sqlite3` has a `binding.gyp` and no `install` script, so npm runs an implicit `node-gyp rebuild`, and the Nixpacks image has no Python. Node 24 was never the cause: the package ships N-API prebuilds including `prebuilds/linux-x64.node`, and `lib/binding.js` prefers them over `build/Release`. **What this decision got wrong:** `buildCommand` replaces only Nixpacks' **build** phase; Nixpacks generates a separate **install** phase that runs plain `npm ci` first, so the failing command was never the one changed. Superseded by **D115**, which overrides `[phases.install]` in `nixpacks.toml`. `.nvmrc` is retained for local tooling. | Founder |
| D115 | 2026-09-07 | **Railway's Nixpacks install phase is overridden by `nixpacks.toml`**, and the build verifies the SQLite binding before it is allowed to succeed. `[phases.install] cmds = ['npm ci --ignore-scripts']`; `[phases.build]` runs `build:clean` then `scripts/deploy/verify-binding.mjs`. `railway.json` keeps `builder: NIXPACKS` and **drops `buildCommand`**. | Two builds failed to behaviour Nixpacks generated on its own, visible only in the build log. **(1)** Nixpacks emits its own `install` phase running `npm ci`, before `buildCommand` — so D114 never touched the failing command. **(2)** It selected `nodejs_24` while `NIXPACKS_NODE_VERSION=22` appeared to be set; **that second symptom was later shown to have the same cause as the crash and not to be a Nixpacks fault at all — see D116.** `[phases.install]` is the documented override and targets the phase that actually fails. A `Dockerfile` was written first and **withdrawn at the founder's direction**. **The addition that should have existed from the start:** `verify-binding.mjs` opens a database, creates a STRICT table, runs a transaction and asserts `integrity_check` **on the build host**, so an unloadable `linux-x64` binding fails the build instead of surfacing at the first sale. **Rejected:** adding `python3` to compile a binary that already ships; and `RAILPACK_INSTALL_CMD`, which belongs to Railpack and would be ignored by a NIXPACKS build. **Cost accepted:** `--ignore-scripts` skips `esbuild`'s postinstall, so the deployment image cannot run the test suite (`A104`). | Founder |
| D116 | 2026-09-07 | **The Railway training deployment is running.** Build green, `linux-x64` SQLite prebuild verified on the build host, fourteen migrations applied to `/data/telga.sqlite` on the mounted volume, worker sweeping, POS listening, `TRAINING MODE — NO REAL VALUE` printed. | **One root cause produced two symptoms that were diagnosed as separate problems for three builds: the Railway variables were never applied.** The dashboard showed them; the containers never received them. **(1)** `TELGA_DB_PATH is not set` — because no `TELGA_*` variable reached the container at all. **(2)** `setup | nodejs_24` despite `NIXPACKS_NODE_VERSION=22` — because Nixpacks never saw that variable either, and fell back to `engines: ">=20"`. D115 recorded the second as "set and ignored"; **that was wrong**, and the first green build proved it by reporting `setup | nodejs_22` the moment the variables were applied. **What found it was not a better theory but a better failure message.** The refusal now prints the **names** of every `TELGA_*` variable present and a count of `RAILWAY_*` ones; the next deployment printed `TELGA_* (0): (none)` beside `RAILWAY_* present: 25`, which eliminated a name typo and a broken platform in one line. The founder then found the unapplied change. **Three build failures preceded that, each a guess acted on:** the log named Node 24 so Node was blamed; `railway.json` had a `buildCommand` so it was assumed to govern installation; and `.nvmrc` was proposed as a pin that `engines` outranks. **The lesson:** a deployment log is evidence, not a diagnosis, and a fix asserted without execution is a hypothesis. `R36` carries the class forward. **Now proven:** the build, the Linux binding (`A103`), the volume mount, single-writer migration, and the running services (`A94`). **Not proven:** sign-in over the proxy (`A93`), volume persistence across a redeploy, and a backup taken off Railway (`R31`, gate 10). **Ten launch gates remain open.** | Founder |
| D119 | 2026-09-08 | **A shop's bank reference is a purpose-built Deposit Reference Code, never the device key; and no funding decision can credit value without a bank record.** Two new pure-domain modules, `depositReference.ts` and `fundingSubmission.ts`, with 27 tests. Nothing is wired: no route, no migration, no persistence, no change to the running deployment. | The founder proposed that a shop deposit at CBE quoting **the device key** as the reference, with Telga crediting the float on receipt of a WhatsApp photograph of the slip. Both halves needed correcting, and both corrections are now enforced by code rather than by convention. **(a) The reference.** `credentialsOf()` in `http/authHandlers.ts` requires four fields to sign in — `userId`, `pin`, `deviceId`, **`deviceSecret`** — so the device key is a password. Writing it on a deposit slip would disclose it to a teller, print it on two paper copies, key it into CBE's core system and place it on every subsequent statement, and A52 records that a copied key is indistinguishable from the original. The replacement identifies a shop and authorises nothing: eight data characters plus a check character from the enrollment token's 32-symbol alphabet (`I`, `O`, `0`, `1` excluded because it is read aloud and copied by hand), generated rather than derived from the merchant id **so that it can be rotated** — a code mis-printed on a shop's paperwork must be replaceable without the shop's identity changing. A generalised Luhn check character catches every single-character substitution, proved exhaustively over all 279 substitutions of a generated code; sparseness (40 bits) does the rest, and an unresolvable reference goes to a person rather than being rounded to the nearest shop. **(b) The photograph.** CLAUDE.md forbids this by name at **L128** — *"Never credit a balance from a screenshot alone"* — and **L402** — *"No screenshot-only credit."* The interim flow is nonetheless sound once the slip is treated as the **claim** and the bank record as the **proof**, which is precisely the state machine [[Funding Verification]] already specifies. `verifyDeposit()` therefore takes the bank record as an input and reads every credited amount from it; **there is no branch in which a claim alone produces value, and no argument that could be passed to make one.** A test varies the claim every way a shop or an attacker could — inflated amounts, a cap raised to `MAX_SAFE_INTEGER`, a cap of zero, plausible references — and asserts that with no bank record none yields a credit. The photograph is not an input to the function at all. **Other properties fixed in code:** the credited amount is the bank's figure whichever way it differs from the claim, so a mistyped claim never blocks a real deposit and never inflates one; the duplicate check runs **before** the bank lookup, so a replayed slip cannot race a first-time one; `CREDITED`, `REJECTED` and `DUPLICATE` are terminal with no inbound transition, so a credited deposit cannot be re-opened and credited twice — a correction is an authorised adjustment entry per §13.8; and deposits above a configurable cap return `NEEDS_APPROVAL`, which is where §20's second approval for high-value deposits lives. **Scope deliberately withheld:** no route, no `funding_submissions` migration, no console screen, no WhatsApp intake, no rail adapter. The founder's standing instruction is that the built system is not to be touched, so the two modules are additive files plus two export lines, and nothing imports them yet. **Recorded honestly:** the founder delegated the choice with *"continue choose best option"*; the decision to correct the reference design was mine and is the one place I declined to build what was asked. **Verified:** 27 new tests, 209/209 across `tests/domain`, typecheck clean, eslint clean. **Unchanged:** gate 6 is not cleared, `funding.submission` remains false, and §20's requirement that the verifier and the reconciler be different people is untouched by any of this. | Founder (delegated) |
| D120 | 2026-09-08 | **Approving an application now provisions the shop, in the same request. Storage is one shared database with logical isolation, implemented as a single injected port so the D103 reversal stays one function rather than a rewrite.** `admin/provisioning` is exported, `apps/operations-console/src/provisioningPorts.ts` implements `ProvisioningPorts`, and `/applications/:id/decide` calls `provisionMerchant()` on approval. Nine HTTP-level tests. | This closes gap 2 of the four in [[Multi-Shop Onboarding]] — the one identified as highest value. **The defect it fixes was invisible to unit tests because every unit worked:** `provisionMerchant()` was written, tested as a pure function, and had **no caller**; it was not even exported from `@telga/api`. So an `APPROVED` application and a shop that could trade were unrelated facts, and the only way to turn one into the other was a CLI run by hand that nothing recorded. **Why the console is a safe place to build:** `scripts/deploy/railway-start.mjs` starts the worker and the POS and **not** the console, so console changes cannot affect the running training deployment. **The storage decision, and how it was contained.** D103 chose one database per shop and called it irreversible; D113 deferred it; the founder's multi-tenant brief then asked for data *"logically isolated, even if in the same database"*, which is weaker, and D118 recommended taking the weaker requirement because the same brief wants cross-shop aggregate reporting — one query here, a fan-out under D103. `ProvisioningPorts` made it possible to act without pre-empting the decision: **`createTenantDatabase` is the entire difference between the two models.** Under the shared model it reports the version the open database is already on and creates nothing; reinstating D103 means implementing that one port against `tenantRouting.ts`, which is already written, with every other line unchanged. **The registry is populated regardless** — the tenant screen reads it, `provisionMerchant` uses its primary key as the lock that stops two operators approving one application into two shops, and a registry kept current from the start can be walked to split the file later, where one reconstructed afterwards is archaeology. **What the tests found that reading the code had not:** deciding an application is on the console's **step-up** list, so an admin who has cleared password and MFA is still sent to `/step-up` and provisions nothing. The first run failed on exactly that, the control is correct, and it now has its own test — a later change that dropped the requirement would otherwise make the suite pass and the console weaker. **Other properties asserted:** the merchant id is derived from the application, so a double approval collides on the primary key and returns `ALREADY_PROVISIONED` rather than creating a second shop; a rejected or returned application leaves **no** merchant and no registry row, which is Decision 3's "no account until approved"; provisioning runs *after* the decision is recorded, so a failed provisioning still leaves an auditable approval; `mode` is `TRAINING` by schema `CHECK`, so a live shop cannot be provisioned down this path even by mistake; and the application lands on `DEVICE_PENDING`, not `ACTIVE_TRAINING`, because no device is enrolled yet. A new audit event `ADMIN_MERCHANT_PROVISION_REFUSED` exists so an approved application with no shop behind it is findable. **Still missing:** no shop can submit an application (gap 1), no route redeems an activation code (gap 3), `tenantRouting.ts` still has no callers (gap 4), and there is no forced PIN change on first login. **Verified:** 9 new tests, 416/416 across `tests/auth`, `tests/domain` and `tests/admin`, typecheck clean, eslint clean. **Not done:** no migration, no commit, no deployment. | Founder (delegated) |
| D121 | 2026-09-08 | **Migration 015 adds registration documents and a temporary-PIN flag; application intake, activation-code redemption and forced PIN change are implemented.** Gaps 1, 2 and 4 of [[Multi-Shop Onboarding]] closed; gap 3 resolved as deferred-by-design under D120. | **Migration 015.** `merchant_application_documents` — a row per document rather than columns, because each carries its own expiry (an Ethiopian trade licence renews annually), its own verdict and its own reference, and a new document type should not be a migration. **No column holds an image**: CLAUDE.md §24 requires data minimisation, so what is stored is the licence or TIN *number* plus who checked it; `document_uri` exists for a later, deliberate retention decision and nothing writes it. A unique index on `(kind, reference)` refuses one licence number claimed by two shops — the signal that one person is opening shops under several names. Also `merchant_users.must_change_pin`, `DEFAULT 0` so **every operator that exists today is unaffected**; that is the migration's only risk, since the training deployment has an operator signed in against that table, and it is the property most heavily tested. **Gap 1 — intake.** `admin/applicationIntake.ts` encodes the document register from [[Multi-Tenant Architecture Proposal]] §5: six documents for a sole trader, eight for a company. An expired trade licence is refused outright rather than treated as paperwork to fix after approval — the shop is not currently licensed to trade. Phone validation is deliberately loose (digits, optional `+`, 9–15 long) because a validator encoding today's Ethiopian prefixes would reject a real shop the day a new one is issued, and **rejecting a real merchant is worse than accepting a typo a callback catches within a day**. **There is no public registration route** — D113(b)'s deferral of self-service stands, and a Telga operator records what a shop brought in, which is how Ethiopian merchant onboarding actually works. **Gap 2 — activation.** `admin/redeemEnrollment.ts` exchanges the one-time code for a 256-bit device key by reusing `enrolDevice`, so activation and re-enrolment cannot drift apart. Single use is **structural, not a flag**: redemption replaces the code's hash with the key's, so a replayed code has nothing left to verify against. An unknown device and a wrong code get the same answer, because distinguishing them would make this a device-id oracle for learning which shops exist and which are mid-activation; expiry *is* told apart, since a shop with a lapsed code needs to know to ask for another, and has already proved it holds one genuinely issued. **A latent bug was found by wiring this:** the console hashed the code in its *displayed* form, hyphens included, so the grouping was part of the secret and a shop typing the code without hyphens would have been refused with no way to tell why. Nothing had ever redeemed a code, so it had never had a chance to show. Both sides now normalize. **Gap 4 — forced PIN change.** `application/changePin.ts` already existed; what was missing was the *forcing*. `pinChangeRequired()` answers the question, and `updateMerchantUserPin` clears the flag **in the same statement that writes the new hash**, so no window exists in which a changed PIN is still marked temporary. Nothing else clears it: signing in with a temporary PIN does not make it permanent. **Gap 3 — not fixed, and correctly so.** `tenantRouting.ts` having no callers is what D120's shared-database model *means*; wiring it would silently reinstate D103. It stays unused until that reversal is decided, and the port design keeps the swap to one function. **Verified:** 15 migration tests, 21 intake and activation tests, plus the 9 from D120 — typecheck and eslint clean. **Not done, and needing explicit approval because it touches the deployed POS:** no route serves intake, redemption or the forced PIN change. The logic is built and tested; the surfaces are not wired. No commit, no deployment. | Founder (delegated) |
| D122 | 2026-09-08 | **`npm test` classifies its own failures.** It is now `node scripts/run-suite.mjs`: exit `0` passed, `1` a test failed, `3` the harness failed. `npm run test:raw` keeps the unwrapped `vitest run`. | A51 — vitest's reporter RPC timing out under CPU starvation — makes a full run exit `1` **while every assertion passes**, and the exit code cannot say which happened. [[Test Stability Runbook]] already solved this for the *stress* passes and recorded that **A51 stays open by design**: the reporter limit is a property of the machine, so the fix was never to make it impossible, only to make it unmistakable. `npm test` had been left as a bare `vitest run`, and on 2026-09-08 it produced exactly the ambiguous case — `1 failed | 1497 passed` **together with** `Timeout calling "onTaskUpdate"` — which had to be separated by hand. This applies the existing rule where it was missing. **An assertion wins over an infrastructure signature**, because a run that both failed a test and starved its reporter is a test failure; an output matching neither pattern is reported as a test failure, since an unexplained non-zero exit must not be waved through as "just the machine". **Two faults found while building it, both fixed:** importing the module to test `classify` **spawned a nested vitest run**, now guarded by an `invokedDirectly` check; and `shell: true` on Windows concatenates rather than escapes arguments (DEP0190), replaced by spawning vitest's entry point under `process.execPath`. **Two side-effects worth having:** piping `vitest run` to a file produced *zero bytes* until it finished, so a long run was indistinguishable from a hung one — output is now streamed; and the dot reporter is used when stdout is not a terminal, which is the unattended loaded case A51 is about. **Nothing changes what any test asserts, and CI still fails on both non-zero codes.** **Verified:** 6 classifier tests over the real 2026-09-08 output, and a full suite of **1498/1498 across 94 files, exit 0** — which also confirms the `migration-008` table-guard fix and that migration 015 caused no regression. | Founder (delegated) |
| D123 | 2026-09-08 | **Every shop's balance is its own, and the admin panel now shows it that way.** `/merchants` carries each shop's available float and a count of its sales; 12 new tests pin the isolation at the ledger and on the screen. | **The trigger was a misunderstanding of my own wording, and it was worth taking seriously.** A summary said *"all devices for a shop share the same balance"*, meaning that one shop's tills draw on that shop's float. The founder read it as *all shops sharing one balance* and objected — different shops have different owners who deposited different amounts. **The code was already correct; the sentence was not.** `balanceFor()` sums `WHERE merchant_id = ?` over `ledger_entries`, and every entry carries a `merchant_id`, so shop B's rows cannot enter shop A's total. **But a claim about money is worth more as a test than as a paragraph**, so it is now one: `tests/admin/shop-balance-isolation.test.ts` proves two shops funded differently hold exactly their own deposits, that funding one moves nothing in the other, that a shop which deposited nothing has zero however much its neighbour holds, that reserved and under-review are separated too, that no `MERCHANT_*` entry is unattributed, and that the platform figure is a **sum of shops that no shop can see**. Written as deltas rather than absolutes, because the fixture seeds one shop with a small float and a test pinning an absolute would break on a fixture change while proving nothing extra. **The screen was the other half.** A correct ledger displayed through a query that forgot its `WHERE` would still hand one shop another's money, so `/merchants` gained two columns and `tests/admin/console-shop-balances.test.ts` asserts the rendered page shows `1,000.00 ETB` beside `500.00 ETB` and **never** their sum. Every added column is a correlated subquery keyed on `m.id`; `BANK_CLEARING` is excluded exactly as it is in `balanceFor`, so a deposit's counterparty side can never appear as a shop's money. **This closes requirement 5** of the founder's brief — shop-level performance — while keeping requirement 6: *"sales"* is a **count**, and the test asserts that no transaction id, masked recipient or product type reaches the console. The screen states the rule in words too, because a number with no owner named is the ambiguity that caused all this. **Verified:** 42/42 across `tests/admin`, 198/198 across `tests/auth` with no console regression, typecheck and eslint clean. **Not done:** the intake and suspend routes are still unbuilt, and nothing is committed or deployed. | Founder (delegated) |
| D124 | 2026-09-08 | **Telga Pay is switched back on.** `card.simulated` is `true`, reversing D112 and restoring D87's surface. `payments.acceptance` stays `false`. | The founder asked for Telga Pay back *"just like it was on localhost"* — the **simulator**, not payment acceptance. **What that restores:** the twelve `/pay` routes, and with them `/pay/deposit`, which is the only in-app way to credit a training float and was unreachable while the flag was off (A100, D113c). CLI-only funding was the direct cause of that gap. **What it does not touch, and the test that proves it:** `payments.acceptance` remains off. D87 built card payment as two ports precisely so restoring a training screen could never be the same edit as enabling a licence requirement, and `feature-flags.test.ts` now asserts that pairing as the **live** case rather than a hypothetical — simulator on, acceptance off, and `card.simulated` still absent from `MOVES_REAL_MONEY`. Nothing behind the flag reaches a card network, processor or bank: `services/provider-adapters/` has no `fetch`, no HTTP client and no socket; `ui/telgaPay.ts` writes nothing; the wordmarks carry *"shown as training examples only."* **What the change actually cost, which is the interesting part.** Flipping the flag was one line; four test files encoded the *off* state as fact and had to be re-aimed. **`tests/domain/feature-flags.test.ts` failed first, and correctly** — it reads `02 Product/Feature Flags` and asserts the code register matches the note, so the vault had to be updated before the code would pass. That guard is doing exactly the job it was written for. `tests/ui/card-screens.test.ts` was rewritten from *"proves the tree is refused"* to *"proves the tree is served"*, keeping the three properties that outlive the setting: **the kill switch still covers all twelve paths** (the hole D112 exposed), the licence flag did not move, and **enabled is not public** — an unauthenticated caller is still sent to sign in. `launcher-entry-flow.test.ts` now asserts the Telga Pay tile is drawn **and** that `/pay` answers `200`: a tile leading to a 404 and a working screen with no way in are both failures of CLAUDE.md §7, and asserting only one is how each survives a suite. **A flow refusal is not a feature refusal:** `/pay/card` answers 404 without an `amount` because it is a step, not an entry point, so the assertions check for `FEATURE_DISABLED` rather than a bare status. **Verified:** 600/600 across `tests/ui` and `tests/domain`, typecheck, eslint and docs:validate clean. **Unchanged:** all eleven regulated flags off, 0 of 10 launch gates, TRAINING MODE — NO REAL VALUE. | Founder |
| D125 | 2026-09-08 | **Registration documents are stored as images, for fraud identification. The Device Key is the bank deposit reference.** Both settled by the founder after the risks were put to them, and both are recorded here with what they oblige. | **(a) Document images.** The founder: *"it supposed to be stored, it's not sensitive data, we need it if shop owner do fraud so we can catch with its identification."* **The purpose is legitimate and standard** — identifying an accountable natural person is why every regulated financial platform holds identity documents, and a KYB file with no ID is not a KYB file. Migration 015 already carries `document_uri`, reserved for exactly this decision; the decision is now taken. **One factual correction is recorded rather than argued:** a photographed national ID or passport *is* sensitive personal data under any data-protection standard, and CLAUDE.md §8 lists data protection among the matters needing local advice. That does not make holding it wrong — it makes holding it **carelessly** wrong. So the decision is accepted and the obligations are written down with it: encrypted at rest; stored on the volume and never in the repository, a log, or a backup that leaves Telga unencrypted; access behind step-up re-authentication and itself audited; a retention and deletion policy before a second shop is onboarded; and never returned by any API that a merchant session can reach. Recorded as **R37**. **(b) Device Key as deposit reference.** Raised three times and reaffirmed three times; the founder's decision stands and is implemented as specified. **The precise cost, stated without alarm:** `credentialsOf()` requires four fields — `userId`, `pin`, `deviceId`, `deviceSecret`. A deposit slip discloses the last two, so it removes the **device** factor and leaves the **operator** factor (id plus PIN) doing the work alone. What is lost is exactly the guarantee `Device Binding` claims — *"an unenrolled machine cannot sign in, even with a correct PIN"* — and A52 already records that a copied key is indistinguishable from the original. It is **not** a full account compromise: a slip alone signs nobody in. **Mitigations that do not change the founder's flow** and are therefore adopted: the deposit matcher treats the reference purely as a lookup key and never as authentication; a device whose slip is known to have been mishandled is re-enrolled, which issues a new key and revokes live sessions; and sign-ins are watched for a device appearing from an unfamiliar context. Recorded as **R38**. **The alternative remains built and unused:** `depositReference.ts` (D119) does the identical job without disclosing a credential, and switching to it later is a configuration change, not a redesign. **No code was written for either decision in this entry** — it records what was decided, so the implementation in M1 and M3 has something to conform to. | Founder |
| D126 | 2026-09-08 | **M1a is built: "Register Telga User" records a shop's registration from the console.** `GET /applications/new` renders the founder's form, `POST /applications` validates and writes the application and its documents in **one transaction**. 16 HTTP-level tests. Console only — the deployed POS and worker are untouched. | This closes gap 1 of [[Multi-Shop Onboarding]]: nothing in the source could insert into `merchant_applications`, so the review queue was real and reviewed nothing. **The required document set was narrowed from six to the founder's three** — business licence, TIN, owner photo ID. Commercial registration, proof of address and bank-account proof moved to `OPTIONAL_DOCUMENTS`: the specified form does not ask for them, and a hidden requirement would refuse registrations the founder expects to succeed. They stay in migration 015's `CHECK`, because an unused enum value costs nothing and widening a `CHECK` later costs a migration. The company-conditional requirement was dropped for the same reason — one form, one set. **One field was added to the founder's seven:** city or town. `locality` is `NOT NULL` and is what the applications list is grouped by, so splitting it out of the address costs one box and makes "every shop in Adama" a query rather than a scan of free text. **The bug the tests found, which is the entry's real content.** The first version wrote the application row and then its documents, so a document colliding on the unique `(kind, reference)` index left the application behind — **a shop in the review queue with no papers, created by the very check meant to refuse it.** A duplicate TIN is precisely the case that index exists for (one person opening shops under several names) and therefore the case least able to afford a half-written record. Both writes are now one transaction, and a test asserts nothing is left behind. **What the route deliberately does not do:** create a merchant, an operator, a device, an enrolment or a registry row — asserted by counting all five as zero. That is `Admin Operations Console` Decision 3, *"nothing to attack beats a guard that must never be forgotten"*, and it is what makes this route safe to expose to an admin standing in a shop with an applicant watching. **Also held:** the audit entry records the reference and the locality and **never a document number**, since a trail carrying identity-document numbers turns every reader of the audit screen into a holder of them; a refusal hands the form back filled in, so an admin fixes one field rather than retyping seven; an expired licence is refused outright; and an auditor is refused. **The chain now runs end to end** — register, pick up, approve, and only then does a merchant, a registry row and the link back exist — proved in one test. **Verified:** 59/59 across `tests/admin`, 407/407 across `tests/auth` and `tests/domain`, typecheck and eslint clean. **Not done:** M1b, the document images (D125, R37). Nothing committed or deployed. | Founder (delegated) |
| D127 | 2026-09-08 | **M1b is built: scanned licences and identity documents are uploaded, encrypted at rest, and opened only under step-up with an audit entry.** `admin/documentVault.ts` (AES-256-GCM), `admin/multipart.ts` (written, not installed), the upload path on `POST /applications`, and `GET /applications/:id/documents/:docId`. 42 new tests. Console only. | This implements what D125 decided and R37 obliged. **Encryption.** AES-256-GCM, a fresh nonce per file, the media type bound as additional authenticated data. Four properties are asserted rather than assumed: the plaintext never appears on disk; the same file stored twice produces different ciphertext (a reused nonce is the one mistake GCM does not survive, and identical ciphertext would also disclose that two shops filed the same document); a file altered on disk does not open; and **a document re-filed in the database as another type does not open**, because the type is bound into the tag. **The rule that matters most:** with no key configured the vault **refuses to exist**, and the console records registrations by reference number only. There is no path that writes an unencrypted passport because a key was missing — which is exactly how a store of identity documents ends up in the clear on a volume nobody remembers provisioning. The key is read once at startup, so a misconfigured console fails on the operator's screen rather than at the first upload with an admin standing in a shop holding somebody's passport. **A parser was written rather than installed.** `better-sqlite3` is the project's only runtime dependency and this one parses attacker-controlled bytes on an administrative console. It is bounded everywhere — body cap before parsing, part cap, size cap — and covers exactly what a browser posts for a form with no JavaScript, which is what the console serves (`script-src 'none'`). The cases tested are the ones a hand-written multipart parser gets wrong: binary survival across all 256 byte values, the trailing CRLF that belongs to the delimiter, and an unchosen file input, which is absence rather than error. **One test I wrote was wrong and is recorded as such:** it asserted that content containing the boundary parses correctly. Multipart has no escaping — the format's whole answer is that a sender picks a boundary the content does not contain, which browsers do because it is random. The property worth holding is that a crafted body is **refused rather than silently mis-parsed**, and that is what is now asserted. It is not a vulnerability: a caller crafting their own request could send any fields directly. **Authorisation is `ADMIN_EXPORT_DATA`, not `ADMIN_REVIEW_APPLICATION`.** Reading the queue and taking a copy of somebody's passport are different acts, and the second **is** a data export. Saying so keeps the reviewing role narrow and brings **step-up re-authentication** with it for free, since `STEP_UP_REQUIRED` already lists it — the password must have been typed *now*, not at the start of a shift. An earlier draft invented a second step-up check, which would have duplicated the window logic `requireAdmin` already owns. **The audit entry is written before the bytes are sent**, so a read that happened is recorded even if the response never completes, and it carries the document *kind* and never the reference number. **Response headers:** `no-store`, `attachment`, `nosniff`, and a sandbox CSP — a passport left in a browser cache on a shared laptop outlives the session that opened it. **Migration 015 was edited rather than patched**, having been verified uncommitted and applied to no durable database (the local one is at 014): `media_type` and `byte_size` belong with the table they describe, and a 016 patching a table introduced in the same uncommitted change would be noise. **Verified:** 248/248 across `tests/admin` and `tests/persistence`, typecheck and eslint clean. **Still owed under R37:** a written retention and deletion policy **before a second shop is onboarded**. Deletion is `unlink` plus clearing the column; the policy is the hard part. Nothing committed or deployed. | Founder (delegated) |
| D128 | 2026-09-08 | **M2 is built: a provisioned shop is issued its four sign-in parameters, shown once.** `admin/issueCredentials.ts` plus `POST /merchants/:id/credentials` and the hand-over screen. Operator ID, Device ID, a 43-character Device Key and a six-digit temporary PIN. 12 tests. Console only. | The founder's Steps 5 and 6. **Kept separate from provisioning (D120) for three reasons**, not tidiness: the display can be a `POST`, so a device key never enters browser history and pressing back does not bring it up again; it is **repeatable**, because a shop that lost its key needs new parameters rather than a new shop; and it carries its own step-up requirement and audit event. **Authorisation is `ADMIN_REGISTER_DEVICE`**, which `STEP_UP_REQUIRED` already lists — creating a device that can trade for a shop's money is not something an unattended laptop should do. **What is secret and what is not.** The ids are sequential and readable, as the founder specified, because they are not secrets: `Device Binding` is explicit that a device identifier is a string the client sends and evidence of nothing. The two things that are secret are generated with `randomBytes` and `randomInt` and stored only as scrypt hashes. A test dumps **every text column of every table** and asserts neither the key nor the PIN appears anywhere; another asserts the audit entry records the ids and not the secrets. **The temporary PIN is drawn, not adjusted:** a value `pinRejection` refuses is re-drawn rather than nudged, since nudging `111111` to `111112` would bias the distribution towards whatever the nudge produces. `must_change_pin` is set to 1, so a PIN Telga staff have read out cannot remain the shop's credential. **The test that makes the rest worth having** is the last one: the key and PIN an admin writes down are verified against the stored hashes **using the same `verifySecret` call and the same parameter string `sessions.ts` uses at sign-in**. Credentials of the right shape that do not authenticate are the failure a console-only test cannot see, and it is now ruled out. **One transaction throughout**: an operator without a device, or a device without an enrolment, is a shop that looks provisioned and cannot sign in. **Each issue creates a fresh operator and device rather than overwriting** — the ledger references devices, and rewriting one would re-point history at a machine that did not make it. **Verified:** 311/311 across `tests/admin` and `tests/auth`, typecheck and eslint clean. **Not done:** nothing tells the operator to change the PIN yet — `pinChangeRequired()` exists (D121) and no POS screen reads it, which is M2's remaining edge and belongs with the POS rather than the console. Nothing committed or deployed. | Founder (delegated) |
| D129 | 2026-09-08 | **The mojibake gate now covers source and configuration, not only the vault.** `npm run docs:validate` scans 320 files under `packages`, `services`, `apps`, `scripts` and `tests`, plus `package.json`. | **I corrupted a file the same way as the original incident, in the same session that fixed a different gate.** A PowerShell `Get-Content -Raw` / `Set-Content` round trip over `migrations/index.ts` — used to register migration 016 — read UTF-8 as the system codepage and wrote it back double-encoded, turning one em dash into the familiar three-character mangle. `Get-Content` defaults to ANSI in PowerShell 5.1, which is precisely the mechanism recorded for the 2026-09-07 vault corruption. **The sequence itself is deliberately not quoted here** — writing it into the record would fail this very gate, which the first draft of this entry duly did. It was caught within a minute because the file was read back and checked; it would not have been caught by the gate, because **the gate only looked at `.md`**. **A second, older one was found by the same search:** `package.json`'s description has carried a double-encoded em dash **in every commit since the project began** — `git diff` confirms the line is untouched by this session's edit, so it is inherited rather than introduced. Both are fixed. **The gate is extended rather than the habit trusted.** Scanning source costs nothing and closes the class: the original incident, this one, and the `package.json` one would all now fail `docs:validate`. `validate-vault.mjs` is excluded from its own scan because it contains the patterns it hunts for. Only the mojibake rule is applied to source — frontmatter, wikilinks and reachability are meaningless for a `.ts` file. **Proved by injection**, not asserted: a file containing one double-encoded sequence was written into `tests/`, the gate reported it by path and line and exited 1, and the file was removed. **The lesson, which is the same one R36 already carries:** the fix for "a tool corrupted a file" is not care, it is a check that runs whether or not anyone was careful. | Founder (delegated) |
| D130 | 2026-09-08 | **M3's core is built: a verified deposit credits the shop that quoted the reference, once.** Migration 016 (`funding_submissions`, `device_enrollments.deposit_lookup`), `admin/recordDeposit.ts`, and the lookup written at credential issue. 13 tests against a real ledger. | **Consulting the vault before building changed the schema, which is the reason the founder asked for it.** `05 Operations/Funding Verification` names the record every submission must carry — *merchant, amount, currency, bank reference, timestamps, verifier, **approver**, evidence, reason, ledger entry* — and separates the duties: a **second approver** for high-value, and a reconciler who is *"never the verifier"*. The first draft had `decided_by` and no approver at all, so a second approval by a different person could not be expressed. `approved_by`, `approved_at`, `currency` and `evidence` were added, and separation of duties is now a `CHECK` in the database — a row where one person did both is refused, so no code path can write one by forgetting. **The engineering blocker the founder should know about.** The device key is the deposit reference by decision (D125), and `device_enrollments` stores a **salted scrypt hash** — deliberately slow and unsearchable. Matching a quoted key would mean scrypt over every enrolment; at 10,000 shops that is minutes of CPU per deposit. Solved without disturbing the decision: an indexed `deposit_lookup`, a plain SHA-256 of the key, written at issue because that is **the only moment the key exists in plaintext**. Safe precisely because the key is 256 bits from `randomBytes` — there is no dictionary to run against it — which is why scrypt is right for a PIN and wrong for a lookup. The scrypt hash still does all the authenticating. **A constraint of mine said the wrong thing and the tests caught it.** The unique index was on `bank_reference` outright, which expresses *"one row per bank transaction"*. The invariant is *"one **credit** per bank transaction"*, and the difference is not pedantic: the first made it impossible to **record** that a slip had been submitted twice, losing evidence an operator may be asked about while protecting nothing. It is now partial, `WHERE status = 'CREDITED'`. A second credit is refused by the database; a second *record* is written as `DUPLICATE`. **What the tests assert** is balances read through `balanceFor` — the same function the POS shows an operator — rather than rows, because a row-level test can pass while a shop's screen says something else: shop A's deposit credits shop A and moves shop B by nothing; two shops' deposits land separately; an unknown reference is **never guessed at a shop** and waits as `MANUAL_REVIEW` with no merchant; no bank record means no credit; a wrong account is refused; and a deposit over the cap is matched to the right shop, recorded, and **not credited**, because §20's second approver is a second person and none is assigned. **Verified:** 273/273 across `tests/admin` and `tests/persistence`, typecheck and eslint clean. **Not built:** the console screens for the deposit queue, and the shop-facing display of its own reference, which lives in the POS and is deployment-affecting. Nothing committed. | Founder (delegated) |
| D131 | 2026-09-08 | **M3b's console half is built, and the founder's whole flow now runs end to end in one test:** register a shop, approve it, issue its four parameters, pay money in quoting the issued device key, and that shop's balance rises. `/deposits`, `/deposits/new` and `POST /deposits`, on the permission model the vault already specified. | **The permission model turned out to already encode the separation of duties**, which consulting `05 Operations/Funding Verification` first is what surfaced: `ADMIN_REVIEW_FUNDING` reads the queue, `ADMIN_APPROVE_FUNDING` records and credits and carries **step-up and dual control**, and `ADMIN_SECOND_APPROVE_FUNDING` is a separate permission that `FINANCE_VERIFIER` deliberately does **not** hold. Nothing needed inventing; the routes were mapped onto what was there. **The form asks for the bank's figures separately from the shop's claim**, because they are different kinds of thing and the separation *is* the control. Leaving the bank fields empty is a legitimate answer meaning "the bank has no such transaction", and it produces a rejection — a single amount box would have quietly invited the credit-from-a-claim that CLAUDE.md forbids at L128 and L402. A partial bank record is treated as no record at all: all three fields or none, because two out of three is a claim wearing a bank record's clothes. **The deposit screens close entirely when no credit port is configured**, rather than recording `CREDITED` with no posting behind it — a false record of a shop's balance is worse than an absent feature, because every later reconciliation would start from it. **The audit entry carries the bank reference and never the quoted one:** under D125 the quoted reference *is* the shop's device key, so recording it would hand a credential to every reader of the audit screen. Asserted. **A test-harness fault worth recording, because it produced a plausible wrong answer.** `onboardShop` selected the new application with `ORDER BY created_at DESC, id DESC`, and every row in the fixture shares one frozen timestamp — so "the newest application" is not a question the database can answer. The second shop was silently onboarded onto the first, both device keys belonged to one merchant, and the balance read 1,250 instead of 1,000. The id now comes from the redirect, and the test asserts the two merchants and two keys differ **before** any money moves, since if that is wrong every balance below it is meaningless. **Verified:** 10 end-to-end tests, including two shops paid separately with neither touching the other, an unmatched reference left with a null merchant rather than guessed at a shop, an over-cap deposit matched and uncredited, and an auditor refused. **Not built:** the shop-facing display of its own deposit reference, which lives in the POS and is deployment-affecting. Nothing committed. | Founder (delegated) |
| D132 | 2026-09-08 | **M4 and M5 are built: a shop can be stopped from trading and started again, one device can be stopped without stopping the shop, and the dashboard reports platform totals and what needs attention.** 21 tests. Console only. | **M4 — shop control.** Consulting the code before building found that both controls already bite: `createSale.ts` refuses a merchant whose status is not `ACTIVE` (line 142) **and** a device that is not `ACTIVE` (line 150). Suspending is therefore not a display change — it stops that shop selling on every device it owns, immediately. The two controls are deliberately separate: suspending a merchant stops the shop, stopping a device stops one machine while the business carries on, which is **R14's** case — a POS lost or stolen and a shop still open. Stopping a device does three things in one transaction, because a stopped device holding a live session would still be usable: the device leaves `ACTIVE`, the enrolment is `REVOKED` with reason `REMOTE_STOP`, and every session it carried is revoked. **Both are bounded by the rule the vault states twice** — `05 Operations/Merchant Onboarding` and `09 Engineering/Security Model`: *"halts selling without deleting history"*. Only one column moves, and the tests count transactions, devices, users, enrolments and session rows before and after to prove it. A suspension **requires a reason**, because it is the thing an operator will be asked about; reinstating does not, since it returns a shop to normal rather than taking something away. A shop that does not exist and one already suspended are answered identically, so the route cannot be used to discover which merchant ids exist. **The bug this turn, caught by reading the schema rather than trusting typecheck:** the session-revoking SQL used `revoked_reason`; migration 006 calls the column `revocation_reason`. TypeScript cannot see inside a SQL string, so it would have failed at runtime, on a stolen device, at the worst possible moment. **M5 — aggregates and alerts.** Platform totals (sales, volume, float held by shops) and three alerts (deposits waiting, shops low on float, licences expired or expiring within thirty days). Every figure is a **count or a sum with no way to open it**, which is how the brief's *"see aggregate metrics"* and *"**not** view individual shop transactions"* are both satisfied; a test asserts no transaction id, masked recipient, product type or idempotency key reaches the page. `BANK_CLEARING` is excluded from the platform float exactly as it is from `balanceFor`, or the counterparty side of every deposit would double the figure. **A stale claim was corrected:** the dashboard reported that the platform ledger residual *"cannot be proven"* because per-shop databases were unreadable from the console. Under D120's shared model that is no longer true and saying it would be the opposite failure — telling an operator nothing is known when it is. It is now one query, and an unbalanced ledger is reported plainly with the amount. **The low-float threshold is flat and deliberately so:** Telga has no evidence about what a busy shop turns over, and inventing a per-shop figure would be the fabricated number CLAUDE.md §30 forbids. It becomes a setting when pilot data exists. **Verified:** 157/157 across `tests/admin`, typecheck and eslint clean. Nothing committed. | Founder (delegated) |
| D133 | 2026-09-08 | **Priority, taken from the vault rather than from preference: (1) start the backup schedule, (2) assign the owners, (3) settle the stored-value licensing question — then the deployment-affecting phases.** The schedule itself is built: `services/backup/src/schedule.ts`, 14 tests. | **The vault answers the priority question directly, in two places.** [[Launch Gates]]'s dependency graph shows [[Founders and Roles]] feeding gates 8 and 9; gate 10's own row says the restore evidence exists but *"no named owner has reviewed it — this register does not clear a gate by assertion"*; gate 6 requires *"separation of duties proven"*, which needs two people; and both `money.live` keys are unassigned. **Every owner row in [[Founders and Roles]] reads NOT ASSIGNED** — seven of them. So one decision, costing nothing, is what stands between four gates and any progress at all. **Correction, same day, after the founder challenged it.** This entry first ranked the backup schedule **first**, on the grounds that *"volume data is deleted thirty days after credits lapse"*. **That was overstated for this deployment and the founder was right to question it.** The rule is about credits *running out*; the same section of [[Railway Deployment Checklist]] says **Hobby's $5 credit covers this service with no overage**, and the founder is on Hobby. The deletion clock never starts while the subscription is paid, so there was no imminent loss and it should not have been described as one. **What survives the correction**, from the same checklist: *"**Copy the backup off Railway** — a backup on the volume does not survive the volume."* One volume is one copy, and corruption, a bad migration or an accidental delete are not payment-related. So the schedule is worth having and worth wiring; it is **not** an emergency, and the priority is revised accordingly — see D134. `05 Operations/Backup Restore Implementation` still lists it as *"Not designed — this is a CLI, not a cron job"*, and it no longer is. **What was built.** A loop with injected clock, sleeper and backup runner, so it is tested without waiting for hours to pass. Its design goal is not "run a backup every day" but *"make the absence of a recent backup impossible to miss"*, because **a schedule that stops silently is worse than none** — somebody believes they are covered. Hence: every attempt records its outcome with a reason; a failure **never stops the loop**, since a full disk at 02:00 must not mean no further attempt is ever made; and pruning happens **only after a successful backup**, because deleting an old copy on the one night no new one was taken is the worst possible moment to reduce what exists. **Two refusals at start-up**, checked before the first backup rather than discovered on the first prune: an interval under a minute is a mistake rather than a policy, and **a retention window shorter than twice the interval is refused outright** — it would delete each backup before the next was taken, leaving nothing, a schedule that runs perfectly and protects nobody. Retention was *"deliberately not implemented"* in the CLI on the grounds that a tool which deletes backups can delete the wrong one; here it is necessary, because an unbounded schedule fills the volume the ledger lives on and a full volume stops every shop trading. **`scheduleWarnings` states what it does not do**, returned rather than logged once, so it can reach a screen: backups written to the same volume protect against a corrupted database and **not** against the volume being lost, which is exactly what the thirty-day rule describes. **Gate 10 stays open** until a copy leaves the platform, and the code says so rather than letting anyone infer otherwise. **A typecheck finding worth recording:** TypeScript narrowed `signal.aborted` from the loop condition and called the later check unreachable — true of the type, false of the program, since the flag flips *during* the awaited backup. Reading it through a function call restores the check, and without it a `SIGTERM` arriving mid-backup would have been answered by sleeping for a day first. **Verified:** 14 schedule tests, 41/41 across `tests/backup`, and a full suite of **1633 passing** (exit 3, the A51 classifier reporting reporter starvation rather than a failure — D122 working on its second real encounter). **Not done:** wiring the schedule into `scripts/deploy/railway-start.mjs`, which is a deployment change and needs its own approval. Nothing committed. | Founder (delegated) |
| D134 | 2026-09-08 | **Recommendation: M6, deploy the operations console, is the next step.** Everything built in M0–M5 is reachable from exactly one laptop until it is. | **The finding that decides it:** `scripts/deploy/railway-start.mjs` starts the recovery worker and the POS, and **not the console**, and `apps/operations-console/src/cli.ts` refuses anything but loopback over HTTP. That refusal is deliberate and correct — *"a console session can suspend merchants, approve funding and create administrators"* — but its consequence is that registering a shop, issuing its four sign-in parameters, recording a deposit and watching a balance move are all real, tested, and **usable by nobody**. Six phases of work are behind a door that is not open. Building a seventh does not change that. **What M6 actually is: a security decision with a small amount of engineering attached.** The console needs TLS and an address, and the founder must decide whether it faces the public internet at all. Three things already argue it can: `admin_users` is a separate identity table with no merchant and no device; every state-changing route is behind password, TOTP and — for approvals, device registration and document access — **step-up re-authentication**, proved at HTTP level; and `script-src 'none'` means there is no client-side attack surface to speak of. Three things argue for caution: it can suspend a merchant, create an administrator, and open a scanned passport. **The middle path worth considering** is a separate Railway service with its own domain, TLS, and no public link — reachable but not advertised — with the option of IP allow-listing later. **What is deliberately not recommended next.** *The backup schedule* (D133's correction: not urgent on a paid plan). *M3c*, the shop-facing deposit reference, because it touches the deployed POS and a shop cannot use it until the console that records deposits is reachable anyway. *M7*, Android self-registration, for the same ordering reason. *M8*, Postgres, which needs evidence of contention rather than a target number. **Two things that cost a decision rather than engineering** should happen alongside, not after: assigning the seven unassigned owners in [[Founders and Roles]], which alone blocks gates 6, 8, 9, 10 and both `money.live` keys; and the stored-value licensing question, which decides whether the deposit model built in M3 is lawful without a licence Telga does not hold. **Nothing was built for this entry.** It records the recommendation and its reasoning so the next decision has something to argue with. | Claude Code |
| D135 | 2026-09-08 | **M6 is approved and half built. The console will run in the **same** Railway service as the POS, on its own port and its own domain — not as a separate service.** The console-side work is done: a trusted-proxy termination mode, a health endpoint, 11 tests. The one deployment-affecting file is untouched and awaits approval. | **The founder's constraint "separate Railway service" cannot be met, and the vault says why:** *"Two services cannot share one volume, and a copied ledger is not a ledger"*, and *"one volume per service is Railway's limit"*. A separate console service would open an **empty database** — no shops, no deposits, no balances. The alternatives were: same service with a second port and domain; or hold M6 until D108's Postgres refactor, which would leave everything built in M1–M5 unusable behind an ~80-file async migration that D108 itself says *"is not justified before a provider agreement exists"*. **Same service was chosen.** It delivers six of the seven constraints — own domain, TLS, unlisted, admin-only, step-up on state-changing routes, audit logs — and loses only process isolation, which is mitigated by adding the console as a **non-fatal** child so a broken admin screen never stops a shop selling. It is also reversible: when M8 lands the console moves to its own service with no application change. **The loopback rule was not relaxed, which was the temptation.** The founder's brief said to update `cli.ts` *"to allow the Admin Console domain (not just loopback)"*. Doing that literally would serve anything reaching the container and lose the scheme detection that makes a `Secure` cookie work. Instead a **third way to satisfy the same rule** was added: binding beyond loopback requires either a TLS pair or a `--trust-proxy` range, which is the POS's own answer (D109) and is **reused rather than reimplemented** — `parseTrustedEntry` is imported from `@telga/merchant-pos`, so D45's "there is no trust-all setting" holds for the console because it is literally the same parser. `--allowed-hosts` becomes mandatory under a proxy, since the `Host` header is client-controlled. `secureCookies` is now true under a proxy as well as under in-process TLS — getting that wrong is the failure that looks like nothing, with the session travelling in clear text on the last hop. **Checking the vault changed the health endpoint after it was written.** `09 Engineering/Health Endpoints` specifies properties the first draft lacked: the `nosniff` and `no-referrer` headers, a **stable reason code** rather than an exception message, and a `405` on anything but `GET`. All three were added, and a test asserts the response discloses no schema, version, path or identifier — it is the only route reachable without signing in. **Three corrections to the founder's decision, stated rather than quietly worked around:** *face biometry does not exist* (WebAuthn columns exist, nothing implements it, and `script-src 'none'` blocks it), *an admin PIN does not exist* and the vault's Decision 1 says *"never a PIN"* for admins, and *no Railway service or URL can be created by me* — those are dashboard actions on the founder's account. What actually protects the console is password, TOTP and step-up re-authentication. **Founder answers recorded:** Railway-generated domain for now, keeping `telga.pro` unattached; the backup schedule to be wired in the same deployment; and document retention left unset until legal question **L15** is answered, with the deletion mechanism built and the clock deliberately not started. **Verified:** 168/168 across `tests/admin`, 366/366 with `tests/auth`, typecheck and eslint clean. **Not done:** `scripts/deploy/railway-start.mjs`, the only deployment-affecting file, which needs its own approval. Nothing committed. | Founder |
| D136 | 2026-09-08 | **Administrator sign-in becomes two paths: email and password followed by a code emailed to that address, or a biometric passkey with no second factor. No admin PIN.** Migration 017 and `auth/emailOtp.ts` are built with 17 tests; the passkey half is not. | **The founder's design, and their decision to make.** I had been reporting face biometry as impossible because the console serves `script-src 'none'` and WebAuthn is a JavaScript API. **That is a constraint this project wrote, not a law**, and the founder was right to say so: *"in my system everything must be as I want, I am owner so I can decide — you should ask me if I want or not but you can't decide."* The trade is stated once and accepted: the console will run JavaScript on its sign-in page. **Why the passkey path skips the code, which is the part worth understanding.** A passkey is already two factors — the device must be present, and it must verify its owner biologically before it will sign. Adding an emailed code to that would be a third factor on the strong path and a second on the weak one, pointed at the wrong door. Email and password is a single factor, so it gets the code. **The biometric itself is stored nowhere**, which D105 settled: the device verifies the face and releases a key, and Telga holds a public key — never an image, never a template. The columns have been waiting in migration 014 since it was written. **This replaces TOTP, and does not delete it.** The founder chose emailed codes; `mfa_secret_hash` stays in the schema because dropping it would make the switch irreversible and would break any administrator enrolled before it. Columns are cheap; a migration that deletes credentials is not. **Three bounds on a six-digit code, because one is not enough.** A million possibilities is a small number against unlimited guessing, so: scrypt-hashed at rest, so reading the table hands nobody a live code; **five attempts**, after which the *code* dies — not the account, because locking the account would hand anybody who knows an administrator's email a denial-of-service button; and a ten-minute expiry. A hash without a limit is still brute-forceable at six digits; a limit without a hash leaks every pending code to whoever reads the database. **Cheap refusals come first**: expiry and the attempt limit are settled before any scrypt derivation, so a dead code costs nothing to refuse — asserted by a timing test with a generous margin. Resends are rate-limited to thirty seconds, or a caller who knows an address can make Telga mail it as fast as they can press a button. **Delivery is a port, and its default refuses.** Telga has no email infrastructure and choosing a provider is the founder's call, so `noSenderConfigured` rejects every send with a message naming what is missing **and pointing at the passkey path, which needs no code** — an administrator is not locked out by a missing sender. A console that issued codes nobody received would lock everyone out while looking like it worked. **Verified:** 17 tests, 147/147 across `tests/persistence` with migration 017 applied, typecheck and eslint clean. **Not built:** the passkey ceremonies. The founder chose a vetted library over a hand-written verifier — *"a hand-rolled auth verifier with a subtle bug is an authentication bypass"* — so that step adds `@simplewebauthn/server`, breaking the single-runtime-dependency norm deliberately and with the reason recorded. **Also outstanding:** an email sender, and the CSP change that lets the sign-in page run script. Nothing committed. | Founder |
| D137 | 2026-09-09 | **Device registration is written into `CLAUDE.md` §18.1 and [[Device Registration]]: an admin creates the device and issues a one-hour single-use activation code; the device exchanges it for a 256-bit key. Device-initiated self-registration stays forbidden.** | The founder asked what the documentation said about a POS or Android device registering itself on first launch, and asked that it be added to `CLAUDE.md` if nothing was there. **Nothing was there.** `CLAUDE.md` had no registration flow at all — only §7.1 naming "device registration" as scope, §18 listing device *controls*, §22 listing a "registration" screen and §24 saying "device binding". Four of the six vault notes named in the request did not exist. Meanwhile the mechanism **had already been built**: `admin/enrollment.ts` issues the code and `admin/redeemEnrollment.ts` redeems it. So the documentation was behind the code, which is the failure mode where somebody later "fixes" a deliberate refusal. **What is now written down:** the two secrets are separate — the 100-bit code a person reads down a phone line, and the 256-bit key no human ever sees — because a credential that authenticates every later request must never have been spoken aloud. Single use is structural: `device_enrollments` holds one hash per device, and redemption overwrites the code's hash with the key's, so a replay fails because what it compared against is gone. Unknown device and wrong code answer identically, so activation cannot enumerate device IDs; expiry is distinguishable, because that leaks nothing the caller did not supply. **What is refused, and why it is refused rather than deferred silently:** the founder's described flow — the app registering itself when first opened — is device-**initiated**, and everything built is admin-initiated. A device that provisions itself must carry a secret to be trusted on, that secret would ship in the APK, and an APK is a public file. It would also bind a device to no approved merchant, which contradicts [[Merchant Onboarding]] and [[Admin Operations Console]] Decision 3. D113(b) already deferred it; this entry names it in the authoritative file so it cannot be re-added by accident. A safe device-initiated flow exists in principle — attested install, or a merchant-authenticated enrolment session — but it is **new design and needs a founder decision**, not a rebuild. | accepted |
| D151 | 2026-09-10 | **Merchant-initiated reversal is wired: a shop asks from the app, a supervisor decides in the console.** `/reverse` on the POS files a request; `/reversals` in the console is the queue. Approving records the **authorisation** and does not post the ledger entry — settlement is a separate act. | §17.1, and what makes a `LEGITIMATE` complaint verdict actionable: a reviewer could decide a shop was owed money and had no way to return it. **Every request in this build needs a person, and that is not a limitation being worked around.** `decideReversal` refuses outright on a `REDEEMED` token and settles without approval on an `UNREDEEMED` one — and this build can read **neither**: there is no provider API to ask and no redemption column anywhere in the schema. So redemption is `UNKNOWN` for every request, which routes all of them to a human. §17: *never auto-refund an unknown outcome*. Inventing `UNREDEEMED` would auto-settle reversals on no evidence at all. **The queue says "Cannot be read" rather than hiding it**, because a supervisor approving one is deciding on the merchant's word and should know that is what they are doing. **Approval and settlement are deliberately separate.** §13 invariant 8 — *a correction is an authorised adjustment entry, never a silent edit* — requires the authorisation to be visible apart from the entry it authorises. Approving moves the request to `APPROVED` and the console says in words that the balance **has not moved yet**, rather than leaving an operator to assume a shop has been paid. **A bug the tests caught, in money-adjacent code:** `decideReversalRequest` had `APPROVED` in its `WHERE`, so an approved reversal could be flipped to `REFUSED` afterwards — two decisions in the trail with nothing saying which governed the money. Narrowed to undecided statuses only. Settlement moving `APPROVED → SETTLED` needs its own path, which is exactly why widening this one to cover it was wrong. | accepted |
| D150 | 2026-09-10 | **Complaint verification is built, and it closes R44.** A shop reports a problem from the app (`/complaint`); the case lands in the console's **Support** queue, oldest first; a reviewer sees the shop's own words **and the named transaction**, and records a verdict of `LEGITIMATE`, `SCAM` or `UNCERTAIN` with a reason. | §17.2, and the resolution of the highest open risk in the register. **R44** was opened when D144 limited Telga staff to aggregates: §17 obliges Telga to answer *"paid but no airtime"* by searching for the transaction within 24 hours, and that had become impossible with no replacement. **The exception is narrow and it is the whole point.** Staff still see aggregates and never browse a shop's sales. Inside an **open case the merchant themselves opened, about one sale they themselves named**, the reviewer sees that transaction — scoped to the case, to the transaction, and to the merchant, with no way to reach a second sale from it. **Every view is audited** with the case reference it was read under, and never the recipient. **The recipient stays masked even here.** A case is a reason to see the sale, not a reason to see a customer. **A verdict moves no money**, and a test asserts the ledger is untouched by one. §17: never auto-refund an unknown outcome — and never auto-refund a known one either; reversing is a separate authorised act (§17.1), and the console says so in the words it shows after recording a legitimate verdict. **The evidence is stored with the verdict**, not re-read later, so a case stays reviewable against what was actually known at the time. `provider_state` is left null rather than filled in, because this build has no provider API to ask and recording a value nobody looked up would be inventing evidence. **The shop is never promised a refund.** The complaint form promises an answer and names the deadline; §17 forbids the rest. And there is no photograph upload, for D138's reason — a route accepting arbitrary bytes is a way to fill a volume, and an admin attaches evidence at review where the originals are. | accepted |
| D149 | 2026-09-10 | **The review screen shows the paperwork, with a green light.** Every required document, its state (**not supplied · expired · expiring soon · reference only · in order**), its reference, its expiry with days remaining, and a link to the encrypted scan where one exists. A verdict at the top: `READY`, `READY_WITH_WARNINGS` or `NOT_READY`. | The founder asked for a corporate-level registration dashboard. The audit found something worse than a missing dashboard: **the review screen rendered no documents at all.** A reviewer was asked to approve a shop without seeing whether it had supplied a licence, a TIN or an ID, let alone whether any were in date — while the rows and the AES-GCM encrypted scans had existed since D125. **The verdict is information, never a gate.** A reviewer may approve a `NOT_READY` application and sometimes should: a licence expiring next week is a real shop with a real problem, not a forgery. The decide form is not disabled by it. What is prevented is approving one **unknowingly**. **Four states because there are four conversations.** *Documents incomplete* tells a reviewer nothing about what to say on the phone. Missing means you never sent it; expired means the shop is not currently licensed to trade; expiring soon is a diary note; reference-only means nobody has seen the paper. **The worst thing about a document wins.** An expired unscanned licence reports as expired, not as unscanned, and blocking rows sort to the top — a reviewer reads down and must meet the blocking fact first. **Reference-only never blocks**, because it is exactly what the app's own registration form produces (D138); blocking it would refuse every self-service application. **Computed once**, in `admin/registrationReadiness.ts`, so the review screen, the queue and any later report cannot disagree in front of a merchant. Opening a scan keeps its own permission (`ADMIN_EXPORT_DATA`, with step-up) and its own audit line, and the link is offered only when a scan exists and the admin may open it — a *View scan* that 404s is worse than no link. | accepted |
| D148 | 2026-09-10 | **Remote stop gets a button, and a device can be reinstated — but reinstating never returns the old key.** The devices screen shows **Stop** on an active device and **Reinstate** on a stopped one, never both. `POST /devices/:id/reinstate` returns the device row to `ACTIVE` and **leaves the enrolment revoked**, so the shop pairs the machine again with a fresh activation code. | A dead-button audit — every link and form the console renders, against every route it serves — found two gaps in the opposite direction from the one being looked for. **`POST /devices/:id/stop` worked and had no button**, reachable only by typing the address; `CLAUDE.md` §18 lists *"remote stop of new sales without deleting history"* among the device controls a merchant platform must have, so a required control was effectively absent. And **reinstate did not exist at all** — a stopped device was stopped for ever, the commonest real case being a machine reported lost and then found, which needed a whole new device record and activation code for no reason. **Why reinstating is deliberately not the inverse of stopping:** stopping revokes the enrolment because the point of the control is *this device is not where it should be* — the key may have been read off a machine out of the shop's hands. Handing the same credential back would undo the only part that mattered. The machine returns; the credential does not, and the redirect says so in words rather than leaving an operator to discover it. **Also recorded here because it belongs with this work: R46 was withdrawn.** It claimed a stopped device could sign back in, on the grounds that sign-in reads the enrolment state and stop writes `devices.status`. The first half is true; the conclusion was not — stop writes **both**, plus the sessions, in one transaction, so a stopped device was already refused. The inputs to the reader were read and the writer was not, during an audit whose whole purpose is to walk between them. The `devices.status` check added in response is kept as defence in depth and **closed no hole that was open**. | accepted |
| D147 | 2026-09-09 | **Telga Pay gets a real settlement path: a card sale posts a balanced, append-only ledger pair, idempotent on the authorization code. `payments.acceptance` stays off, and settlement *refuses* while it is — it never silently no-ops.** | The founder: *"telga pay supposed to be live money not false simulation … make a path, there no false simulation, everything must work perfectly"*. They are right about the defect. D68 built Telga Pay UI-only — *"no new table, no migration, no persistence"* — which was the correct first step and became theatre once it stayed: **a card sale authorised and posted nothing to the ledger**, so a screen said paid and no balance moved. **What is now real:** the double-entry, the accounts, the append-only ledger, idempotency on the authorization code (§13 invariant 6 — a flaky counter *will* retry, and must credit once), the fee split, and the refusal rules. The `PaymentProcessor` port an acquirer implements was already production-shaped — `authorize`, `reverse`, `healthCheck` — so turning this live is a **contract and configuration** change, not a rewrite. **What code cannot make real, stated once so it is not mistaken for caution:** the acquirer. There is no acquiring contract, no processor credentials and no certified reader — **there is no endpoint to call.** The simulator is the only `PaymentProcessor` wired in because it is the only one that exists. When a real one exists it implements the same three methods and `cardSettlement.ts` does not change. **Two guards against training and production quietly swapping places:** a settlement with `payments.acceptance` off returns `FEATURE_DISABLED` rather than a success with an empty ledger — a no-op that reports success is how a screen says "paid" while a balance disagrees — and a **simulated** authorization can never settle, checked explicitly rather than inferred from the mode, because an inferred answer is one a refactor can get wrong in silence. **`cardSettlementBlockers()`** returns what is outstanding, so a start-up check, a launch-gate report and an operator screen render one list rather than three drifting copies. It can never return empty while the flags are off. **The fee is zero and no rate is invented** (§19, §30). | Founder |
| D146 | 2026-09-09 | **`transfer.shop_to_shop` is switched ON for the training deployment, by founder instruction.** Migration 019 adds `reversal_requests`, `complaint_reviews` and `shop_transfers`; `application/shopTransfer.ts` settles a transfer as one atomic unit. | The founder's words: *"do not need legal advice, if issue happen on feature we will revoke it, for now continue"*. Recorded as their decision. **What was flagged before it was taken, and remains true:** moving value between two legal entities is money transfer, and §2 and §7 list `remittance`, `cash.in_out` and `payments.acceptance` as disabled until legal review and an authorized-partner structure exist. **Why turning this flag on is nevertheless safe today, and this is the part worth understanding:** the flag was never what blocks real money. `money.live` is **false** and `assertSafeStartup` refuses to boot if any `MOVES_REAL_MONEY` flag is on, so what `transfer.shop_to_shop` gates is the **simulation** — a training ledger, no counterparty, no bank, no network — exactly as `card.simulated` gates the Telga Pay simulator (D87/D124). Turning it on in a training deployment changes what a merchant can practise, not what can leave the building. **The regulated question returns in full the day `money.live` is considered**, and this decision does not touch that. **Deliberately still absent from `MOVES_REAL_MONEY`**, because what sits behind it moves none — and adding it would make that list mean "features we are nervous about" rather than "features that move real money", which is the distinction that makes the startup assertion useful. **The one property the implementation is built around:** both ledger sides and the transfer row commit in a single transaction. A debit that lands without its credit is money *destroyed* — the sender poorer, the recipient no richer, the ledger unbalanced against §13 — and no ordering of two separate writes can be made safe, because a process can die between any two statements. Pinned by a test that throws inside the posting and asserts nothing at all was written. **The fee is charged on top, to the sender**, so the recipient receives the number both shops agreed. Otherwise the sender says 500, the recipient says 495, both are right, and Telga arbitrates its own fee. | Founder |
| D145 | 2026-09-09 | **A device key is never displayed. Instead the console shows a *fingerprint* and can *check* a key a shop reads out.** The device list gains a **Key** column — six characters derived from the stored hash, safe to print, read aloud and write in a case note. A **Check a device key** form takes a key an operator has been read and answers `MATCH`, `NO_MATCH` or `NOT_ENROLLED`. The key is compared and discarded: never stored, never logged, never echoed. | The founder asked the console to show *"Device Key (masked)"*. **Taken literally that is not buildable**, and the reason is the whole design: a key is generated at activation, shown once, and stored only as a scrypt hash (§18.2). There is no ciphertext to decrypt and no plaintext to mask — masking implies a value exists to be partly hidden, and none does. Saying only that would have been useless, because the **need** behind the request is real: an operations desk wants one question settled, *is the key this shop is holding the right one?* Both halves here answer it without Telga ever knowing a key. **The fingerprint is derived from the hash, not the key**, with a domain-separated second digest on top — so it cannot be walked backwards, and a shop **cannot compute their own** from their key without the salt. That is deliberate: a value a shop could compute is a value an attacker holding one phished key could use to test it against other devices. It uses the activation code's alphabet, excluding `I`, `O`, `0` and `1`, because a misread fingerprint gives a confident wrong answer. **An unknown device and a wrong key answer differently here** — the opposite of `/activate`, and on purpose. There the caller is anonymous and telling them apart would build a device-id oracle. Here the caller is a signed-in administrator who can already list every device, so it discloses nothing and saves them checking a key against a device that was never activated. **The audit records the outcome, never the key.** An audit trail carrying credentials turns every reader of the audit screen into a holder of them. **A wiring gap was found and closed in the same change:** the route redirected with a `notice` the devices screen did not render, so a check answered silently. A control that answers silently is a control nobody trusts. | accepted |
| D144 | 2026-09-09 | **Telga staff see aggregates, never a shop's individual transactions — and never print a shop's slip.** The console's transaction list and detail page, built the same day, are **removed**: `/transactions` and `/transactions/:id` answer 404, and `/activity` replaces them with per-shop counts, volume and counts by state. No recipient in any form, no transaction id, no single sale amount. No print or reprint of a shop slip exists anywhere in the console, and a test asserts none appears. Feature flags stay **global** (§7, §16 "no merchant override"); per-shop overrides are rejected. | The founder's rule, in their words: *"the Admin Panel sees aggregates and performance, not individual transactions"*, and *"some privacy of shops must be protected"*. A Telga employee browsing a shop's line-by-line trade is a boundary the founder chose to draw. **This reverses a choice made earlier the same day.** Offered three options, the founder first chose *masked recipient with full detail behind a recorded reason*, then supplied a written specification stating aggregates only. The contradiction — and the §17 consequence below — were put back to them explicitly, and they confirmed aggregates only. Recorded as a reversal rather than smoothed over, because the removed screens were built, tested and working. **The cost, stated before the decision and again here: `CLAUDE.md` §17 becomes unimplementable from the console.** §17 requires *"search by transaction ID, receipt, time, amount, or reference"* and a final answer within 24 hours for a "paid but no airtime" complaint. That search is now impossible for Telga staff, and **no replacement mechanism exists**. Recorded as **R44**, open. The likely resolution is that individual rows become visible only inside a support case scoped to one shop — which was the third option offered and is worth revisiting once support cases exist. **Removed rather than unlinked.** The routes 404. The feature-flag work already had to learn once that hiding a button while the endpoint still answers is a defect, not a control. **What the aggregate screen still shows, deliberately:** per-shop **pending** and **under-review** counts. That a shop *has* a problem is a platform fact an operations desk cannot work without; whose money it was is the shop's business. That line is where the boundary sits. | accepted |
| D143 | 2026-09-09 | **The console can run single-factor: a password alone, with no second factor and no step-up re-authentication.** Off by default; switched on with `--single-factor true` or `TELGA_CONSOLE_SINGLE_FACTOR=true`. A banner appears on **every page** and the flag is printed at start-up. **Also: `/deposits` is wired to the ledger and appears in the navigation.** | The founder's instruction after signing in for the first time: an administrator who has just typed a password should not be asked for a six-digit code again on every button. Both checks are relaxed **together**, because leaving step-up on would reproduce the complaint exactly — `ADMIN_APPROVE_MERCHANT` and `ADMIN_REGISTER_DEVICE` are step-up permissions, and they are precisely the buttons needed to approve a shop and issue a device code. **Implemented as a policy parameter, not a deletion.** `requireAdmin` takes an `AdminAuthPolicy` defaulting to strict, so every existing caller is unaffected and switching back on for real money is one setting rather than a rebuild. `!== false` rather than `=== true`, so a half-filled policy object cannot silently disable a check. **What it does not touch: authority.** Permissions are still enforced, a suspended administrator is still refused, and every action is still audited under its actor's name. Relaxing how hard identity is proved and widening what an identity may do are different decisions, and merging them is how an audit finds that training mode granted somebody the ability to move money. Pinned by tests that assert a support agent is still refused `ADMIN_APPROVE_MERCHANT` under single-factor. **Why the banner is not optional:** a deliberate relaxation that is invisible becomes an accidental one the first time somebody copies the configuration somewhere it does not belong. **The deposits finding:** adding the missing navigation entry exposed that `/deposits` answered 404, because the CLI never supplied `creditMerchant` — an option whose absence deliberately closes the screens rather than recording a credit that never posted. That guard was right; the CLI simply never satisfied it. It is now wired to `fundMerchant`, which posts a balanced append-only pair against `BANK_CLEARING`. Nothing reaches a bank: this credits a training float, and `funding.submission` stays off. **This must be off before real money.** `CLAUDE.md` §8's "security and permissions tested" gate cannot close with it on — recorded as A109. | accepted |
| D142 | 2026-09-09 | **`Referrer-Policy: same-origin` replaces `no-referrer` on both apps, and an opaque `Origin` is judged on `Sec-Fetch-Site` rather than refused outright.** | A browser reported *"Refused: cross-site request"* on console sign-in for the second time in one day. The first cause (IPv6 host parsing) was real and already fixed; this was a different bug wearing the same message. **`no-referrer` does not only strip the `Referer`:** when the referrer policy suppresses the referrer, a browser serialises a form POST's `Origin` as the literal `null` — so the console refused a form it had served itself, on its own origin. **Why no test caught it:** everything that is not a browser sends a real `Origin`, so `curl`, Node and every existing test sailed through — and the suite actually asserted that `null` must be refused, encoding the bug as intended behaviour. It was found by reproducing the round trip and reading the headers the server actually sends, not by re-reading the check. **The fix removes the cause and then hardens the fallback.** `same-origin` keeps the property that mattered — no referrer leaves the origin, so no admin URL reaches a third party — while letting a same-origin navigation carry the header the CSRF check needs. An opaque origin is then accepted only when `Sec-Fetch-Site` is `same-origin` or `none`; `cross-site`, `same-site` and a missing header are refused. `Sec-Fetch-Site` is set by the browser and unreachable from script, so the page this check exists to stop cannot forge it. **What was deliberately left alone:** the POS accepts `origin === 'null'` unconditionally, which also admits a sandboxed iframe. Removing the header cause makes that allowance mostly dead code; tightening a deployed merchant sign-in path blind, in the same change, is the wrong order of operations. Recorded as R43, to be fixed when a real device is available to test on. | accepted |
| D141 | 2026-09-09 | **The operations console is served from the Railway deployment, behind a Host-routed front proxy, and only when `TELGA_CONSOLE_HOST` is set.** `$PORT` is taken by a small proxy; the POS and the console bind loopback on internal ports and are unreachable except through it. An unknown or absent `Host` goes to the merchant app, never the console. A first administrator is seeded from `TELGA_CONSOLE_OWNER_EMAIL`/`_PASSWORD` on boot. | The founder's test plan is: a shop applies, they approve it in the admin panel, they test, and they report what needs correcting. **That plan could not run.** `railway-start.mjs` started the POS and the recovery worker and nothing else, so on the live URL a registration could be submitted and nobody could approve it — the console existed only on a laptop. **Why one service and not two:** the console needs the same SQLite file, and a Railway volume mounts to one service. So both processes must live in one container, and one container exposes one port. **Why Host and not a path prefix:** a prefix means rewriting every absolute link, form action and redirect in the console — about 50 places — and one missed link is a silently half-working admin panel, which is worse than none at all. On its own hostname each app sees itself at the root, its cookies scope correctly and its own allow-list is unchanged. The cost is that the operator attaches a second domain, which is one setting. **Why opt-in:** unset, this file behaves exactly as before. An admin panel is not something to switch on by accident, and the change had to be incapable of breaking a deployment that was working. **The extra hop was thought about rather than absorbed:** loopback is added to each app's trusted set because the proxy is inside the container and is the only thing that can reach either port, and it forwards Railway's `X-Forwarded-*` headers **unchanged** — rewriting `X-Forwarded-For` would replace the client's address with the proxy's own, which the POS uses to throttle registration by source. No trust-all entry is introduced (D45, D109). **Owner seeding is idempotent by construction:** the email is unique, so every boot after the first fails and changes nothing; it cannot overwrite an owner, reset a password or reinstate a suspended account. A password alone still opens nothing — the console requires a second factor before any screen works. Pinned by `tests/admin/console-routing.test.ts`, including near-miss hostnames a `startsWith` comparison would hand the admin panel to. | accepted |
| D140 | 2026-09-09 | **Receipts print, via the device's own print stack, and every printed slip carries `TRAINING — NO REAL VALUE`.** A **Print receipt** button on the slip screens calls `window.print()`; the existing print stylesheet hides everything but the slip, sizes it to the shop's 58 mm or 80 mm roll, and forces the mark to solid black. | Founder instruction: it must print, and the paper must say no real value. **The banner was already there** — `slipCard` draws `receipt.training_notice` unconditionally, so no screen can print a slip without it — and what was missing was the action: the stylesheet existed and there was not even a `window.print()` call, which `ASSUMPTIONS` A101 recorded. **What this is, stated plainly so no more is claimed than is true:** it is not an ESC/POS driver and Telga speaks no printer protocol of its own. It reaches whatever the device is already paired with. **Why that is the right first implementation rather than a shortcut:** the alternative is a vendor SDK per POS model and the model is not chosen yet; this works on every device that can print at all, needs no permission, and cannot silently print the wrong thing because a human sees the dialogue. A native path can later sit behind the same button. **Print is a plain button; Reprint stays a form.** Printing moves no money, writes no row and needs no CSRF token; a reprint appends an audit line. The two must not look alike. A failed print is not a failed sale. | accepted |
| D139 | 2026-09-09 | **Sign-in lockout tightened to four attempts; the app's second button is *Register as Telga member*; `/activate` is wired; Telga Pay confirmed on.** Founder instruction. **Lockout 5 → 4 failed attempts, five-minute hold unchanged** — four leaves an honest operator three recoveries from a genuine slip and still ends a guessing run quickly, and five minutes is short enough that a mistyped PIN never becomes a support call. **`/activate` wired**: `redeemEnrollmentToken` had been written, tested and left with no HTTP caller since 2026-09-08, so the activation-code exchange in [[Device Registration]] was logic on a shelf and device keys still came from a CLI — the step that exchange exists to remove. It is served anonymously because a device with no key cannot sign in, and linked from the sign-in screen because that is the only screen such a device can reach; it is throttled on its own budget so activating a second till is not refused because somebody else registered from the same wi-fi. **Telga Pay needed no change**: `card.simulated` and `deposits.training` were already on under D124 and every `/pay` route was already open — verified rather than assumed, and recorded here so the question is not asked a third time. | The founder asked for the lockout, the button label, the pairing feature and Telga Pay, and asked that the remaining choices be made rather than put back to them. **What was deliberately NOT decided here:** D103, shared-versus-per-tenant storage. The founder's own message restates it as something to decide consciously before the second real shop, and `CLAUDE.md` §30 reserves irreversible architecture for the founder — so it stays open, and §18.4 stays the honest statement of it. | accepted |
| D138 | 2026-09-09 | **Self-service vendor registration is opened, reversing the *registration* half of D113(b); the *device* half stays closed.** The Telga app opens on **Login** and **Register as Telga member**. Path A: a shop submits from the app, the submission lands in the console's review queue as `SUBMITTED` / `submitted_via = 'SELF_SERVICE'`, an admin approves or rejects, approval provisions the shop, credentials are issued as a separate act, and only then can that shop sign in. Path B: an admin recording a registration at the counter may tick *Approve immediately* — **admin creation is the approval**, with no pending state, because the recorder and the reviewer are the same person looking at the same documents. Written into `CLAUDE.md` §18.0–18.4 and [[Vendor Registration]]; gated by the new flag `registration.self_service`. | The founder specified the flow and asked whether it was documented. **It was documented twice, incompatibly, and built to neither reading.** [[Admin Operations Console]] Decision 3 said *"an applicant downloads the app, submits shop details, and receives a reference number"* and was marked accepted. D113(b) said *"no self-service registration is exposed in the Android app, the POS, the console or the API"* and was also accepted. `admin/applicationIntake.ts` followed D113(b), so `POST /applications` sat behind `ADMIN_REVIEW_APPLICATION` and only an admin could type an application in. `CLAUDE.md` described **no registration flow at all**. Two accepted decisions cancelling each other, with the authoritative file silent, is how a build ends up matching neither. **Resolved in favour of Decision 3**, which is what the founder described. **What was built:** migration 018 (`submitted_via`, and a `registration_attempts` throttle table); a shared `recordApplication` in `@telga/persistence` that **both** the console and the app now write through, because two hand-written copies of an intake write is how the two drift; `GET`/`POST /register` on the merchant server, which is what the app actually reaches — the console is staff-only and no shop can get to it. **What is deliberately narrower than it could be:** the public form takes document *numbers*, never photographs, because an unauthenticated upload endpoint is a way to put arbitrary bytes on Telga's volume and a person must see the originals before approval anyway. **`submitted_via` is about evidence, not provenance:** until now a reviewer could assume somebody had seen the papers, and that assumption stops being true the moment the internet can write to the table. It defaults to `ADMIN`, not `SELF_SERVICE` — defaulting the other way sounds safer and would retroactively relabel verified applications as unverified, making the flag meaningless on the rows where it is most trustworthy. **A real hole was found and closed on the way:** suspending a merchant wrote `merchants.status` and **nothing read it at sign-in**. `createSale` refused, so a suspended shop could not sell — but its operators could still sign in and hold live sessions. `signIn` now refuses `MERCHANT_NOT_ACTIVE` and `authenticate` revokes a live session on the next request, matching what the device-stop route already did one level down. **What is NOT opened:** device-initiated provisioning. An application grants nothing until a human approves it; a device key authenticates every later request. A self-provisioning device would carry a secret shipped in the APK, and an APK is a public file. **What this accelerates, and the decision it therefore brings forward:** a second real shop can now arrive without anybody at Telga typing anything, and D113(a) says the shared-database deferral *"stops being defensible the moment a second real shop exists"*. The shared-versus-per-tenant storage question (D103) needs a founder decision **before** the second shop is onboarded, not after. See [[Platform Shape]]. | accepted |

## Proposed

Not yet accepted. Recorded so the reasoning is visible before a founder decision, not after.

| # | Date | Decision | Rationale | Recorded by |
|---|---|---|---|---|
| D59 | 2026-08-24 | **Recommend a small VPS or a local/office machine** (category 1 or 4 of five evaluated) as the training deployment target — no vendor named, no account created, nothing purchased | Both categories fit [[Training Deployment Architecture]]'s persistent-host requirement directly, at low or no incremental cost, without the "ephemeral unless configured correctly" risk found in managed-container and PaaS categories. Category 5 (managed database + separate host) is the more credible eventual production path but is a distinct engineering project, not justified before a provider agreement and real load exist. Full comparison: [[Deployment Target Evaluation]]. Recorded as A60. | Claude Code |
| D117 | 2026-09-08 | **Recommendation: build merchant funding — deposit submission and operator verification — as the next project, in TRAINING mode, on the running Railway service. Move the training deposit screen out of `/pay` first. Do not adopt Vercel or Supabase, do not build a second front end, and leave `card.simulated` off.** Full reasoning in [[Balance Top-Up and Funding]]. | The founder asked where a merchant's balance comes from once it runs out, and proposed Telga Pay activation, a Vercel front end, Supabase, and an admin panel as the answer. **Telga Pay is not the answer and cannot be made into it:** `ui/telgaPay.ts` states in its first paragraph that every screen is UI-only and reaches neither database, provider nor ledger, and no card number is ever collected. Turning `card.simulated` on restores a drawing of a terminal, not a funding rail. A card surface that genuinely took money would be `payments.acceptance` — an acquirer and a licence, CLAUDE.md §8. **The real answer already exists on paper:** [[Funding Verification]] carries the whole state machine — bank deposit, operator verification against the bank record, `MATCHED`, `CREDITED`, second approval for high value, daily reconciliation by a separate reviewer — under §20's absolute rules of no overdraft, no personal account, no screenshot-only credit. The schema exists; the workflow is unwritten. **Why it is the correct next project:** it is **launch gate 6**, and of the ten gates only two are engineering work rather than legal or commercial paperwork — gate 6 and gate 10, which already has evidence (R31). It is also the only proposal on the founder's list that clears any gate at all. Its shape is identical in training and live, so nothing built is discarded; and `fundMerchant`, the append-only ledger, the audit event and the console's approval screens already exist, making it wiring rather than invention. **What was rejected, and why. (a) Vercel:** [[Vercel Deployment Limits]] is accepted and unchanged — the POS and console are Node HTTP servers with database-backed sessions, there is no web root to publish, the worker has nowhere to live, and the trusted-proxy model cannot be expressed for an unenumerable ingress without the trust-all range D45 forbids. **(b) Supabase:** this is D108 — `LedgerDriver` is entirely synchronous with zero Promise-returning methods and a synchronous `transaction<T>()` callback, so the swap is an async refactor across ~80 files and 21 transaction call sites, plus re-proving the claim lease (A37/R16) on a new engine, plus resolving D103. It changes no merchant-visible behaviour and clears no gate, and SQLite on the Railway volume is now *proven* — persistence survived a redeploy and an off-platform restore verified by checksum. **(c) A new admin panel:** one already exists. `apps/operations-console` serves login, MFA, step-up authentication, applications, merchants, devices, tenants, admins and audit, and is loopback/TLS-only by design. It is **not** started by `railway-start.mjs`, so the real work is deciding how to reach it safely — which the verifier queue in gate 6 needs anyway. **(d) `card.simulated`:** left off. Re-enabling is cosmetic, is a reversal of D112, and should be decided on its own merits rather than bundled into a funding project. **What this recommendation does not do:** it does not clear gate 6, which additionally needs an approved funds structure (P6) and a tested reconciliation; and it authorises no bank account, since none is approved. **Proposed by Claude Code. Not approved. No code has been changed.** | Claude Code |
| D118 | 2026-09-08 | **Recommendation: keep D103's per-shop SQLite model, wire the three missing links in the onboarding chain, and deploy the existing operations console over TLS. Do not rebuild the console on Vercel and do not adopt Supabase.** Full reasoning in [[Multi-Shop Onboarding]]. | The founder asked for an admin panel that watches every shop's device activity, with sign-in parameters auto-generated after documents are supplied and each device holding its own data — and reported, correctly, that the console has never been tested for this. **The cause was found and it is not the technology.** `services/api/src/index.ts` exports `admin/tenants`, `admin/applications` and `admin/enrollment` but **not** `admin/provisioning` and **not** `admin/tenantRouting` — the two modules that create a shop and route it to its own database are not exported, so the console cannot import them. The wiring was never there to test. **What already exists is more than expected:** the console's own sign-in with MFA and step-up re-authentication; application review with pick-up, approve, reject and return; merchants, devices, tenants, admins and audit screens; an auto-generated device activation code that is hashed at rest and displayed once (`POST /devices/:id/enrollment-token`); the full lifecycle state machine in `admin/applications.ts` running `SUBMITTED → UNDER_REVIEW → APPROVED → PROVISIONING → DEVICE_PENDING → READY_FOR_TRAINING → ACTIVE_TRAINING → LIVE_ELIGIBLE`; a five-step crash-safe `provisionMerchant()`; and a tenant router that re-validates the derived name, the tenant status and the schema version on every open. **Three links are missing:** nothing in the source inserts into `merchant_applications`, so no shop can apply; `/applications/:id/decide` sets `APPROVED` and stops without calling `provisionMerchant()`; and `tenantRouting.ts` has no callers, so every merchant still shares one file. **The decision this forces.** D113 deferred D103 and named the condition that ends the deferral — *"it stops being defensible the moment a second real shop exists"* — and a second shop is now proposed, so the condition has arrived. **Supabase is rejected here for a sharper reason than in D108:** `provisioning.ts` and `tenantRouting.ts` already implement per-shop databases, so adopting Postgres would not add to that work but delete it and reopen a decision the founder took after a full comparison, at the cost of the async refactor across ~80 files. Per-shop files also give *stronger* isolation — "the other shop's rows are not in the file" rather than "every query remembered its `WHERE merchant_id = ?`" — which is the opposite of what the proposal assumes. Postgres remains the right answer once cross-shop reporting or N-backup cost provides evidence for it. **Vercel is rejected because it solves nothing here:** the console is unreachable by deliberate design — `cli.ts` refuses anything but loopback over HTTP because a console session can suspend merchants and create administrators — so the fix is TLS and an address, not a rewrite of login, MFA, step-up, CSRF, RBAC and audit, all of which are covered by fourteen HTTP-level tests. **A separate security decision is required:** whether the console faces the public internet at all. **What must not be claimed:** that multi-shop onboarding works, that per-shop isolation is active, that approval creates a shop, or that any real merchant may be onboarded — real shops mean real money and all ten gates. **Proposed by Claude Code. Not approved. No code has been changed.** | Claude Code |

## Pending — required from MuleSoo

| # | Decision | Blocks | Tracked in |
|---|---|---|---|
| P1 | **Confirm legal entity ownership, founder equity, signing authority, finance approvals, and operational accountability** | Provider signature, funding approval, both `money.live` keys, gates 8 and 9 | [[Founders and Roles]] |
| P2 | First airtime provider | Gates 2 and 5; adapter specifics | the provider assessment (commercial material, kept outside this repository) |
| P3 | Commission rates | Merchant economics, `CommissionRule` values | the provider agreement terms (commercial material, kept outside this repository) |
| P4 | Prices — platform fee, transaction fee, hardware | Merchant agreement, gate 4 | the pilot budget (commercial material, kept outside this repository) |
| P5 | Pilot budget | Gate 9 | the pilot budget (commercial material, kept outside this repository) |
| P6 | Banking and merchant-funds structure | Gates 3 and 6 | [[Legal Questions]] |
| P7 | Legal and payment authorization | Gates 1 and 4 | [[Legal Questions]] |
| P8 | Named Amharic reviewer and sign-off | Any production build with Amharic strings | [[Amharic Strings]] |
| P9 | Pilot area selection | the commercial pilot plan (commercial material, kept outside this repository) | strategic material maintained outside the source repository |
| P10 | Hardware and thermal-paper specification | Merchant onboarding terms | [[Merchant Onboarding]] |
| P11 | Merchant offboarding terms | Contract completeness | [[Merchant Onboarding]] |
| P12 | Brand colour values | [[Design System]] tokens are assumptions until then | [[Design System]] |
| P13 | Confirm "Kazan" refers to Kazang | Any external document naming it | strategic material maintained outside the source repository |

## Superseded

*None.*

## Rejected

*None.*

## How to add a decision

1. Copy [[Decision]] into the relevant folder, or add a row here for a small one.
2. State the decision, the alternatives considered, and the rationale.
3. Set `decision_status` in the affected notes' frontmatter.
4. Link the decision from the notes it changes.

## Related

- [[Launch Gates]]
- [[Founders and Roles]]
- [[Decision]]

---
Back to [[00 Home]]
