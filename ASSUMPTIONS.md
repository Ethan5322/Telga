# ASSUMPTIONS

Every belief Telga is built on that has **not** been verified. Mirrors
the pilot assumptions register, which is commercial material kept outside this
repository; that note and this file must be updated
together.

An assumption is not a risk. A risk is something that might go wrong; an assumption is something
we have decided to treat as true without evidence. Each one below names how it will be tested.

**Owner: NOT YET ASSIGNED** on every row — no role in
`docs/obsidian/07 Governance/Founders and Roles.md` has been filled.

**Status legend:** `OPEN` — unverified · `ACCEPTED` — a decision was taken on it ·
`CONFIRMED` — evidence exists.

---

## Market

### A1 — Shops run several disconnected vending tools and find it a problem
- **Confidence:** Medium
- **Impact:** Business. The entire product thesis. If merchants are content with their current tools, Telga has no wedge.
- **How it will be tested:** `99 Templates/Merchant Interview.md`, recorded outside this repository
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A2 — Merchants will switch for reliability rather than commission
- **Confidence:** Medium
- **Impact:** Business. Determines whether Telga can win without out-bidding incumbents on rate.
- **How it will be tested:** Pilot retention over three months
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A3 — The Flash/Kazang reseller model transfers to Ethiopia
- **Confidence:** Medium
- **Impact:** Business. The reference model behind the whole design.
- **How it will be tested:** Phase 4 commercial pilot
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A4 — A single dependable workflow is worth a monthly fee
- **Confidence:** **Low**
- **Impact:** Business. One of three revenue streams; if false, the model rests on commission alone.
- **How it will be tested:** Pilot economics
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A5 — Merchants will accept a training-mode day before live selling
- **Confidence:** Medium
- **Impact:** Operational. Training mode is the main defence against merchant error on day one.
- **How it will be tested:** Onboarding of the first pilot merchants
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

## Operational

### A6 — Connectivity is good enough for pending states to resolve in minutes
- **Confidence:** **Low**
- **Impact:** Technical and operational. If false, most sales land in under review and support drowns.
- **How it will be tested:** Merchant baseline assessment (recorded outside this repository), then Phase 3
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A7 — A 5-minute automatic pending maximum suits real provider behaviour
- **Confidence:** **Low**
- **Impact:** Technical. Sets when value moves to the under-review bucket. Too short floods the queue; too long strands merchant funds.
- **How it will be tested:** Provider agreement terms (recorded outside this repository), then Phase 3
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN — currently an **assumed default**, configurable in code

### A8 — Merchants can source compatible thermal paper themselves
- **Confidence:** Medium
- **Impact:** Operational. Paper shortage must never become a transaction failure.
- **How it will be tested:** Onboarding
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A9 — Same-day field support is achievable in a compact area
- **Confidence:** Medium
- **Impact:** Operational. A disqualifier in the merchant scorecard depends on it.
- **How it will be tested:** Phase 4 pilot
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A10 — Support can answer a complaint finally within 24 hours
- **Confidence:** **Low**
- **Impact:** Operational and reputational. The commitment in `05 Operations/Support and Disputes.md`.
- **How it will be tested:** Pilot resolution-time metric
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

## Technical

### A11 — The first provider will offer status lookup by reference
- **Confidence:** Medium
- **Impact:** Technical. **Hard gate.** Without it a pending transaction can never resolve, and every silence becomes a human case.
- **How it will be tested:** Provider assessment (recorded outside this repository)
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A12 — The provider will support a client-supplied reference or idempotency key
- **Confidence:** Medium
- **Impact:** Technical. Without it, provider-side duplicate protection is unavailable and Telga carries the whole risk.
- **How it will be tested:** Provider agreement terms (recorded outside this repository)
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

### A13 — SQLite is sufficient for the Phase 2 prototype ledger
- **Confidence:** High
- **Impact:** Technical, reversible. Kept behind a driver interface, so Postgres is a driver swap.
- **How it will be tested:** Phase 3 load
- **Owner:** NOT YET ASSIGNED
- **Status:** ACCEPTED — Decision Log D4

### A14 — A web POS/PWA is sufficient to validate the domain before native Android
- **Confidence:** High
- **Impact:** Technical, reversible. Native Android is deferred, not cancelled.
- **How it will be tested:** Phase 3
- **Owner:** NOT YET ASSIGNED
- **Status:** ACCEPTED — Decision Log D5

### A15 — Thermal printing can be abstracted behind one port across device types
- **Confidence:** Medium
- **Impact:** Technical. Determines how much of the POS layer is device-specific.
- **How it will be tested:** Hardware selection, then Phase 3
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN

## Commercial

Commercial assumptions — revenue policy, pricing, hardware financing and the
merchant agreement — are maintained outside this repository and remain
**NOT YET CONFIRMED**. They are business decisions, not build inputs: nothing in
the source, tests, build, CI or runtime depends on any of them, and
`packages/domain/src/commission.ts` throws `CommissionRateNotConfiguredError`
rather than return a plausible default.

| # | Assumption | Status |
|---|---|---|
| A16-A18 | Commercial assumptions, recorded outside this repository | **OPEN** — owner NOT YET ASSIGNED |

## Documentation

### A19 — Reconstructed note names match founder intent where the source PDF was clipped
- **Confidence:** High
- **Impact:** Documentation. Eight folder lines in `CLAUDE.pdf` were cut mid-word.
- **How it will be tested:** Founder confirmation — given 2026-08-19
- **Owner:** Founder
- **Status:** CONFIRMED — see `05 Operations/Source Specification Clipped In PDF.md`

### A20 — Draft Amharic strings are usable after native review
- **Confidence:** Medium
- **Impact:** Product and financial. A mistranslated "do not retry yet" causes duplicate sales and real loss.
- **How it will be tested:** Named native Amharic reviewer, priority on the pending strings
- **Owner:** NOT YET ASSIGNED
- **Status:** OPEN — **blocks any production build containing Amharic**

### A21 — "Kazan" in the founder brief means Kazang
- **Confidence:** High
- **Impact:** Reputational. Affects any external document naming the reference model.
- **How it will be tested:** Founder confirmation
- **Owner:** Founder
- **Status:** OPEN

---

## Assumptions introduced by the domain implementation

Recorded here because they were taken during implementation on 2026-08-20 and are all reversible.
Each is also a row in `07 Governance/Decision Log.md`.

| ID | Assumption | Confidence | Impact | How tested | Status |
|---|---|---|---|---|---|
| A22 | A `BANK_CLEARING` contra account is acceptable alongside the five segregated buckets | High | Technical. Without a contra account a funding credit cannot balance. Holds no merchant value. | `tests/domain/ledger.test.ts` — "bank clearing holds no merchant value" | ACCEPTED — D8 |
| A23 | `PENDING → REVERSAL_REQUIRED` is legal without passing through `UNDER_REVIEW` | Medium | Technical. A provider callback can confirm value taken and not delivered with no human step. | `tests/domain/states.test.ts` | ACCEPTED — D9 |
| A24 | `RESERVED → PROCESSING` is legal without passing through `SUBMITTED` | High | Technical. Adapters that do not acknowledge separately need the direct edge. | `tests/domain/states.test.ts` | ACCEPTED — D10 |
| A25 | The idempotency key derives from request identity, not payload contents | High | Technical and financial. If the key hashed the payload, a payload mismatch could never be detected — a tampered request would look like a new sale. | `tests/domain/idempotency.test.ts` | ACCEPTED — D11 |
| A26 | The three balance buckets are real ledger accounts, not derived figures | High | Technical. Makes every bucket movement an auditable posting; the four views become simple sums. | `tests/persistence/reservation-balance.test.ts` | ACCEPTED — D12 |
| A27 | `synchronous = FULL` is the right durability trade for a merchant ledger | Medium | Technical. Slower writes; no loss of recent commits on power failure. Revisit if write throughput becomes a pilot constraint. | `synchronous is FULL — this is a ledger, not a cache` | ACCEPTED — D13 |
| A28 | Forward-fix-only migrations are acceptable in production | Medium | Operational. A bad schema change needs a new migration or a restore, and **restore is not yet tested** (launch gate 10). | Restore test, once written | ACCEPTED — D14 |
| A29 | A mask plus a salted hash is sufficient recipient data for support | Medium | Operational and legal. If support cannot resolve cases without the full number, this must be revisited with legal advice on retention (L14, L15). | Pilot support cases | OPEN — D15 |
| A30 | Single-writer migration at startup is sufficient | Low | Technical. Two processes migrating the same file concurrently is **untested**. | Not yet tested | OPEN |
| A31 | A transaction stuck at `PROCESSING` will be recovered by an operator | — | Operational and financial. | Superseded by the recovery sweep | **RESOLVED 2026-08-20** — `recoverInFlight` recovers `PROCESSING`, `RESERVED` and `PENDING` automatically. 61 tests. |
| A34 | The recovery thresholds chosen for the pilot are right | **Low** | Operational. Too short escalates healthy transactions into the review queue; too long strands merchant money. All six settings are NOT YET CONFIRMED. | Phase 3 trial | OPEN — see `Recovery Configuration` |
| A35 | A time-bounded claim lease is long enough for a recovery but short enough to reclaim | Medium | Technical. Too short lets two workers overlap; too long strands a transaction behind a dead worker. | `an expired lease can be reclaimed by another worker` | ACCEPTED — D23 |
| A36 | Classifying a lookup failure by error name and code is reliable enough | Medium | Technical. Reads `name` and `code` only, never the message, so a provider body cannot leak into a decision. An unrecognised failure falls back to `UNKNOWN`, which holds funds. | `classifies %s / %s as %s` | ACCEPTED |
| A37 | Multi-process worker safety | — | Operational. Two workers must never recover the same transaction. | 16 child-process tests spawning the compiled worker | **RESOLVED 2026-08-20** — real operating-system processes, real claim contention |
| A42 | CommonJS output is an acceptable runtime target | High | Technical, reversible. Chosen over rewriting ~60 imports; ESM remains available later by adding `.js` extensions. | `runs without TypeScript stripping — no loader flags are passed` | ACCEPTED — D30 |
| A43 | Building on the deployment target is acceptable | Medium | Operational. CI **executed on a real remote runner** and reached a complete green result on the third push. First push (`b5d96c3`) failed on A57. Second push (`1d06517`) failed on A58. Third push (`d540200`) passed every job. | `verify (Node 22.x)` PASS, `verify (Node 24.x)` PASS, `recovery stress` PASS — <https://github.com/Ethan5322/Telga/actions/runs/32718640781>, 1m43s | **RESOLVED 2026-08-24** — CI verified green on the remote runner |
| A44 | The test suite is deterministic under load | **Low** | Technical. Three intermittent failures observed on 2026-08-20 under full-suite load. Two were reproduced, diagnosed as test defects and fixed (A45, A47); the original — `marks manual review open on the pending row` — was **never reproduced** across a 200-iteration soak, 5 shuffled repeats and repeated isolated and full runs. **A58 is a separate, distinct defect** (a stress-*script* file-scope bug misreporting an unrelated A54 failure as `soak-200`) discovered while chasing a real CI failure on 2026-08-24 — it does not reproduce, explain, or close this assumption. | `npm run test:recovery:stress`; diagnostics now attached to the assertion | **OPEN** — not reproduced, deliberately not closed. See A58 for the unrelated defect found alongside it |
| A45 | The child-process race assertion described the real invariant | — | Technical. It asserted `claimed === 1`. A second process may legitimately claim after the winner releases, then find the transaction terminal on re-read. The safety property is one *resolution*, not one *claim*. | `exactly one claims it; the other records a conflict` | **RESOLVED 2026-08-20** — test defect, not a product defect |
| A46 | Refusing to start on an unmigrated database is sufficient protection for A30 | Medium | Operational. It removes migrate-on-open, so only an explicit `--migrate` migrates. It does not make concurrent migration safe — see [[Multi-Process Migration Plan]]. | `refuses to start when migrations have not been applied` | ACCEPTED — D33 |
| A47 | Child-process tests are independent of the wall clock | — | Technical. Two real-time dependencies found: a 1.5-second lease raced against process startup, and seeded rows carrying a fixed fake timestamp while children read the real clock. Both fixed without adding delay; the second also removes a CI portability hazard. | `a worker that dies holding a lease blocks others only until the lease expires` | **RESOLVED 2026-08-20** — test defect, not a product defect |
| A48 | A component-level UI test is sufficient assurance for the training POS | Medium | Technical. The screens are pure functions returning an element tree; tests assert the tree, not a rendered page. This does not exercise CSS, real focus behaviour, or a screen reader. `mount()` builds real DOM from the same tree, so a browser test can be added without rewriting the screens. | `tests/ui/screens.test.ts` | **OPEN** — accepted for training, revisit before a merchant uses it |
| A49 | The POS may take the merchant id from the URL because it is training-only | **High** | Security. **Superseded.** The merchant now comes from a server-side session; a client-supplied merchant id is a consistency check and is refused on mismatch. Authentication, device binding, CSRF, lockout and rate limits are implemented and tested. | `tests/auth/` — 126 tests | **RESOLVED 2026-08-21** for controlled internal training. See R22, and A52/A53 for what remains |
| A52 | A browser-supplied device id plus a server-issued key is adequate device identity | Medium | Security. It raises impersonation from *know the id* to *hold the key*, and enrolment, revocation, expiry and merchant assignment are enforced on **every** request. It is **not hardware attestation**: a copied id and key on another machine are indistinguishable from the original. | `tests/auth/device-binding.test.ts`, including a test that demonstrates the limitation | **OPEN — training-grade by design.** See [[Device Binding]] |
| A53 | A self-signed certificate is adequate transport security for controlled training | Medium | Security. **Reduced, not closed.** `TRAINING_HTTPS` now serves real TLS, so a passive listener no longer reads a session token off the wire, and cookies are `Secure`. A self-signed certificate is **not production trust**: it does nothing against an active attacker substituting their own, because nothing distinguishes theirs from ours. Plain HTTP remains available but is refused on any non-loopback binding. | `tests/transport/` — 74 tests including a real TLS listener; `npm run training:smoke` | **OPEN — reduced.** Needs a CA-signed certificate or a documented proxy for anything beyond the controlled machine. See [[Training HTTPS Deployment]] |
| A54 | The child-process tests are reliable under full-suite load | — | Technical. **Root cause found: a product defect.** `SqliteLedgerDriver.transaction()` used a **deferred** transaction, which begins as a reader and upgrades on first write; in WAL mode a concurrent writer makes that upgrade fail with `SQLITE_BUSY_SNAPSHOT`, which `busy_timeout` does **not** wait out. Two worker processes racing one transaction is exactly that condition. Fixed with `BEGIN IMMEDIATE`. No retry wrapper added. | `npm run test:child-process:stress` — reproduced on iteration 1 before the fix, **200 iterations clean** after | **RESOLVED 2026-08-21.** Closes neither A44 nor A51 |
| A55 | Test and build commands may run concurrently | — | Technical. They may not, on this constrained two-core environment. A full suite run that overlapped `npm run build:clean` failed 4 tests whose output was truncated; isolated runs of the same suite passed. | Observed 2026-08-21 | **RESOLVED as a working rule 2026-08-21** — never run `npm test` and a build at the same time here |
| A57 | The publication boundary (`.gitignore`, `check-committed.mjs`) covers every real source directory | High | **Resolved: a genuine tracking defect.** `.gitignore`'s bare, unanchored `build/` pattern — intended for generated compiler output — also matched `tests/build/`, a real source directory. That directory was never tracked, staged, or committed, though it existed on disk and was referenced by both CI and the README. Local runs always passed because Vitest reads the filesystem directly, not git state. Fixed by removing the pattern and adding `scripts/check-ci-test-paths.mjs`, which now fails CI fast if any workflow-referenced test path has zero tracked files. | Reproduced via `git archive HEAD` (directory absent from the committed tree); fixed and verified remotely green on `d540200`, run `32718640781` | **RESOLVED 2026-08-24** — verified on the remote runner |
| A58 | `stress-recovery.mjs`'s soak pass runs only the intended scenario | High | **Resolved: a stress-script harness defect, not a product or A44 defect.** The soak pass invoked `vitest --config vitest.stress.config.ts` with no file argument; that config's glob matches both the intended A44 scenario and the unrelated A54 multi-process test, which needs a compiled worker binary the `recovery stress` CI job never builds. Every remote run silently also ran the A54 scenario against a missing build and reported its failure as `soak-200`. Reproduced locally with the build removed: `Test Files 1 failed \| 1 passed (2)`, failure entirely inside `child-process.stress.test.ts`; the real A44 soak passed. Fixed by scoping the soak invocation to its own file, adding `--reporter=dot` (the existing A51 mitigation) to both passes, and classifying failures as ASSERTION / INFRASTRUCTURE / HARNESS. | Reproduced with the build removed; fixed and verified remotely green on `d540200`, run `32718640781` — `soak-200` and all 5 shuffled repeats passed | **RESOLVED 2026-08-24** — does not close A44, which tracks a separate, still-unreproduced failure shape |
| A59 | The current `actions/checkout@v4` and `actions/setup-node@v4` versions are the right ones to keep | Low | **Resolved: low-risk maintenance, not a product or recovery fix.** The `d540200` remote run printed three deprecation warnings — both actions target the Node 20 runtime, which GitHub now forces onto Node 24. Verified each action's actual `action.yml` metadata at `v4` (`node20`) and `v5` (`node24`) directly, rather than trusting a summarized changelog. Upgraded `actions/checkout@v4→v5` and `actions/setup-node@v4→v5` in its own commit (`96b3d4e`), separate from any functional CI fix. Node matrix unchanged: `22.x` and `24.x`. | GitHub Actions run `32730213755` — Success, 1m41s, all jobs green; Annotations panel confirmed empty by direct owner inspection of the authenticated page | **RESOLVED 2026-08-24** |
| A60 | A small VPS or a local/office machine is the right training deployment category | Medium | Operational. [[Deployment Target Evaluation]] compared five categories against [[Training Deployment Architecture]] and shortlisted category 1 (small VPS) or category 4 (local/office machine). **2026-08-25 — the project owner confirmed the deciding facts** (reliable office computer, reliable office internet, can run Telga continuously, training-only use, no remote public access needed) and selected **local/office machine**, recorded in [[Decision Log]] D65 and detailed in [[Persistent Host Deployment Plan]]. No vendor selected, no account created, nothing purchased or deployed — a VPS was never provisioned either. | [[Deployment Target Evaluation]], [[Persistent Host Deployment Plan]], [[Decision Log]] D65 | **OPEN — local/office host category selected for controlled training; detailed host hardening, backup, restore, HTTPS, supervision, and real-infrastructure evidence remain open** |
| A61 | Backup and restore can be safely designed before being implemented | Low | **Resolved as a design question — now implemented.** `@telga/backup` implements the checkpoint-before-copy sequence, isolated-restore procedure, checksum verification, and the session/claim policies from [[Backup and Restore Runbook]]. Tested: 27 cases in `tests/backup/` against real SQLite files plus 5 against the real compiled binary. **Not the same as launch-gate completeness** — see the backup/restore implementation note (implemented separately) "What remains open": no real host, no real schedule, no measured time-to-restore, no full worker-sweep-after-restore integration test. | `tests/backup/backup-restore.test.ts`, `tests/build/backup-restore-cli.test.ts` | **RESOLVED 2026-08-24 as a design/implementation question. Launch gate 10 and A34/A41 remain OPEN** |
| A62 | No HTTP health-check endpoint blocks real host monitoring | Medium | **Resolved.** `GET /api/health/live` and `GET /api/health/ready` are implemented, reusing the existing `recoveryGauges`/`evaluateAlerts` rather than a second definition of recovery health. 17 tests, including that neither route can mutate state and readiness correctly reports `DEGRADED`/`UNHEALTHY`/`NOT_READY` for real injected failures (non-zero residual, missing migration, stuck transaction). | `tests/ui/health.test.ts`; see the health endpoints note (implemented separately) | **RESOLVED 2026-08-24** |
| A56 | A Vercel deployment of this repository is a running Telga | **High** | **Deployment-blocking.** It is not. Telga is stateful and long-running — a local SQLite ledger in WAL mode, a supervised recovery worker, server-side sessions, single-writer migrations — and Vercel is stateless and ephemeral. Two concurrent invocations would hold **different** ledger files, which defeats the claim lease that A37/R16 rest on. The trusted-proxy model also cannot be configured, because Vercel's proxy addresses are not enumerable and there is deliberately no trust-all option (D45). The repository contains no `api/`, no `public/`, no framework config and no `vercel.json`, so a deployment is a build artifact at most. A GitHub push must not be treated as a production deployment; if a Vercel project is connected, its automatic production deployments should be paused until a compatible target exists. | Inspection of the repository and the runtime shape; see [[Vercel Deployment Limits]] | **OPEN — deployment-blocking. A Vercel URL is training/preview only, never merchant or production** |
| A50 | Training denominations are not mistaken for prices | Low | Commercial. `TRAINING_CATALOG` uses 10/25/50/100 birr with every label suffixed "(simulated)". Real denominations and commission are NOT YET CONFIRMED. | `tests/ui/cli.test.ts` | ACCEPTED — labels asserted |
| A51 | The full suite passes on a small machine without being starved of CPU | Low | Technical. Two distinct effects, now separated. **(a) Build contention** — resolved as a rule: `test:child-process:stress` refuses to run against stale output and never builds (A55/D50). **(b) Reporter starvation** — a long-running task can outlast vitest's `onTaskUpdate` RPC on two cores, failing a run in which every assertion passed. Mitigated with a low-resource reporter, and the stress command now **classifies** the two: exit 1 a test failed, exit 3 the harness did. | `npm run test:child-process:stress` — **3 consecutive clean runs**, 600 iterations plus 9 real-file runs, after 1 reporter-timeout run before the reporter change | **OPEN — mitigated, not eliminated.** The reporter limit is a property of this machine; it can recur under load and is now unmistakable when it does |
| A38 | Fixed-delay scheduling is the right trade against a fixed rate | High | Technical. Fixed rate degenerates into continuous execution when a sweep outlasts its interval. | `a slow sweep does not cause a runaway loop` | ACCEPTED — D25 |
| A39 | Database and schema failures should stop the worker rather than back it off | Medium | Operational. Stopping surfaces the fault; retrying hides it behind a growing delay. The cost is that recovery halts until someone looks. | `stops the worker on a database failure rather than retrying into it` | ACCEPTED — D26 |
| A40 | A worker's own claims are safe to release on shutdown | High | Technical. Scoped to `worker_id`, so another worker's live claim is never touched. | `a worker releases only its own claims on shutdown` | ACCEPTED — D29 |
| A41 | The health thresholds distinguish degraded from unhealthy usefully | Low | Operational. Untested against real operational load; the boundaries are a guess until a pilot produces data. | Phase 3 trial | OPEN |

> **How A37 was closed.** The build step it was waiting on now exists. `tests/build/` spawns the
> compiled worker as separate operating-system processes — asserting the child `pid` differs from
> the runner's — and races them for one claim. One winner, one recorded conflict, one settlement,
> residual zero.
| A32 | A 5-minute pending maximum is short enough that merchants tolerate held funds, and long enough that the provider usually answers first | Low | Operational. Too short floods the under-review queue; too long strands merchant money. Same underlying unknown as A7. | Phase 3 trial | OPEN |
| A33 | Provider-side callback de-duplication is not required, because Telga's own guards suffice | Medium | Technical. A repeat is refused at four independent points, so a duplicate callback is harmless. | `a repeated callback does not finalize twice` | ACCEPTED |

| A63 | The 4% training profit rate is a rate Telga can quote | **High** | **Commercial. It is not.** 4% is a training default chosen so the profit model can be exercised end to end; it is not a negotiated commission, not a provider term, and not a production price. Provider commission remains NOT YET CONFIRMED (CLAUDE.md §19), `packages/domain/src/commission.ts` still throws rather than returning a rate, and every profit ledger entry records `training-profit-<bps>bps` so it can never be mistaken for a real one. The settings screen labels the field as training-only in both languages. | [[Decision Log]] D69, [[Top Up Slips and Settings]], `tests/application/settings-and-profit.test.ts` | **OPEN — must not be quoted to a merchant, a provider, or in any public claim** |
| A64 | Crediting a simulated training float from a screen called Telga Pay is not regulated payment acceptance | **High** | **Legal/product.** Assessed as outside the CLAUDE.md §2 gate because no card is read, no PAN exists, no processor or partner is contacted, nothing leaves the machine, and the driver and schema both refuse any mode but TRAINING — it is a training fixture with a ledger behind it, not a cash-in. Recorded rather than left implicit precisely because a future reader seeing ledger credits originating from a Pay screen is entitled to the reasoning. **Not a legal opinion**: no qualified Ethiopian review has been obtained, and CLAUDE.md §8 still forbids claiming compliance without one. | [[Decision Log]] D70, `services/api/src/application/deposits.ts`, `tests/application/topup-and-deposit.test.ts` | **OPEN — training only. Any move toward real deposits requires the §8 legal gate first** |
| A65 | Storing a customer phone number on a top-up order is acceptable data minimization | Medium | **Privacy.** The number is required for a direct top-up — there is nowhere else to send the airtime — and is masked at the boundary before it reaches `pending_orders.recipient`, so no full number is ever stored, exactly as `transactions.recipient_masked` has always worked. This reverses the earlier instruction not to ask for a customer number, which was written when the only flow was a counter voucher that needed none. Retention and deletion policy for masked recipients is unwritten. | `tests/application/topup-and-deposit.test.ts` (asserts the full number is absent from the stored row), [[Top Up Slips and Settings]] | **OPEN — retention and deletion policy still unwritten (CLAUDE.md §24)** |
| A66 | The data bundle volumes, validities and prices are usable figures | **High** | **Commercial. They are not.** Every bundle in `TRAINING_DATA_BUNDLES` is a training value invented to exercise the flow — 1GB/30 days at 125 birr and the rest are **not** researched market rates, not provider terms, and not prices Telga may quote. Real data products, their costs and any commission on them remain NOT YET CONFIRMED, exactly as airtime commission does (CLAUDE.md §19, §31). | [[Decision Log]] D72, `apps/merchant-pos/src/cli.ts` | **OPEN — must not be quoted to a merchant, a provider, or in any public claim** |
| A67 | Printing a simulated redemption code on a slip is safe | Medium | **Product/privacy.** A slip that a merchant hands to a customer now carries a PIN, a token reference and a dial string, none of which will load anything. The mitigations are structural rather than procedural: every code is `TRAIN-` prefixed, the dial prefix `*000*` belongs to no operator, the notice `SIMULATED CODE — will not load airtime` is drawn by the slip card itself rather than passed in, and the general `TRAINING — NO REAL VALUE` banner is printed as well. **The residual risk is a customer who reads none of it** and believes they bought airtime — which is a training-operations question (who is handed these slips, and what are they told) rather than a code question. | [[Decision Log]] D73, `tests/ui/data-navigation-slipcodes.test.ts` | **OPEN — needs a training-operations rule about who receives a slip** |
| A68 | `transactions.product_type` recorded what was sold | Low | **Resolved: it did not.** `createSale` wrote the literal `AIRTIME` for every sale, so the column carried no information. The slip derives its Network line from the product id in that column, which therefore never rendered, and no redemption code could be derived either. Now stores `transaction.productId`. The column has no CHECK constraint and never had one (migration 001), so no schema change was needed and no existing row is invalid — older rows simply still read `AIRTIME`. | `tests/ui/data-navigation-slipcodes.test.ts`, full suite green | **RESOLVED 2026-08-27** |
| A69 | Remembering the device key on the client is an acceptable trade | **High** | **Security. It is a real reduction, made on an explicit founder instruction.** The device key now lives in an `httpOnly` cookie so that an idle timeout costs an operator their PIN alone (D74). Consequence: whoever holds the browser profile holds the device key, so the key is no longer a second factor beside the session — it is part of what the device is. What still holds: the **PIN is never stored anywhere**, the cookie is `httpOnly`/`SameSite=Strict`/`Secure`-when-available and never reaches `localStorage`, server-side device revocation overrides every cookie, and Settings sign-out clears it for a device changing hands. **Not yet assessed against a shared or public-counter device**, which is the case where it matters most. | [[Decision Log]] D74, `services/api/src/auth/cookies.ts` | **OPEN — revisit before any device is used by more than one person, and before live money** |
| A70 | The Telga mark on a slip is the supplied artwork | Low | **It is not.** The mark is a stylised vector redraw of the supplied composition, not a trace of the photoreal render, because a thermal head is one bit per dot and gradients print as a smear (D76). Colours are the founder-directed change away from gold: verdigris metal, cut-stone ground. If the exact render is wanted on screen it can be embedded as a transparent PNG data URI alongside, with the mono SVG still doing the printing. | [[Decision Log]] D76, `apps/merchant-pos/src/ui/logo.ts` | **OPEN — founder to confirm the redraw is an acceptable likeness** |
| A71 | A bulk print of 20 vouchers performs acceptably | Medium | **Untested at the top of the range.** Authorizing a batch creates one transaction per voucher, sequentially, each with its own reservation and postings — twenty sales inside one request. Verified correct for a batch of four (four transactions, four distinct codes, ledger residual zero); the wall-clock cost of twenty against a real provider adapter and a real printer queue is unmeasured. The 20 ceiling is a guess at a safe bound, not a measured one. | `tests/ui/` bulk-print coverage; Phase 3 trial | **OPEN — measure before a pilot; lower the ceiling if a batch outlives the request timeout** |
| A72 | The business details on a slip mean Telga checked them | **High** | **Legal/consumer. It did not.** Business name, address, phone, TIN and trade licence are typed by the owner in Settings and printed verbatim (D80). Telga performs no registration, licensing or tax verification of any kind, and the presence of a TIN box must never be read as one. The `TRAINING — NO REAL VALUE` banner still prints above all of it. **Unresolved:** whether printing a TIN or licence number on a training slip carries any obligation of its own under Ethiopian consumer or tax rules — that is a question for the legal review CLAUDE.md section 8 requires, not one the code can answer. | [[Decision Log]] D80, `services/api/src/application/settings.ts` | **OPEN — include in the legal review before any live use** |
| A73 | Moving profit to the selling balance is a payout | Medium | **It is not, and must never be described as one.** It moves a simulated profit figure into a simulated selling balance inside one merchant ledger (D78). No money leaves Telga, no bank is involved, and nothing settles. The wording on screen is "move to selling balance", never "withdraw", "pay out" or "settle" — those describe regulated activities this build is expressly forbidden from performing (CLAUDE.md sections 2 and 7). | [[Decision Log]] D78, `tests/application/profit-transfer-and-pin.test.ts` | **OPEN — keep the wording under review as real settlement is designed** |
| A74 | The support contact on the Help screen can be dialled | **High** | **It cannot.** `TRAINING_SUPPORT` in `apps/merchant-pos/src/ui/content.ts` holds marked placeholders — no Telga support desk is staffed, and the number is not a real line. Inventing a dialable number that somebody might call during a real dispute would be worse than a stand-in that says what it is. **Must be replaced with the real desk before any merchant uses this build in front of a customer.** | [[Decision Log]] D84 | **OPEN — blocks any merchant-facing use** |
| A75 | The customer list stores full phone numbers | Medium | **It stores masks.** A saved regular is a name beside `09******78` (D83), so the list recognises a customer but cannot dial or message one, and sending airtime to them still means typing the number. That is a deliberate trade: a list of full numbers on a counter machine would be the largest pool of personal data in the product, in a build with no data-protection review. **If the founder wants full numbers, that is a decision requiring the data-protection review CLAUDE.md section 24 calls for**, not a code change. | [[Decision Log]] D83, migration 012 | **OPEN — founder to decide, after review** |
| A76 | "Change password" in Settings changes something | **High** | **There is no password in Telga.** Sign-in is operator id + PIN + device key; no password exists anywhere in the schema, the auth code, or the login form. The founder brief asked for a "change password" button, and one has **not** been added because it would be a control that cannot do anything. Change PIN exists and works. **If a password is genuinely wanted, it is a new authentication factor** — a schema change, a login-flow change, and a security decision — not a button. | `services/api/src/auth/`, `packages/persistence/src/schema/types.ts` | **OPEN — founder to confirm whether a password is actually wanted** |
| A77 | The Telga mark is final artwork | Medium | **It is a reworked render, and it is reproducible rather than hand-finished.** `scripts/brand/build-logo.py` derives it from `gpt.png` every time it runs (D85), so the rock and bush scatter comes from a seeded generator, not from a designer placing stones. It is good enough to ship in a training build and to judge the direction from. **Before any public or printed use it should be reviewed by a designer**, and if the composition changes the reference render has to change with it — the script cannot move the letter relative to the figure, because they are one flat image. | [[Decision Log]] D85, `scripts/brand/build-logo.py` | **OPEN — designer review before public use** |
| A78 | The fall can be animated as three separate layers | Medium | **Not from a flat PNG.** The brief is a sequence: the letter falls, the figure catches it, the figure pushes it upright. Animating that properly needs the letter, the figure and the ground as **separate images**, so each can move independently. The reference is one flat render, so the current animation rotates the whole composition and rests it upright — the ground rotates with it, which is the visible compromise. **Supplying the artwork as three layers would remove the limitation entirely.** | [[Decision Log]] D85, `apps/merchant-pos/src/ui/document.ts` | **OPEN — needs layered source art** |
| A79 | The card flow means Telga can take real payments | **High** | **It cannot yet, and the gap is hardware and licensing, not code.** The flow, the screens and the decline handling are complete and tested (D87), but the two ports are served by simulators. Taking a real card needs: a certified EMV reader reached through the terminal vendor SDK, an acquirer contract, EMV Level 1/2/3 certification, and a PCI DSS assessment. **None exist.** `PAYMENT_ACCEPTANCE` is off and the build refuses to start with it on. What has been removed is the software risk — connecting real hardware is now implementing two interfaces. | [[Decision Log]] D87, D89, `packages/domain/src/cardPayment.ts` | **OPEN — blocked on hardware, acquirer and certification** |
| A80 | The simulated test cards resemble a real issuer response set | Medium | **They are a training set, not a certification suite.** Eight behaviours cover the cases a shop handles worst at a counter — no money, wrong PIN, expired, blocked, over limit, bank unavailable, no answer. A real acquirer returns dozens of response codes, and EMV certification tests hundreds of scenarios. The mapping from a real response code to the sentences in `DECLINE_MESSAGE_KEYS` **does not exist yet** and cannot until an acquirer is chosen. | `services/provider-adapters/mock-airtime/src/cardSimulator.ts` | **OPEN — needs the acquirer response-code map** |
| A81 | Printing "TELGA TRADING PLC" on a slip is safe today | **High** | **It asserts a registered legal form on a document that leaves the shop.** The name was set by the founder (D98) and is now on every printed slip and shop copy. CLAUDE.md §8 makes *"company authority documented"* a launch gate, and that gate is **OPEN** — no registration document has been shown to this repository. What keeps it honest in the meantime is the `TRAINING — NO REAL VALUE` banner printed above it and the fact that no real value moves. Before a real customer holds one of these, the registration must be verified. | [[Decision Log]] D98, `apps/merchant-pos/src/ui/screens.ts` | **OPEN — blocked on company registration evidence** |
| A82 | The four new dashboard tiles imply Telga will sell those services | Medium | **They are `COMING_SOON` and nothing behind them exists.** Internet, school fees, insurance and government fees were added (D100) only after a source confirmed each is still settled manually or through an agent in Ethiopia — they are a statement about the market, not a roadmap commitment. None has a provider agreement, a price, or a commission, and the rule at the top of `dashboard.ts` forbids any of them becoming `IMPLEMENTED` until a real route, backend, validation and result path exist. A merchant who reads a tile as a promise is a real risk and the badge is what answers it. | [[Decision Log]] D100, `apps/merchant-pos/src/ui/dashboard.ts` | **OPEN — needs provider agreements before any becomes real** |
| A83 | Migration 013 means attempts and provider health are now tracked | Medium | **The tables exist and nothing writes to them.** `transaction_attempts` and `provider_health_events` were created by migration 013 (D99) with their constraints tested, but no orchestration fills either one. So the questions they were built to answer — when did we ask the provider, what did it say each time, how long was it down — are **still unanswerable from the data**. Shipping the shape empty was the deliberate choice; wiring it is separate work. | [[Decision Log]] D99, `packages/persistence/src/migrations/013_attempts_and_provider_health.ts` | **OPEN — schema only, no writer yet** |
| A84 | The operator manual is bilingual like the rest of the product | Low | **It is English only.** `learningSteps()` accepts a locale and ignores it; every string in it is a literal rather than a `t()` key, so translating it is about forty sentences of real work, not a wiring change. CLAUDE.md §22 requires English and Amharic, and this screen does not meet it. An Amharic-speaking operator gets correct Amharic everywhere else and English here. | `apps/merchant-pos/src/ui/content.ts` | **OPEN — needs translation** |
| A85 | Migration 014 means the admin console exists | Medium | **Partly true now, and the half that is false matters.** The console **is** built: `apps/operations-console` serves `/login`, `/mfa`, `/step-up`, `/merchants`, `/devices`, `/applications`, `/tenants`, `/admins` and `/audit`, with `authenticateAdmin`, RFC 6238 TOTP and an admin audit trail. What migration 014 still does **not** deliver is the thing the assumption was written about: **there is no merchant self-registration or provisioning workflow.** `merchant_applications` is written by nothing (its only reference outside the schema is the migration test), and `provisionMerchant`/`merchantIdFor` in `services/api/src/admin/provisioning.ts` have **no callers anywhere**. A merchant still comes into being only through `provision()` on the command line. | [[Decision Log]] D102, D113 · [[Admin Operations Console]] | **OPEN — console built; registration and provisioning absent** |
| A86 | Per-shop databases are implemented | **High** | **The code exists and nothing calls it, so the effect is unchanged.** `services/api/src/admin/tenantRouting.ts` is written — it opens a per-tenant database as the single permitted call site and re-checks the registry, the derived name, the tenant status and the schema version on every open — and `tenant_registry` holds the mapping. **But it has no callers.** `provision()` opens the one database it was given, every merchant lives in that single file, and no per-tenant migration runner or N-backup tooling exists. **The effective isolation boundary today is server-side authorization plus merchant-scoped SQL**, not file separation: `context.merchantId` comes from the session row and a client-supplied id is a consistency check that is then discarded. That boundary is real and tested (`tests/persistence/merchant-isolation.test.ts`, `tests/auth/authorization.test.ts`), but it is **not** the model D103 chose. Per D113 this is accepted for the single-shop training deployment only, and **must not be described as active per-tenant routing**. | [[Decision Log]] D103, D113 | **OPEN — routing written, not wired; shared file in use** |
| A87 | An applicant can register a shop | Medium | **There is no registration form.** `merchant_applications` can hold an application through eleven states, but nothing creates one: the only way a merchant, device and operator come into being is still `provision()` in `apps/merchant-pos/src/cli.ts`, a command-line function that creates all three at once and marks the shop `ACTIVE` with no review. | [[Decision Log]] D104 | **OPEN — schema only** |
| A88 | MFA and the passkey are working | **High** | **TOTP is implemented; the passkey is not.** `services/api/src/auth/totp.ts` is RFC 6238 verified against the RFC's own published test vectors, enrolment issues a secret once and the code is never shown on the screen that asks for it, and a lost authenticator can be reset by a Platform Owner under step-up with the target's sessions revoked. Verified in the running console, not only in tests. WebAuthn remains unimplemented — it needs a script surface the console deliberately does not have (`script-src 'none'`). | [[Decision Log]] D105 | **PARTIAL — TOTP done, passkey open** |
| A89 | `telga.et` and `mulesoo.et` are available and will be Telga's domains | **Medium** | **Neither is registered.** They are placeholders in `apps/mobile/shell.config.json`, which is the allow-list the Android shell will connect to. The Android app builds and installs today but can reach nothing until a domain exists, is served over real HTTPS, and is put in that file. A `.et` registration may also carry local presence requirements nobody has checked. | [[Decision Log]] D106 · [[Android Release and Play Store]] | **OPEN — blocks the Play Store listing** |
| A90 | The Android app carries Telga's own branding | **Medium** | **The icon does; the store listing does not exist.** Capacitor scaffolds its own logo into every mipmap and splash drawable, so the first build wore another project's mark. `npm run mobile:icons` now rescales the founder's artwork from `apps/merchant-pos/assets/icon-512.png` and `icon-maskable-512.png` — the same files the web app serves — so the phone icon and the browser icon are one picture. Nothing is redrawn, per the instruction in `logo.ts`. Still missing: the feature graphic, screenshots and description a Play listing requires, in English and Amharic. | [[Android Release and Play Store]] | **OPEN — listing artwork outstanding** |
| A91 | The repository layout matches `CLAUDE.md` §27 | **Low** | **Four deviations, none of which was recorded until now.** `packages/ledger` does not exist — the append-only ledger lives in `packages/persistence` (`repositories/ledger.ts`) with its rules in `packages/domain`, because a separate package would have split a table from the invariants that guard it. `packages/design-system` does not exist — tokens and components live in `apps/merchant-pos/src/ui`, there being exactly one consumer. `infra/` does not exist because nothing is deployed yet (A89). `apps/android/` is `apps/mobile/` per D106. The first three are defensible; **none had been written down**, which is the actual finding — §30 requires assumptions to be documented, and an undocumented deviation is indistinguishable from an oversight. | [[Decision Log]] D106 | **OPEN — recorded, not yet ratified** |
| A92 | An APK that is valid will install | **Medium** | **Not on a real phone, necessarily.** The release APK is signed (v1+v2+v3), architecture-independent, minSdk 24, zip-intact, and installs and launches on Android 14 — and still returned "App not installed" on the owner's device. Everything the build controls has been eliminated; the remaining causes are Play Protect, a conflicting package, a broken transfer, or a device policy, and none can be told apart without the device. `npm run mobile:diagnose` reports the real code. **Until an install is confirmed on a real phone, no claim that the app works on Android is supportable.** | [[APK Install Troubleshooting]] | **OPEN — unverified on real hardware** |
| A93 | The address Railway's edge forwards from is `100.0.0.0/8` | **High** | **RESOLVED 2026-09-07 — observed, and the community figure was wrong in the dangerous direction.** One browsing session produced four addresses from a rotating pool: `100.64.0.2`, `100.64.0.3`, `100.64.0.4`, `100.64.0.5`. All lie in **`100.64.0.0/10`**, the RFC 6598 carrier-grade NAT block — reserved, not publicly routable. `TELGA_TRUST_PROXY` is set to that range and sign-in works. **The caution this row existed to enforce paid off:** the community's `100.0.0.0/8` covers `100.0.0.1` and `100.63.255.254`, which are publicly allocated, so configuring it would have let any host on the internet spoof `X-Forwarded-Proto` — `R26` exactly — and it would have *looked* correct, because sign-in would have started working. Verified against Telga's own parser: `/10` accepts the observed addresses and rejects `100.0.0.1`, `100.63.255.254` and `100.128.0.1`. **A rotating pool also rules out pinning a single address**, which would have broken on the next request. | [[Railway Deployment Checklist]] · [[TLS and Proxy Configuration]] | **RESOLVED 2026-09-07 — `100.64.0.0/10`, observed and verified** |
| A94 | Telga will behave on real infrastructure as it does locally | **Medium** | **RESOLVED 2026-09-07 — it now runs on Railway.** The volume mounts, the ledger opens at `/data/telga.sqlite` on that volume, all fourteen migrations apply under a single writer, the recovery worker sweeps every 30s against the same file, the POS listens on the port the domain targets, the host allowlist answers for `healthcheck.railway.app` and the generated domain, and the `TRAINING MODE — NO REAL VALUE` banner prints from both the supervisor and the POS. **What this does not cover, each on its own row:** sign-in over the proxy (`A93`, trust still `127.0.0.1/32`), volume persistence across a redeploy, and a backup taken off Railway and restored elsewhere (`R31`, launch gate 10). | [[Railway Deployment Checklist]] · [[Decision Log]] D116 | **RESOLVED 2026-09-07 — deployed and serving** |
| A95 | Running Telga on Railway costs about $2.50/month | **Medium** | **An estimate from published rates, not a dashboard figure.** Derived from RAM $10/GB/month and CPU $20/vCPU/month against roughly 0.2 GB and near-idle CPU for two Node processes. No Railway account exists, so no actual usage has ever been measured. Two consequences if it is wrong in the upward direction: the Free plan's **$1/month** credit is exhausted sooner than the estimated twelve days, and **workloads stop when it runs out**. Volume data is then deleted after 30 days — which is the ledger, so off-platform backups are mandatory. | [[Railway Deployment Checklist]] | **OPEN — confirm on the billing page before any spend** |
| A96 | `packages/persistence` can be pointed at Postgres by writing one more file | **High** | **False, and it had been written down as true.** `driver/types.ts` said the swap meant a second implementation "and changing nothing else". The interface has *zero* Promise-returning methods and a synchronous `transaction<T>(work: () => T): T`; every Node Postgres client is async, so the swap forces ~80 files and 21 transaction call sites async, and the claim lease (A37/R16) needs re-proving against a different engine's locking. Corrected in both the note and the code comment on 2026-09-05. The finding is not just the cost — it is that a sizing claim nobody had tested was sitting in a header comment being believed. | [[Vercel Deployment Limits]] · [[Decision Log]] D108 | **RESOLVED 2026-09-05 — corrected; the migration itself stays deferred** |

| A97 | The recipient salt is a deployment secret, as [[Security Model]] says it is | **High** | **Only half true in code, and the other half is public.** The recovery worker reads `TELGA_RECIPIENT_SALT` but falls back to the literal `cli-salt-not-a-production-secret`, which is committed and therefore public — an attacker with the repository can recompute any digest made with it, which is exactly the rainbow-table lookup the salt exists to prevent. The POS does not read the variable at all: `cli.ts` uses a fresh `randomUUID()` per process, deliberately, because the training build stores no real recipient. So the two processes salt differently and the POS's salt changes on every restart — on Railway, every redeploy. **Consequence today:** none, because recipients are simulated and no digest is compared across processes. **Consequence the day a real recipient is stored:** the control does not work. Mitigation for the training deployment is to set `TELGA_RECIPIENT_SALT` in Railway so the public fallback is never used; the POS asymmetry needs a decision before any real recipient exists. | [[Security Model]] · [[Railway Deployment Checklist]] | **OPEN — set the variable on Railway; resolve the POS asymmetry before real recipients** |
| A98 | A Railway training deployment could be mistaken for a production Telga | **Medium** | The same shape as R30, on a different platform. It will have a real HTTPS URL, real uptime and a real database, and none of that makes it production: it runs a mock provider, simulated funds, a `TRAINING MODE — NO REAL VALUE` banner, and every regulated capability switched off with 0 of 10 launch gates cleared. **Controls:** no custom domain (`telga.pro` stays unattached), no merchant is given the address, and [[Railway Deployment Checklist]] and `SECURITY.md` both say so in terms. The risk is not technical — it is that somebody describes the URL to a merchant or a partner as "Telga, live". | [[Railway Deployment Checklist]] · [[Decision Log]] D108 | **OPEN — a communication control, not a code one** |

| A99 | A feature flag gates the feature it is named after | **High** | **Two of them did not, and the fault was invisible while they were on.** `card.simulated` gated `/pay/card` only, leaving nine Telga Pay routes served with the flag off; `deposits.training` gated `/deposit`, a path no route in the system uses, so it gated nothing whatever. Both were found by switching a flag off and testing every path, not by reading the table — a route table entry that matches nothing looks identical to one that matches everything until something depends on it. The table now names `/pay` and `/api/training/pay/deposits`, and `tests/ui/card-screens.test.ts` asserts the refusal across all twelve Pay paths for an operator, an unauthenticated caller, and a POST. **The general risk is unchanged:** nothing checks that a `FEATURE_ROUTES` prefix corresponds to a route that exists, so a future entry can be dead on arrival the same way. | [[Feature Flags]] · [[Decision Log]] D112 | **OPEN — the two known cases are fixed; the class is not** |
| A100 | Disabling Telga Pay leaves training top-up usable | Medium | **It does not, and the founder has accepted that rather than moved the screen.** The training deposit is the only in-app way to credit a simulated float and its single screen was `/pay/deposit`, inside the tree `card.simulated` now refuses. **Decision D113: CLI provisioning is the only supported way to create the training balance for the first Railway merchant** — `--training-float <birr>` at provision time. The endpoint and `deposits.training` survive and stay strictly guarded: `application/deposits.ts` refuses any mode but `TRAINING` before reading an amount, bounds the credit to 10-50,000 birr, posts a balanced append-only entry against `BANK_CLEARING`, audits `TRAINING_DEPOSIT_CREDITED`, and imports no HTTP client or provider. **No real funding, no merchant funds and no customer money are involved at any point.** Moving the screen to `/topup` is explicitly deferred, not forgotten. | [[Decision Log]] D70, D112, D113 | **OPEN — CLI-only top-up accepted for the training deployment** |

| A101 | Telga can print a receipt on a POS machine | **High** | **No printer integration exists at all.** Searched the whole repository: no `ReceiptPrinter` interface, no ESC/POS encoder or library, no Bluetooth support or permission, no USB support or `UsbManager`, no network/TCP printer client, no Android Print Framework or `PrintManager`, no vendor POS SDK, and no printer selection, paper-width, encoding, density, copies, reconnect or saved-printer settings. `AndroidManifest.xml` requests **no** printing permission. What exists is a receipt **screen** with `@media print` CSS in `apps/merchant-pos/src/ui/document.ts` — there is not even a `window.print()` call. The vault named a `ReceiptPrinter` port in [[Architecture]] and [[Receipt Specification]] that was never written, which is the discrepancy this assumption records. **Printing is a build project (B8), not a test task**, and it needs the smart-POS make, model and Android version first, because a built-in POS printer normally requires a vendor SDK. **No claim of printer support may be made until it is tested on real hardware with real paper.** | [[Receipt Specification]] · [[Architecture]] · [[Decision Log]] D113 | **OPEN — not implemented, hardware details required** |
| A102 | `telga.et` is Telga's domain | Medium | **It is not, and it never was registered.** The founder has purchased **`telga.pro`**. Eight files still name `telga.et` or `mulesoo.et`: `ASSUMPTIONS.md`, [[Decision Log]], [[Railway Deployment Checklist]], [[Android Release and Play Store]], and four code/config files — `apps/mobile/shell.config.json`, `apps/mobile/capacitor.config.ts`, `apps/mobile/www/connect.js` and `tests/mobile/connect.test.ts`. **Per D113 none of them is changed yet.** `telga.pro` is purchased but deliberately **not attached, not configured and not pointed at anything**; the first Railway deployment uses the Railway-generated HTTPS address. The Android allow-list and backend URL are **not** to be edited until the approved domain-configuration phase, so the app currently cannot connect to a Railway host — which is correct, because none exists. | [[Decision Log]] D106, D113 · [[Railway Deployment Checklist]] | **OPEN — telga.pro purchased, unconfigured by decision** |

| A103 | A native dependency will install cleanly on a hosting platform | **Medium** | **RESOLVED 2026-09-07 on Railway.** Three builds failed first. `better-sqlite3` has a `binding.gyp` and no `install` script, so npm runs an implicit `node-gyp rebuild`; the Nixpacks image has no Python. Build 1 was misread as a Node-version problem; build 2 changed `buildCommand`, which Nixpacks' own `install` phase runs before; build 3 overrode `[phases.install]` and installed in 4 seconds with no `node-gyp`. **The claim is now proven on the platform it was about:** `verify-binding.mjs` printed `[telga] better-sqlite3 verified on linux-x64, node v22.19.0` on the Railway build host. Every earlier proof ran on Windows and loaded `win32-x64.node`; CI runs Linux but compiles from source, so **Linux + prebuild had never executed anywhere** until this build. | [[Decision Log]] D114, D115, D116 | **RESOLVED 2026-09-07 — `linux-x64` prebuild verified** |
| A104 | The deployment image can run the test suite | Low | **It cannot, by design.** `--ignore-scripts` skips `esbuild`'s postinstall, so the binary vitest needs is absent on the Railway image. Deliberate and costless: tests run locally and in CI against a full install, and a deployment image is not a test runner. **It would matter** the day somebody tries to debug a failure by running the suite on the deployed container and finds a tool missing rather than a test failing. | [[Decision Log]] D115 · [[Testing Strategy]] | **OPEN — accepted, recorded so it is not rediscovered** |

---

*Last updated 2026-09-05. Mirrored by a pilot assumptions register kept outside this repository.*
